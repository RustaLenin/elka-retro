# TODO: Рефакторинг использования UI-Kit компонентов

Этот файл содержит список задач по замене нативных HTML элементов на компоненты из UI-Kit для обеспечения стилистического единства проекта.

**Важно:** Перед началом работы ознакомьтесь с правилами использования UI-Kit:
📖 [UI_KIT_USAGE_RULES.md](./UI_KIT_USAGE_RULES.md)

## Принцип

Все кнопки должны использовать компонент `<ui-button>` из UI-Kit. Если нужный визуальный стиль или поведение недоступны в UI-Kit, это повод доработать UI-Kit, а не создавать собственные элементы.

## Список задач

### 1. Компонент `toy-instance-card`

**Файл:** `components/toy-instance/toy-instance-card/toy-instance-card-template.js`

**Строки:** 202, 207

**Текущий код:**
```html
<button class="toy-instance-card_cart-btn ..." type="button" aria-label="...">
  <ui-icon name="..." size="small"></ui-icon>
</button>
<button class="toy-instance-card_details-btn" type="button">
  Больше подробностей
</button>
```

**Задача:** Заменить на `<ui-button>` с соответствующими типами и атрибутами.

**Примечания:**
- Кнопка корзины уже имеет иконку, используйте `icon` атрибут
- Кнопка "Больше подробностей" может использовать тип `ghost` или `secondary`

---

### 2. Компонент `cart-page`

**Файл:** `components/cart/cart-page/cart-page-template.js`

**Строка:** 56

**Текущий код:**
```html
<button 
  class="cart-page_checkout-btn" 
  type="button"
>
  Оформить заказ
</button>
```

**Задача:** Заменить на `<ui-button type="primary">` с полной шириной (`width="full_width"`).

---

### 3. Компонент `order-wizard`

**Файл:** `components/cart/order-wizard/order-wizard-template.js`

**Строки:** 37, 44

**Текущий код:**
```html
<button 
  class="order-wizard_prev-btn" 
  type="button"
  ${currentStep === 1 ? 'disabled' : ''}
>
  Назад
</button>
<button 
  class="order-wizard_next-btn" 
  type="button"
>
  ${currentStep === steps.length ? 'Завершить заказ' : 'Далее'}
</button>
```

**Задача:** Заменить на `<ui-button>`:
- Кнопка "Назад": `type="ghost"` или `type="secondary"`
- Кнопка "Далее": `type="primary"`

**Примечания:** Убедитесь, что атрибут `disabled` корректно обрабатывается через `ui-button`.

---

### 4. Компонент `step-auth`

**Файл:** `components/cart/order-wizard/steps/step-auth-template.js`

**Строки:** 30, 41

**Текущий код:**
```html
<button 
  class="step-auth_choice step-auth_login-btn ..." 
  type="button"
>
  <ui-icon name="account" size="medium"></ui-icon>
  <div class="step-auth_choice_content">
    <h4 class="step-auth_choice_title">Войти</h4>
    <p class="step-auth_choice_text">У меня уже есть аккаунт</p>
  </div>
</button>
<button 
  class="step-auth_choice step-auth_register-btn ..." 
  type="button"
>
  <ui-icon name="user_plus" size="medium"></ui-icon>
  <div class="step-auth_choice_content">
    <h4 class="step-auth_choice_title">Зарегистрироваться</h4>
    <p class="step-auth_choice_text">Создать новый аккаунт</p>
  </div>
</button>
```

**Задача:** Заменить на `<ui-button>` с соответствующими типами.

**Примечания:** 
- Это карточки выбора, возможно потребуется новый тип кнопки в UI-Kit (например, `card`) или использование существующего типа с дополнительными стилями
- Если текущий дизайн не укладывается в существующие типы, рассмотрите возможность добавления нового типа в `ui-button`

---

### 5. Компонент `step-confirmation`

**Файл:** `components/cart/order-wizard/steps/step-confirmation-template.js`

**Строка:** 133

**Текущий код:**
```html
<button 
  class="step-confirmation_submit-btn" 
  type="button"
  ${isSubmitting ? 'disabled' : ''}
>
  ${isSubmitting ? 'Создание заказа...' : 'Подтвердить заказ'}
</button>
```

**Задача:** Заменить на `<ui-button type="primary">` с поддержкой состояния `loading`.

**Примечания:** Используйте атрибут `loading` компонента `ui-button` для отображения состояния загрузки.

---

### 6. Компонент `site-header`

**Файл:** `components/navigation/site-header/site-header-template.js`

**Строки:** 24, 39, 79

**Текущий код:**
```html
<button class="site_header-action_button" aria-label="Search">
  <ui-icon name="search" size="medium" class="site_header-action_icon"></ui-icon>
</button>
<button class="site_header-menu_button MobileMenuToggle" aria-label="Open menu">
  <ui-icon name="menu" size="medium" class="site_header-action_icon"></ui-icon>
</button>
<button class="site_header-mobile_link CloseMobileMenu" data-action="login" style="...">
  <ui-icon name="account" size="medium" class="site_header-nav_icon"></ui-icon>
  <span>Войти</span>
</button>
```

**Задача:** Заменить на `<ui-button>` с типом `ghost` для иконок без текста.

**Примечания:**
- Для кнопок с только иконкой используйте `ui-button` без `label`, только с `icon`
- Возможно потребуется новый тип или модификатор для кнопок-иконок

---

### 7. Компонент `user-menu`

**Файл:** `components/user-profile/user-menu/user-menu-template.js`

**Строки:** 11, 20, 38

**Текущий код:**
```html
<button class="user-menu__button user-menu__button--login" aria-label="Войти" title="Войти">
  <ui-icon name="login" size="medium" class="user-menu__icon"></ui-icon>
</button>
<button class="user-menu__button" aria-label="Меню пользователя" aria-expanded="${open}" title="...">
  <span class="user-menu__avatar">${initials}</span>
</button>
<button data-action="logout" class="user-menu__link user-menu__link--logout">
  <ui-icon name="close" size="small"></ui-icon>
  <span>Выйти</span>
</button>
```

**Задача:** Заменить на `<ui-button>` где это уместно.

**Примечания:**
- Кнопка с аватаром может требовать специальной обработки, так как содержит кастомный контент
- Кнопка "Выйти" в выпадающем меню может использовать тип `ghost` или `danger`

---

### 8. Компонент `tab-navigation`

**Файл:** `components/user-profile/profile-page/tab-navigation/tab-navigation-template.js`

**Строка:** 13

**Текущий код:**
```html
<button 
  class="tab-navigation__button ..." 
  data-tab-id="${tab.id}"
  aria-label="${tab.label}"
  ${tab.id === activeTab ? 'aria-current="page"' : ''}
>
  <span class="tab-navigation__button-text">${tab.label}</span>
</button>
```

**Задача:** Заменить на `<ui-button>` с типом `ghost` или создать специализированный компонент для табов.

**Примечания:**
- Табы могут требовать специального компонента, если текущий дизайн не укладывается в стандартные типы кнопок
- Рассмотрите возможность создания `ui-tab` компонента в UI-Kit

---

### 9. Компонент `catalog-sidebar`

**Файл:** `components/catalog/sidebar/sidebar-template.js`

**Строка:** 55

**Текущий код:**
```html
<button type="button" class="catalog-sidebar__reset-button" data-reset-filters>
  Сбросить фильтры
</button>
```

**Задача:** Заменить на `<ui-button type="ghost">` или `type="secondary"`.

---

### 10. Компонент `ny-accessory-card`

**Файл:** `components/ny-accessory/ny-accessory-card/ny-accessory-card-template.js`

**Строка:** 102

**Текущий код:**
```html
<button class="ny-accessory-card__cart-button ..." type="button" aria-label="...">
  <ui-icon name="..." size="small"></ui-icon>
</button>
```

**Задача:** Заменить на `<ui-button>` с соответствующими типами и атрибутами, аналогично `toy-instance-card`.

**Примечания:**
- Кнопка корзины уже имеет иконку, используйте `icon` атрибут
- Используйте тип `ghost` или `danger` в зависимости от состояния `inCart`

---

### 11. Компоненты UI-Kit (исключения)

**Файлы:**
- `components/ui-kit/form/inputs/segmented-toggle/segmented-toggle-template.js` (строка 32)
- `components/ui-kit/form/chips/filter-chip/filter-chip-template.js` (строка 31)

**Примечания:** 
Эти компоненты являются частью UI-Kit и используют `<button>` для специфичных целей (переключатели, чипы). Это допустимо, так как они являются специализированными компонентами UI-Kit, а не обычными кнопками действий. Однако, если в будущем будет создан универсальный компонент для таких случаев, стоит рассмотреть рефакторинг.

---

## Дополнительные замечания

1. **Проверка стилей:** После замены кнопок убедитесь, что стили компонентов корректно применяются. Возможно потребуется обновить CSS селекторы.

2. **Обработка событий:** Убедитесь, что обработчики событий корректно работают с `ui-button`. Компонент поддерживает атрибуты `event`, `action`, `href` для различных сценариев.

3. **Доступность:** `ui-button` автоматически устанавливает правильные ARIA атрибуты, но проверьте их при необходимости.

4. **Новые типы кнопок:** Если текущий дизайн не укладывается в существующие типы (`primary`, `secondary`, `warning`, `ghost`, `danger`), рассмотрите возможность добавления нового типа в `components/ui-kit/button/button-styles.css` и обновите документацию.

## После выполнения

После завершения рефакторинга:
1. Удалите неиспользуемые CSS стили для старых кнопок
2. Обновите этот файл, отметив выполненные задачи
3. Проверьте, что все кнопки работают корректно
4. Убедитесь, что визуально ничего не сломалось

---

**Дата создания:** 2024
**Последнее обновление:** 2024

