# План разработки каталога

## ✅ Выполнено

1. **Единый стор (`catalog-store.js`)** — создан и интегрирован в `catalog-page.js`
   - Централизованное управление состоянием
   - Синхронизация с URL
   - Подписки для компонентов
   - Хранение метаданных

2. **Интеграция стора в `catalog-page.js`**
   - Заменено локальное состояние на использование стора
   - Подписки на изменения состояния и метаданных
   - Обновление метаданных через стор

3. **Filter Registry (`sidebar/filter-registry.js`)** — реализован
   - Чтение полей из `window.data_model` с `show_in_filters: true`
   - Маппинг типов полей (taxonomy, number, currency) в типы UI Kit
   - Генерация конфигураций фильтров для `ui-form-controller`
   - Подготовка `dataSource` из `window.taxonomy_terms`
   - Нормализация и валидация значений фильтров
   - Функции: `getFiltersForMode()`, `normalizeFilterValue()`, `getFilterKey()`

4. **Mode Toggle (`sidebar/mode-toggle.js`)** — реализован
   - Компонент переключателя режимов (Типы / Экземпляры)
   - Интеграция со стором: подписка на изменения mode, обновление при клике
   - Сброс страницы и фильтров при смене режима
   - Синхронизация визуального состояния с актуальным режимом
   - Использование `ui-segmented-toggle` из UI Kit

5. **Catalog Sidebar (`sidebar/catalog-sidebar.js`)** — реализован
   - Главный компонент сайдбара, оркестратор фильтров
   - Интеграция Mode Toggle
   - Динамическое отображение фильтров на основе режима
   - Кнопка сброса фильтров
   - Интеграция в `catalog-page.js`

---

## 📋 План дальнейших работ

### Этап 1: Базовые компоненты фильтров (высокий приоритет)

#### 1.1. Filter Registry (`sidebar/filter-registry.js`) ✅
**Цель:** Маппинг полей из `data-model.json` в конфигурации фильтров

**Статус:** Реализовано
- ✅ Реализован `getFiltersForMode(mode)` — возвращает список фильтров для режима
- ✅ Интеграция с `window.data_model` и `window.taxonomy_terms`
- ✅ Маппинг типов полей (`taxonomy`, `number`, `currency`) в типы UI Kit
- ✅ Валидация и нормализация ключей фильтров через `normalizeFilterValue()` и `getFilterKey()`

**Зависимости:** `core/data-model.json`, `window.taxonomy_terms`

**Файлы:**
- `components/catalog/sidebar/filter-registry.js` ✅

---

#### 1.2. Mode Toggle (`sidebar/mode-toggle.js`) ✅
**Цель:** Переключатель между режимами "Типы" и "Экземпляры"

**Статус:** Реализовано
- ✅ Создан компонент переключателя (используя `ui-segmented-toggle` из UI Kit)
- ✅ Интеграция со стором: подписка на `mode`, обновление при клике
- ✅ Сброс страницы и фильтров при смене режима
- ✅ Визуальная обратная связь (активный режим через ui-segmented-toggle)
- ✅ Синхронизация состояния при внешних изменениях (URL, программные обновления)

**Зависимости:** UI Kit `ui-segmented-toggle` ✅

**Файлы:**
- `components/catalog/sidebar/mode-toggle.js` ✅
- `components/catalog/sidebar/mode-toggle-template.js` ✅
- `components/catalog/sidebar/sidebar-styles.css` (стили добавлены) ✅

---

#### 1.3. Catalog Sidebar (`sidebar/catalog-sidebar.js`) ✅
**Цель:** Главный компонент сайдбара, оркестратор фильтров

**Статус:** Реализовано (базовая версия)
- ✅ Инициализация и рендеринг структуры сайдбара
- ✅ Интеграция `mode-toggle`
- ✅ Динамическое отображение фильтров на основе режима (заглушка до интеграции с UI Form Kit)
- ✅ Кнопка сброса фильтров
- ✅ Подписка на изменения стора и обновление UI
- ⏳ Интеграция с UI Form Kit (`ui-form-controller`) — следующий этап
- ⏳ Обработка изменений фильтров и обновление стора — следующий этап

**Зависимости:** 
- `filter-registry.js` ✅
- `mode-toggle.js` ✅
- UI Kit `ui-form-controller` с autosubmit (ожидается)

**Файлы:**
- `components/catalog/sidebar/catalog-sidebar.js` ✅
- `components/catalog/sidebar/sidebar-template.js` ✅
- `components/catalog/sidebar/sidebar-styles.css` ✅

---

#### 1.4. Type Filters (`sidebar/type-filters.js`)
**Цель:** Генерация конфигурации фильтров для режима "Типы игрушек"

**Задачи:**
- [ ] Реализовать `getTypeFilterDefinitions()` — возвращает массив конфигураций
- [ ] Маппинг таксономий в `select-single` / `select-multi` на основе `multi_choose`
- [ ] Подготовка `dataSource` из `window.taxonomy_terms`
- [ ] Обработка диапазонов (год производства через `ui-input-range` после реализации)

**Зависимости:**
- `filter-registry.js`
- UI Kit `ui-input-range` (требование 2)
- UI Kit `ui-select-single` / `ui-select-multi`

**Файлы:**
- `components/catalog/sidebar/type-filters.js`

---

#### 1.5. Instance Filters (`sidebar/instance-filters.js`)
**Цель:** Генерация конфигурации фильтров для режима "Экземпляры игрушек"

**Задачи:**
- [ ] Реализовать `getInstanceFilterDefinitions()` — возвращает массив конфигураций
- [ ] Маппинг таксономий экземпляров
- [ ] Подготовка `dataSource` из `window.taxonomy_terms`
- [ ] Обработка диапазона цен (через `ui-input-range`)

**Зависимости:**
- `filter-registry.js`
- UI Kit `ui-input-range` (требование 2)
- UI Kit `ui-select-single` / `ui-select-multi`

**Файлы:**
- `components/catalog/sidebar/instance-filters.js`

---

### Этап 2: Интеграция UI Form Kit (высокий приоритет)

#### 2.1. Интеграция формы фильтров
**Цель:** Подключение `ui-form-controller` к сайдбару

**Задачи:**
- [ ] Создать конфигурацию формы на основе `getTypeFilterDefinitions()` / `getInstanceFilterDefinitions()`
- [ ] Настроить `autosubmit` с debounce (500ms)
- [ ] Реализовать `pipeline.submit` — обновление стора при изменении фильтров
- [ ] Синхронизация начальных значений из стора
- [ ] Обработка событий `ui-form:change` и обновление стора

**Зависимости:**
- UI Kit `ui-form-controller` с autosubmit (требование 1)
- `catalog-sidebar.js`
- `type-filters.js` / `instance-filters.js`

**Файлы:**
- `components/catalog/sidebar/catalog-sidebar.js` (обновить)

---

### Этап 3: Toolbar компоненты (средний приоритет)

#### 3.1. Search Box (`toolbar/search-box.js`)
**Цель:** Поле поиска по названию

**Задачи:**
- [ ] Создать компонент поиска (используя `ui-input-text`)
- [ ] Debounce ввода (300ms)
- [ ] Интеграция со стором: обновление `search` при вводе
- [ ] Подписка на стор: отображение текущего значения поиска
- [ ] Кнопка очистки поиска

**Зависимости:**
- UI Kit `ui-input-text`
- Стор

**Файлы:**
- `components/catalog/toolbar/search-box.js`
- `components/catalog/toolbar/search-box-template.js`
- `components/catalog/toolbar/search-box-styles.css`

---

#### 3.2. Sort Control (`toolbar/sort-control.js`)
**Цель:** Выбор сортировки

**Задачи:**
- [ ] Создать компонент сортировки (используя `ui-select-single`)
- [ ] Генерация опций сортировки на основе `window.catalogSettings.sort_options`
- [ ] Интеграция со стором: обновление `sort` при выборе
- [ ] Подписка на стор: отображение текущей сортировки

**Зависимости:**
- UI Kit `ui-select-single`
- Стор

**Файлы:**
- `components/catalog/toolbar/sort-control.js`
- `components/catalog/toolbar/sort-control-template.js`
- `components/catalog/toolbar/sort-control-styles.css`

---

#### 3.3. Active Filters Summary (`toolbar/filters-summary.js`)
**Цель:** Отображение активных фильтров в виде чипсов

**Задачи:**
- [ ] Создать компонент для отображения чипсов (используя `ui-filter-chip` / `ui-filters-summary` после реализации)
- [ ] Подписка на стор: получение активных фильтров
- [ ] Генерация чипсов на основе `state.filters`
- [ ] Обработка удаления фильтра (клик на крестик)
- [ ] Обработка клика на чипс (скролл к соответствующему фильтру в сайдбаре)

**Зависимости:**
- UI Kit `ui-filter-chip` / `ui-filters-summary` (требование 4)
- Стор

**Файлы:**
- `components/catalog/toolbar/filters-summary.js`
- `components/catalog/toolbar/filters-summary-template.js`
- `components/catalog/toolbar/filters-summary-styles.css`

---

#### 3.4. Catalog Toolbar (`toolbar/catalog-toolbar.js`)
**Цель:** Главный компонент тулбара, оркестратор элементов

**Задачи:**
- [ ] Инициализация и рендеринг структуры тулбара
- [ ] Интеграция `search-box`, `sort-control`, `filters-summary`
- [ ] Управление layout (адаптивность)

**Зависимости:**
- `search-box.js`
- `sort-control.js`
- `filters-summary.js`

**Файлы:**
- `components/catalog/toolbar/catalog-toolbar.js`
- `components/catalog/toolbar/toolbar-template.js` (обновить)
- `components/catalog/toolbar/toolbar-styles.css` (обновить)

---

### Этап 4: Иерархический фильтр категорий (низкий приоритет)

#### 4.1. Category Tree Filter (`sidebar/category-tree-filter.js`)
**Цель:** Кастомный компонент для иерархического дерева категорий

**Задачи:**
- [ ] Создать Web Component для дерева категорий
- [ ] Построение дерева из `window.taxonomy_terms['category-of-toys']` (используя `parent`)
- [ ] Раскрытие/сворачивание веток
- [ ] Выбор категорий (чекбоксы)
- [ ] Интеграция со стором: обновление `filters['category-of-toys']`
- [ ] Визуальное отображение иерархии (отступы, индикаторы)

**Зависимости:**
- `window.taxonomy_terms['category-of-toys']`
- Стор

**Файлы:**
- `components/catalog/sidebar/category-tree-filter.js`
- `components/catalog/sidebar/category-tree-filter-template.js`
- `components/catalog/sidebar/category-tree-filter-styles.css`

---

#### 4.2. Shared Category Filter (`sidebar/shared-category-filter.js`)
**Цель:** Обёртка для `category-tree-filter`, интеграция в сайдбар

**Задачи:**
- [ ] Интеграция `category-tree-filter` в `catalog-sidebar`
- [ ] Синхронизация с стором
- [ ] Отображение в обоих режимах (типы и экземпляры)

**Зависимости:**
- `category-tree-filter.js`
- `catalog-sidebar.js`

**Файлы:**
- `components/catalog/sidebar/shared-category-filter.js` (обновить)

---

### Этап 5: Интеграция компонентов в страницу (высокий приоритет)

#### 5.1. Интеграция Sidebar в Catalog Page
**Задачи:**
- [ ] Инициализация `CatalogSidebar` в `catalog-page.js`
- [ ] Передача стора в сайдбар
- [ ] Обработка событий от сайдбара

**Файлы:**
- `components/catalog/catalog-page.js` (обновить)

---

#### 5.2. Интеграция Toolbar в Catalog Page
**Задачи:**
- [ ] Инициализация `CatalogToolbar` в `catalog-page.js`
- [ ] Передача стора в тулбар
- [ ] Обработка событий от тулбара

**Файлы:**
- `components/catalog/catalog-page.js` (обновить)

---

### Этап 6: Бэкенд метаданные (средний приоритет)

#### 6.1. Facet Counts в REST API
**Цель:** Отдача счётчиков для каждого значения фильтра

**Задачи:**
- [ ] Расширить `Catalog_Toy_Type_Service::get_filter_metadata()` — добавить counts
- [ ] Расширить `Catalog_Toy_Instance_Service::get_filter_metadata()` — добавить counts
- [ ] Обновить `Catalog_REST_Controller` — включить `facet_counts` в ответ
- [ ] Оптимизация запросов (кеширование, индексы)

**Файлы:**
- `core/catalog/catalog-toy-type-service.php` (обновить)
- `core/catalog/catalog-toy-instance-service.php` (обновить)
- `core/catalog/catalog-rest-controller.php` (обновить)

---

#### 6.2. Available Filters в REST API
**Цель:** Отдача списка доступных фильтров с их опциями

**Задачи:**
- [ ] Генерация `available_filters` на основе текущих фильтров
- [ ] Фильтрация опций (показывать только те, что дают результаты)
- [ ] Обновление формата ответа REST API

**Файлы:**
- `core/catalog/catalog-toy-type-service.php` (обновить)
- `core/catalog/catalog-toy-instance-service.php` (обновить)

---

## 🎯 Приоритеты выполнения

### Сначала (блокирует остальное):
1. **Filter Registry** — нужен для всех фильтров
2. **Mode Toggle** — базовый переключатель режимов
3. **Type Filters / Instance Filters** — генерация конфигураций
4. **Интеграция UI Form Kit** — подключение формы к сайдбару

### Затем (улучшает UX):
5. **Search Box** — поиск по названию
6. **Sort Control** — сортировка
7. **Filters Summary** — отображение активных фильтров
8. **Интеграция компонентов в страницу**

### Потом (дополнительные фичи):
9. **Category Tree Filter** — иерархический фильтр
10. **Facet Counts** — счётчики в бэкенде

---

## 📝 Заметки

- Все компоненты должны использовать единый стор через `getCatalogStore()`
- UI Kit компоненты будут доступны после реализации требований из `UI_KIT_REQUIREMENTS.md`
- При отсутствии UI Kit компонентов можно использовать временные заглушки
- Тестирование каждого этапа перед переходом к следующему

---

## 🔗 Связанные документы

- `components/catalog/README.md` — архитектура каталога
- `components/catalog/UI_KIT_REQUIREMENTS.md` — требования к UI Kit
- `core/data-model.json` — модель данных

