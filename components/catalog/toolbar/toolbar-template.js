/**
 * Toolbar template helpers.
 *
 * TODO:
 * - Export renderToolbarShell() containing search box slot, sort select, and chip container.
 * - Export renderActiveFilterChip({ label, value }) helper.
 */

export const renderToolbarShell = () => {
  // TODO: return markup string/fragment for toolbar.
  return '';
};

export const renderActiveFilterChip = (/* chip */) => {
  // TODO: return markup for a single chip with remove button.
  return '';
};

