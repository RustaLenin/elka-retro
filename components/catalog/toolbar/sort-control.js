/**
 * Sort control widget.
 *
 * TODO:
 * - Implement createSortControl({ options, onChange }).
 * - Provide mapping between UI options and backend sort keys.
 * - Handle default option and external updates.
 * - Ensure accessibility for keyboard/screen readers.
 */

export const createSortControl = (/* options */) => {
  return {
    render() {},
    setValue() {},
    getValue() {
      return null;
    },
    destroy() {},
  };
};

