# Анализ UI-kit компонентов для модуля профиля

Анализ существующих компонентов UI-kit и определение, какие компоненты достаточны для реализации модуля профиля, а какие требуют доработки.

## Достаточные компоненты

### ✅ Модальные окна
**Компонент:** `ui-modal`
- **Статус:** Полностью достаточен
- **Использование:** Все модальные формы (авторизация, регистрация, восстановление пароля) будут использовать `ui-modal`
- **Особенности:**
  - Поддержка заголовка, контента, закрытия
  - Автоматическое управление overlay
  - Поддержка клавиатуры (Escape для закрытия)
  - Методы `show()`, `hide()`, `setBodyContent()`

### ✅ Формы
**Компонент:** `ui-form-controller`
- **Статус:** Полностью достаточен
- **Использование:** Все формы в модуле профиля будут использовать `ui-form-controller`
- **Особенности:**
  - Декларативная конфигурация форм
  - Встроенная валидация
  - Pipeline для обработки данных (sanitize → validate → submit)
  - Автосохранение состояния
  - События для интеграции

### ✅ Поля ввода
**Компоненты:** `ui-input-text`, `ui-input-number`, `ui-form-checkbox`
- **Статус:** Полностью достаточны
- **Использование:**
  - `ui-input-text` - для email, имени, фамилии, телефона, адреса, паролей
  - `ui-input-number` - для числовых полей (если потребуются)
  - `ui-form-checkbox` - для "Запомнить меня", согласий

### ✅ Селекты
**Компоненты:** `ui-select-single`, `ui-select-multi`
- **Статус:** Полностью достаточны
- **Использование:**
  - `ui-select-single` - для выбора приоритета сообщения, статуса заказа
  - `ui-select-multi` - для множественного выбора (если потребуется)

### ✅ Кнопки
**Компонент:** `ui-button`
- **Статус:** Полностью достаточен
- **Использование:** Все кнопки в формах и на странице профиля

### ✅ Иконки
**Компонент:** `ui-icon`
- **Статус:** Полностью достаточен
- **Использование:** Иконки в формах, кнопках, навигации

### ✅ Уведомления
**Компонент:** `ui-notification`
- **Статус:** Полностью достаточен
- **Использование:** Уведомления об успешных действиях, ошибках

### ✅ Загрузчики
**Компонент:** `ui-loader` / `block-loader`
- **Статус:** Полностью достаточен
- **Использование:** Индикаторы загрузки при отправке форм, загрузке данных

## Компоненты, требующие доработки

### ⚠️ Переключатель вкладок (Tabs)
**Текущее состояние:** Компонент отсутствует
**Требуется:** Создать компонент `ui-tabs` или `tab-navigation`

**Требования:**
- Навигация между вкладками
- Поддержка активной вкладки
- События при смене вкладки
- Адаптивность (возможно, горизонтальный скролл на мобильных)

**Варианты реализации:**
1. Создать отдельный компонент `tab-navigation` в модуле профиля
2. Создать универсальный `ui-tabs` в UI-kit для переиспользования

**Рекомендация:** Создать универсальный `ui-tabs` в UI-kit, так как вкладки могут использоваться в других местах сайта.

### ⚠️ Поле для пароля с переключателем видимости
**Текущее состояние:** `ui-input-text` поддерживает `mask: "password"`, но нет переключателя видимости
**Требуется:** Добавить возможность показать/скрыть пароль

**Требования:**
- Кнопка/иконка для переключения видимости
- Иконка глаза (открытый/закрытый)
- Сохранение состояния видимости

**Варианты реализации:**
1. Расширить `ui-input-text` поддержкой `show-password-toggle` атрибута
2. Создать отдельный компонент `ui-input-password`

**Рекомендация:** Расширить `ui-input-text` для универсальности.

### ⚠️ Поле для подтверждения пароля
**Текущее состояние:** Можно использовать два `ui-input-text`, но нет встроенной валидации совпадения
**Требуется:** Валидация совпадения паролей

**Требования:**
- Сравнение двух полей пароля
- Показ ошибки при несовпадении
- Валидация в реальном времени

**Варианты реализации:**
1. Добавить кастомный валидатор в `validation-registry.js`
2. Использовать существующую валидацию через `ui-form-controller`

**Рекомендация:** Добавить кастомный валидатор `passwordsMatch` в `validation-registry.js`.

### ⚠️ Поле для телефона с маской
**Текущее состояние:** `ui-input-text` поддерживает `mask`, но нет готовой маски для телефонов
**Требуется:** Маска для российских телефонов

**Требования:**
- Автоматическое форматирование при вводе
- Валидация формата
- Поддержка разных форматов (+7, 8, без префикса)

**Варианты реализации:**
1. Расширить `ui-input-text` поддержкой `mask: "phone"` или `mask: "phone-ru"`
2. Использовать библиотеку для масок (например, imask.js)

**Рекомендация:** Добавить поддержку `mask: "phone-ru"` в `ui-input-text` с использованием простой логики форматирования.

## Новые компоненты для UI-kit (рекомендуется)

### 📦 ui-tabs
Универсальный компонент для вкладок.

**Атрибуты:**
- `active-tab` - ID активной вкладки
- `orientation` - `horizontal` | `vertical` (по умолчанию `horizontal`)

**Структура:**
```html
<ui-tabs active-tab="settings">
  <ui-tab id="settings" label="Настройки">...</ui-tab>
  <ui-tab id="orders" label="Заказы">...</ui-tab>
  <ui-tab id="contact" label="Обратная связь">...</ui-tab>
</ui-tabs>
```

**События:**
- `ui-tabs:change` - при смене вкладки (detail: `{ activeTab, previousTab }`)

### 📦 ui-input-password (опционально)
Специализированный компонент для паролей с переключателем видимости.

**Атрибуты:**
- Все атрибуты `ui-input-text`
- `show-toggle` - показывать ли кнопку переключения видимости (по умолчанию `true`)

**Альтернатива:** Расширить `ui-input-text` поддержкой `show-password-toggle`.

## Итоговая оценка

### ✅ Готово к использованию (90%)
Большинство компонентов UI-kit полностью готовы для использования в модуле профиля:
- Модальные окна
- Формы и контроллеры форм
- Все типы полей ввода
- Селекты
- Кнопки, иконки, уведомления, загрузчики

### ⚠️ Требуются доработки (10%)
1. **Переключатель вкладок** - создать `ui-tabs` или `tab-navigation`
2. **Поле пароля с переключателем видимости** - расширить `ui-input-text`
3. **Валидация совпадения паролей** - добавить валидатор в `validation-registry.js`
4. **Маска для телефона** - добавить поддержку `mask: "phone-ru"` в `ui-input-text`

## План доработок

### Приоритет 1 (критично для MVP)
1. ✅ Переключатель вкладок (`ui-tabs` или `tab-navigation`)
2. ✅ Валидация совпадения паролей

### Приоритет 2 (желательно для MVP)
3. ⚠️ Поле пароля с переключателем видимости
4. ⚠️ Маска для телефона

### Приоритет 3 (можно отложить)
5. ⏳ Дополнительные улучшения UX

## Выводы

**UI-kit в целом готов для реализации модуля профиля.** Основные компоненты присутствуют и функциональны. Требуются только небольшие доработки для полного соответствия требованиям модуля профиля.

Рекомендуется:
1. Создать универсальный компонент `ui-tabs` в UI-kit
2. Расширить `ui-input-text` поддержкой переключателя видимости пароля
3. Добавить валидатор `passwordsMatch` в `validation-registry.js`
4. Добавить поддержку маски телефона в `ui-input-text`

Эти доработки будут полезны не только для модуля профиля, но и для других частей сайта.

