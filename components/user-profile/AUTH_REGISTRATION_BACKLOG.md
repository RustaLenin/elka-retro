# Беклог: Авторизация и Регистрация

> 📋 **Ответственность:** Специалист 1 (Профиль/Авторизация)  
> 📖 **Документация:** `components/user-profile/AUTH_REGISTRATION_IMPLEMENTATION.md`

---

## Обзор

Реализация новой упрощённой системы авторизации и регистрации:
- Email-only авторизация/регистрация
- 6-значный код для авторизации и подтверждения почты
- Модальное окно, доступное с любой страницы сайта

---

## Backend задачи

### 0. Архитектурные решения (зафиксировано)

#### 0.1. Хранение кодов авторизации

**Решение:** Использовать WordPress Transients с автоматической очисткой через 24 часа.

**Реализация:**
- Ключ: `elkaretro_auth_code_{md5(email)}`
- Данные: массив с `code`, `email`, `generated_at`, `expires_at`, `attempts`, `blocked_until`, `codes_sent_today`, `last_code_sent_at`, `ip`, `user_agent`
- Срок жизни: 24 часа (автоматическая очистка через `set_transient()`)
- Дублирование: дополнительно сохранять в `wp_options` с ключом `elkaretro_auth_code_backup_{md5(email)}` для надёжности (fallback при проблемах с кешем)

**Преимущества:**
- Автоматическая очистка через 24 часа
- Единое место хранения для всех случаев (существующие и новые пользователи)
- Простой поиск по email
- Стандартный механизм WordPress
- Работает с объектным кешем (Redis/Memcached), если настроен

#### 0.2. Авторизация пользователя после проверки кода

**Решение:** Использовать обе функции WordPress вместе.

**Реализация:**
```php
// После успешной проверки кода
wp_set_current_user($user_id);  // Для текущего запроса
wp_set_auth_cookie($user_id, false);  // Для последующих запросов (cookie)
```

**Объяснение:**
- `wp_set_current_user()` — устанавливает пользователя только для текущего PHP-запроса
- `wp_set_auth_cookie()` — создаёт cookie для постоянной авторизации между запросами
- Обе функции нужны для полной авторизации (без пароля нельзя использовать `wp_signon()`)

#### 0.3. Структура ответов API

**Решение:** Использовать стандартные HTTP статусы и структурированные ответы.

**HTTP статусы:**
- `200 OK` — успешный запрос кода или проверка кода
- `429 Too Many Requests` — rate limiting (`too_frequent`) или блокировка после 10 попыток (`too_many_attempts`)
- `400 Bad Request` — истечение кода (`code_expired`) или другие ошибки валидации

**Формат ответов:**
- Успешный запрос кода: `200 OK` с `success: true`, `message`, `can_resend_after`
- Успешная проверка кода: `200 OK` с `success: true`, `user` (без поля `token`, cookie уже установлен)
- Rate limiting: `429 Too Many Requests` с `success: false`, `error: "too_frequent"`, `message`, `retry_after`
- Блокировка: `429 Too Many Requests` с `success: false`, `error: "too_many_attempts"`, `message`, `blocked_until` (timestamp)
- Истечение кода: см. раздел 0.4
- Превышение лимита отправок: см. раздел 0.5

#### 0.4. Обработка истечения кода

#### 0.4. Обработка истечения кода

**Решение:** Автоматически отправлять новый код при истечении старого.

**Логика:**
- При проверке истёкшего кода автоматически генерировать и отправлять новый код
- Возвращать ошибку `code_expired` с HTTP 400 Bad Request
- На фронте показывать уведомление: "Старый код истёк, мы вам на почту отправили новый код"

**Плюсы автоматической отправки:**
- ✅ Удобство для пользователя (не нужно вручную запрашивать новый код)
- ✅ Меньше действий от пользователя
- ✅ Пользователь сразу получает рабочий код

**Минусы автоматической отправки:**
- ⚠️ Дополнительная нагрузка на email-сервер (если пользователь часто проверяет истёкший код)
- ⚠️ Потенциальный спам, если кто-то специально проверяет истёкшие коды
- ⚠️ Нужно проверять rate limiting (180 секунд) перед отправкой нового кода

**Альтернатива (только ошибка без отправки):**
- Пользователь должен сам запросить новый код через кнопку "Отправить код повторно"
- Меньше нагрузки на email-сервер
- Но менее удобно для пользователя

**Выбранный вариант:** ✅ Автоматическая отправка с проверкой rate limiting (если прошло меньше 180 секунд с последней отправки — не отправляем, возвращаем ошибку `too_frequent`)

**Формат ответа при истечении кода:**
```json
{
  "success": false,
  "error": "code_expired",
  "message": "Старый код истёк, мы вам на почту отправили новый код",
  "new_code_sent": true
}
```
HTTP статус: `400 Bad Request`

#### 0.5. Лимит отправок в сутки (10 кодов)

**Решение:** Скользящее окно 24 часа с автоматическим сбросом через transient.

**Логика:**
- Хранить счётчик `codes_sent_today` и `last_code_sent_at` в transient вместе с кодом
- При запросе нового кода проверять:
  - Если прошло >= 24 часа с `last_code_sent_at` → сбросить `codes_sent_today = 0`
  - Если `codes_sent_today >= 10` → вернуть ошибку `daily_limit_exceeded`
  - Иначе → увеличить счётчик и обновить `last_code_sent_at`
- Transient истекает через 24 часа → автоматический сброс счётчика
- При достижении лимита: просто показывать ошибку (блокировка не нужна, transient сам истечёт через 24 часа)

**Формат ответа при превышении лимита:**
```json
{
  "success": false,
  "error": "daily_limit_exceeded",
  "message": "Превышен лимит отправок кодов (10 в сутки). Попробуйте позже.",
  "retry_after": 86400  // секунд до истечения transient (примерно)
}
```
HTTP статус: `429 Too Many Requests`

#### 0.6. Интеграция с существующими модальными окнами

**Решение:** Удалить старые формы и создать новую единую форму с динамическим содержимым.

**Подход:** Гибридный (удалить старое, переиспользовать инфраструктуру)

**Удалить:**
- `app/forms/sign-in.js` (старая форма с паролями)
- `app/forms/register.js` (старая форма с паролями)
- Модальное окно `auth:passwordReset` (не нужно в новой системе)

**Создать:**
- `app/forms/auth-code.js` — новая единая форма с динамическим содержимым (email → код)

**Переиспользовать:**
- Инфраструктуру `app.modal` (оставить)
- `user-ui-service.js` (обновить для нового модального окна)
- Стили `auth-modals-styles.css` (можно переиспользовать)

**Структура новой формы:**
- Начальное состояние: поле `email`
- После успешного запроса кода: автоматически меняется на поле `code`
- Если аккаунта не было: показывать чекбоксы согласия при вводе кода (или при вводе email — уточнить)
- Управление состоянием через `form-controller.setState({ fields: [...] })`

**Новое модальное окно:**
- ID: `auth:code` (вместо `auth:signIn` и `auth:register`)
- Метод: `showAuthModal()` (вместо `showSignInModal()` и `showRegisterModal()`)
- Действие: `user.showAuthModal` (вместо `user.showSignInModal` и `user.showRegisterModal`)

#### 0.7. Миграция существующих пользователей

**Решение:** Полная замена старой системы, без миграции данных.

**Логика:**
- Все пользователи (кроме админов) могут авторизоваться через новую систему (email + код)
- Пароли не удаляются, просто не используются в новой системе
- Новым пользователям пароль генерируется автоматически при создании аккаунта, но не сообщается им
- Старые endpoint'ы `/auth/login` и `/auth/register` можно переиспользовать по тому же URL (заменить логику) или удалить, если новые будут по другому URL
- Уведомлять существующих пользователей о переходе на новую систему не требуется

**Проверка администраторов:**
- При запросе кода проверять: если пользователь имеет права администратора → возвращать ошибку
- Использовать `user_can($user_id, 'manage_options')` или проверку роли
- Администраторы продолжают авторизоваться через `/wp-admin/`

#### 0.8. Интеграция с Flow заказа

**Решение:** Авторизация независима от процесса заказа, пользователь сам решает, авторизоваться или оставить анонимный заказ.

**Логика:**
- Данные заказа не передаются в модальное окно авторизации (авторизация независима)
- После успешной авторизации: возвращать пользователя на шаг 3, он сам подтверждает оформление заказа
- В модальном окне показывать сообщение об успешной авторизации: "Вы успешно авторизовались. Теперь вы можете продолжить оформление, заказ будет привязан к вашему профилю..."
- Пользователь сам закрывает модальное окно и нажимает "Оформить заказ" на шаге 3
- Ошибки авторизации обрабатываются в модальном окне (визуально)
- При ошибке: оставлять модальное окно открытым, показывать ошибку в overlay поверх модального окна

**События авторизации:**
- Использовать существующее событие `elkaretro:auth:login` для интеграции с Flow заказа
- Flow заказа может слушать это событие и обновлять состояние (например, обновить данные пользователя на шаге 3)

#### 0.9. Обработка ошибок на фронте

**Решение:** Использовать `ui-form-controller` для ошибок формы, overlay для показа ошибок поверх модального окна.

**Логика:**
- Ошибки формы показываются через `ui-form-controller` (fieldMessages/formMessages)
- `ui-form-controller` имеет overlay, который в модальном режиме покрывает весь `modal_body` (z-index: 100)
- Ошибки должны быть видны поверх всего модального окна через overlay

**Обработка конкретных ошибок:**
- `too_frequent` (rate limiting):
  - Показывать таймер обратного отсчёта отдельно (слева снизу, отдельно от ошибки)
  - Ошибка через `form-controller` (formMessages) или overlay
- `too_many_attempts` (блокировка):
  - Показывать только сообщение об ошибке: "Заблокировано на 3 часа"
  - Таймер на 3 часа не нужен
  - Ошибка через `form-controller` (formMessages) или overlay
- `code_expired` (истечение кода):
  - Уточнить: показывать через `form-controller` (formMessages) или отдельный overlay
  - Сообщение: "Старый код истёк, мы вам на почту отправили новый код"
- `daily_limit_exceeded` (превышение лимита):
  - Показывать сообщение: "Превышен лимит отправок кодов (10 в сутки). Попробуйте позже."
  - Время до сброса лимита показывать не требуется
  - Ошибка через `form-controller` (formMessages) или overlay

#### 0.10. IP-адрес и устройство в письме с кодом

**Решение:** Минимальная реализация, можно отложить на потом.

**Логика (если реализовывать):**
- Получение IP: использовать функцию `get_client_ip()` (учитывать прокси через `HTTP_X_FORWARDED_FOR`, `HTTP_CLIENT_IP`, `REMOTE_ADDR`)
- Определение устройства: использовать User-Agent из `$_SERVER['HTTP_USER_AGENT']` как есть (без библиотек)
- Сохранение: сохранять в transient вместе с кодом (поля `ip` и `user_agent` уже есть в структуре)
- Отображение в письме: простой текст "IP-адрес: {ip}, Устройство: {user_agent}" (мелким шрифтом внизу письма)

**Приоритет:** Низкий, можно реализовать позже или вообще не делать (не критично для MVP)

---

### 1. Создание REST API endpoint'ов

#### 1.1. Endpoint: Запрос кода (`/auth/request-code`)

**Задача:** Реализовать endpoint для запроса кода авторизации/регистрации

**Требования:**
- [ ] Принимает `email` в теле запроса
- [ ] Валидирует формат email (регулярное выражение)
- [ ] Проверяет, существует ли пользователь с таким email
- [ ] Если пользователь существует — проверяет, не является ли он администратором (блокировать для админов, см. раздел 0.7)
- [ ] Если пользователя нет — создаёт аккаунт (с автоматически сгенерированным паролем, который не сообщается пользователю)
- [ ] Генерирует 6-значный код
- [ ] Сохраняет код в WordPress Transient (см. раздел 0.1):
  - Ключ: `elkaretro_auth_code_{md5(email)}`
  - Данные: массив с `code`, `email`, `generated_at`, `expires_at`, `attempts`, `blocked_until`, `codes_sent_today`, `last_code_sent_at`, `ip`, `user_agent`
  - Срок жизни: 24 часа (автоматическая очистка)
  - Дублирование: также сохранять в `wp_options` с ключом `elkaretro_auth_code_backup_{md5(email)}` для надёжности
- [ ] Проверяет rate limiting (180 секунд между запросами)
- [ ] Проверяет лимит отправок в сутки (10 кодов)
- [ ] Отправляет email:
  - Если новый пользователь — письмо о регистрации + письмо с кодом
  - Если существующий — только письмо с кодом
- [ ] Возвращает успешный ответ с информацией о возможности повторной отправки

**Файлы для изменения:**
- `core/user-profile/user-profile-rest-controller.php` — добавить новые route'ы
- `core/user-profile/user-auth-service.php` — создать новый сервис для логики авторизации

**Оценка:** 4-6 часов

---

#### 1.2. Endpoint: Проверка кода (`/auth/verify-code`)

**Задача:** Реализовать endpoint для проверки кода авторизации

**Требования:**
- [ ] Принимает `email` и `code` в теле запроса
- [ ] Валидирует формат email и кода
- [ ] Проверяет, существует ли пользователь с таким email
- [ ] Если пользователь существует — проверяет, не является ли он администратором (блокировать для админов, см. раздел 0.7)
- [ ] Получает код из WordPress Transient (см. раздел 0.1)
- [ ] Проверяет, не заблокирован ли аккаунт (`blocked_until`)
- [ ] Проверяет, не истёк ли код (`expires_at`)
- [ ] Если код истёк — отправляет новый код и возвращает ошибку
- [ ] Проверяет правильность кода
- [ ] Увеличивает счётчик попыток при неверном коде
- [ ] Если попыток >= 10 — блокирует на 3 часа (обновляет `blocked_until` в transient)
- [ ] При успешной проверке:
  - Устанавливает `email_verified = true` в user meta (если первый раз)
  - Авторизует пользователя через `wp_set_current_user()` и `wp_set_auth_cookie()` (см. раздел 0.2)
  - Очищает код из transient и опции (удаляет оба ключа)
  - Возвращает данные пользователя (токен не нужен, cookie уже установлен)
- [ ] Возвращает ошибки с понятными сообщениями

**Файлы для изменения:**
- `core/user-profile/user-profile-rest-controller.php` — добавить route
- `core/user-profile/user-auth-service.php` — добавить метод проверки кода

**Оценка:** 4-6 часов

---

### 2. Сервис авторизации (`user-auth-service.php`)

**Задача:** Создать сервис для бизнес-логики авторизации

**Требования:**
- [ ] Метод `request_code($email)` — запрос кода
- [ ] Метод `verify_code($email, $code)` — проверка кода
- [ ] Метод `generate_code()` — генерация 6-значного кода
- [ ] Метод `check_rate_limit($email)` — проверка rate limiting
- [ ] Метод `check_daily_limit($email)` — проверка лимита отправок в сутки (скользящее окно 24 часа)
- [ ] Метод `save_code($email, $code, $ip, $user_agent)` — сохранение кода в transient и опции
- [ ] Метод `get_code($email)` — получение кода из transient (с fallback на опцию)
- [ ] Метод `delete_code($email)` — удаление кода из transient и опции
- [ ] Метод `is_code_expired($code_data)` — проверка истечения кода
- [ ] Метод `is_account_blocked($code_data)` — проверка блокировки
- [ ] Метод `increment_attempts($email)` — увеличение счётчика попыток в transient
- [ ] Метод `block_account($email, $hours)` — блокировка аккаунта (обновление `blocked_until` в transient)
- [ ] Метод `get_client_ip()` — получение IP клиента
- [ ] Метод `is_admin_user($user_id)` — проверка, является ли пользователь администратором (`user_can($user_id, 'manage_options')`)
- [ ] Метод `create_user_if_not_exists($email)` — создание пользователя, если не существует (с автоматически сгенерированным паролем, который не сообщается пользователю)
- [ ] Метод `send_registration_email($user_email)` — отправка письма о регистрации (принимает email, не user_id)
- [ ] Метод `send_code_email($user_email, $code, $generated_at, $ip, $user_agent)` — отправка письма с кодом

**Файлы для создания:**
- `core/user-profile/user-auth-service.php`

**Оценка:** 6-8 часов

---

### 3. Email-шаблоны

**Архитектура:**
- Каждый сервис хранит свои email шаблоны в отдельном файле
- Используется универсальный сервис `Email_Service` для отправки (`core/email/email-service.php`)
- Логирование уже встроено в `Email_Service`
- Формат: HTML с inline CSS (для совместимости с почтовыми клиентами)
- Переменные: простая замена через `esc_html()`, `esc_url()`, `sprintf()`

**Файл для изменения:**
- `core/user-profile/user-email-templates.php` — добавить новые методы в существующий класс `User_Email_Templates`

#### 3.1. Письмо о регистрации

**Задача:** Создать шаблон письма о успешной регистрации

**Требования:**
- [ ] Метод `send_registration_email($user_email)` — отправка письма
- [ ] Метод `get_registration_email_template($user_email)` — генерация HTML шаблона
- [ ] Красивый HTML-шаблон с inline CSS
- [ ] Благодарность за создание аккаунта
- [ ] Описание механизма авторизации на сайте (email + код)
- [ ] Ссылки на основные разделы сайта
- [ ] Короткий F.A.Q.
- [ ] Использовать `Email_Service::send()` с контекстом `type: 'registration'`

**Пример использования:**
```php
$email_templates = new User_Email_Templates();
$email_templates->send_registration_email($user_email);
```

**Оценка:** 2-3 часа

---

#### 3.2. Письмо с кодом

**Задача:** Создать шаблон письма с кодом авторизации

**Требования:**
- [ ] Метод `send_code_email($user_email, $code, $generated_at, $ip, $user_agent)` — отправка письма
- [ ] Метод `get_code_email_template($code, $generated_at, $ip, $user_agent)` — генерация HTML шаблона
- [ ] Упрощённый HTML-шаблон с inline CSS
- [ ] Код крупным шрифтом (выделенный блок)
- [ ] Дата создания кода по серверному времени (форматированная)
- [ ] Комментарий о том, что новый код можно создать не ранее чем через 3 минуты
- [ ] Комментарий о том, что код действителен только в течение суток (24 часа)
- [ ] Ссылка на главную страницу
- [ ] Информация о IP и устройстве (для безопасности, мелким шрифтом)
- [ ] Использовать `Email_Service::send()` с контекстом `type: 'auth_code'`

**Пример использования:**
```php
$email_templates = new User_Email_Templates();
$email_templates->send_code_email($user_email, $code, $generated_at, $ip, $user_agent);
```

**Оценка:** 2-3 часа

---

### 4. Интеграция с WordPress

**Задача:** Интегрировать новую систему авторизации с WordPress

**Требования:**
- [ ] Регистрация REST API route'ов в `user-profile-loader.php`
- [ ] Проверка, что система не применяется к администраторам
- [ ] Интеграция с WordPress session management
- [ ] Обработка ошибок и логирование

**Файлы для изменения:**
- `core/user-profile/user-profile-loader.php`
- `core/user-profile/user-profile-rest-controller.php`

**Оценка:** 2-3 часа

---

## Frontend задачи

### 5. Модальное окно авторизации/регистрации

#### 5.1. Форма авторизации/регистрации

**Задача:** Создать единую форму авторизации/регистрации с динамическим содержимым

**Требования:**
- [ ] Создать `app/forms/auth-code.js` — единая форма с динамическим содержимым
- [ ] Два состояния формы (управление через `form-controller.setState()`):
  - Состояние 1: Поле для ввода email
  - Состояние 2: Поле для ввода кода (после успешного запроса кода)
- [ ] Переключение между состояниями автоматически после успешного запроса кода
- [ ] Валидация email на фронте (регулярное выражение)
- [ ] Валидация кода на фронте (6 цифр)
- [ ] Обработка ошибок с отображением сообщений через `ui-form-controller` (fieldMessages/formMessages)
- [ ] Таймер для повторной отправки кода (180 секунд) — кнопка "Отправить код повторно"
- [ ] Индикация загрузки через статус формы (`status: 'validating'` или `status: 'submitting'`)
- [ ] После успешной авторизации: показывать сообщение об успехе в модальном окне (через `form-controller` или обновление body модального окна)
- [ ] Сообщение об успехе: "Вы успешно авторизовались. Теперь вы можете продолжить оформление, заказ будет привязан к вашему профилю..."
- [ ] Пользователь сам закрывает модальное окно после успешной авторизации
- [ ] Интеграция с глобальным сервисом авторизации (`authService.requestCode()`, `authService.verifyCode()`)
- [ ] Отправка события `elkaretro:auth:login` после успешной авторизации (для интеграции с Flow заказа)

**Файлы для создания:**
- `app/forms/auth-code.js` — новая единая форма

**Файлы для удаления:**
- `app/forms/sign-in.js` — старая форма с паролями
- `app/forms/register.js` — старая форма с паролями

**Оценка:** 6-8 часов

---

#### 5.2. Интеграция с глобальным сервисом авторизации

**Задача:** Обновить глобальный сервис авторизации для поддержки новой системы

**Требования:**
- [ ] Обновить `components/user-profile/services/auth-service.js`
- [ ] Добавить метод `requestCode(email)` — запрос кода авторизации
- [ ] Добавить метод `verifyCode(email, code)` — проверка кода и авторизация
- [ ] Метод `verifyCode()` должен:
  - Отправлять запрос на `/wp-json/elkaretro/v1/auth/verify-code`
  - После успешной проверки обновлять `this.currentUser`
  - Отправлять событие `elkaretro:auth:login` (для интеграции с Flow заказа)
  - Обновлять состояние в `window.app.auth` (если используется)
- [ ] Обновить метод `login()` — можно оставить для обратной совместимости или удалить (если старые endpoint'ы удаляются)
- [ ] Обновить метод `register()` — можно оставить для обратной совместимости или удалить (если старые endpoint'ы удаляются)
- [ ] Удалить старые методы, связанные с паролями (если старые endpoint'ы удаляются)

**Файлы для изменения:**
- `components/user-profile/services/auth-service.js`

**Оценка:** 3-4 часа

---

#### 5.3. Обновление модального окна и триггеров

**Задача:** Обновить модальное окно и триггеры для новой системы

**Требования:**
- [ ] Обновить `components/user-profile/services/user-ui-service.js`
- [ ] Удалить регистрацию старых модальных окон (`auth:signIn`, `auth:register`, `auth:passwordReset`)
- [ ] Зарегистрировать новое модальное окно `auth:code` с формой `app.forms.authCode`
- [ ] Обновить метод `showAuthModal()` (вместо `showSignInModal()` и `showRegisterModal()`)
- [ ] Обновить действие `user.showAuthModal` (вместо `user.showSignInModal` и `user.showRegisterModal`)
- [ ] Обновить все места, где используются старые методы/действия:
  - `components/user-profile/user-menu/user-menu.js`
  - `components/user-profile/user-menu/user-menu-template.js`
  - `components/cart/order-wizard/steps/step-auth.js` (если используется)
  - Компонент хедера (если есть кнопка "Войти")

**Файлы для изменения:**
- `components/user-profile/services/user-ui-service.js`
- `components/user-profile/user-menu/user-menu.js`
- `components/user-profile/user-menu/user-menu-template.js`
- Компонент хедера (если есть)

**Оценка:** 2-3 часа

---

## Тестирование

### 6. Unit-тесты (опционально)

**Задача:** Написать unit-тесты для критичной логики

**Требования:**
- [ ] Тесты для генерации кода
- [ ] Тесты для проверки rate limiting
- [ ] Тесты для проверки лимита отправок в сутки
- [ ] Тесты для проверки истечения кода
- [ ] Тесты для блокировки аккаунта

**Оценка:** 4-6 часов (опционально)

---

### 7. Интеграционное тестирование

**Задача:** Протестировать полный flow авторизации/регистрации

**Требования:**
- [ ] Тест регистрации нового пользователя
- [ ] Тест авторизации существующего пользователя
- [ ] Тест повторной отправки кода
- [ ] Тест истечения кода
- [ ] Тест блокировки аккаунта после 10 неудачных попыток
- [ ] Тест rate limiting
- [ ] Тест лимита отправок в сутки

**Оценка:** 2-3 часа

---

## Удаление старого кода

### 8. Удаление старой системы авторизации

**Задача:** Удалить старые endpoint'ы и компоненты авторизации

**Требования:**
- [ ] Удалить старые endpoint'ы регистрации/авторизации
- [ ] Удалить старые формы регистрации/авторизации
- [ ] Удалить старые методы из `auth-service.js`
- [ ] Обновить документацию

**Файлы для удаления/изменения:**
- Старые endpoint'ы в `user-profile-rest-controller.php`
- Старые формы в `app/forms/register.js` и других местах
- Старые методы в `auth-service.js`

**Оценка:** 2-3 часа

---

## Итого

**Общая оценка времени:**
- Backend: ~20-28 часов
- Frontend: ~10-14 часов
- Тестирование: ~6-9 часов
- Удаление старого кода: ~2-3 часа

**Итого: ~38-54 часа (~5-7 рабочих дней)**

---

## Приоритеты

### Высокий приоритет (критично для интеграции)
1. Endpoint `/auth/request-code`
2. Endpoint `/auth/verify-code`
3. Сервис авторизации
4. Модальное окно авторизации/регистрации
5. Интеграция с глобальным сервисом авторизации

### Средний приоритет (можно сделать параллельно)
6. Email-шаблоны
7. Интеграция с WordPress

### Низкий приоритет (можно сделать после интеграции)
8. Unit-тесты
9. Удаление старого кода

---

## Заметки

- Все endpoint'ы должны возвращать понятные ошибки
- Важно логировать все действия для отладки
- Email-шаблоны должны быть красивыми и информативными
- Модальное окно должно быть удобным и интуитивным
- Важно обработать все edge cases (истечение кода, блокировка, rate limiting)

