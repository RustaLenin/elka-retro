# Авторизация и регистрация — документация для реализации

## ⚠️ ВАЖНО: Новая концепция

**Статус:** Эта документация описывает **новую упрощённую систему** авторизации и регистрации, которая **требует реализации**.

**Связанные документы:**
- `components/cart/AUTH_REGISTRATION.md` — общая документация с описанием проблем и новой концепции
- `components/cart/ORDER_STEPS.md` — интеграция с процессом оформления заказа

---

## Общая концепция

### Основные принципы

1. **Упрощение процесса:**
   - Объединение `login` и `email` в одно поле (email)
   - Одна форма для авторизации и регистрации (только email)
   - Пользователь вводит только email, больше ничего не требуется

2. **Авторизация через код:**
   - После ввода email отправляется 6-значный код на почту
   - Код используется и для авторизации, и для подтверждения почты (2 в 1)
   - После получения ответа от бэка форма меняется: поле email → поле для ввода кода

3. **Создание аккаунта:**
   - Если аккаунта с такой почтой нет — создаём его автоматически
   - Отправляем письмо об успешной регистрации на сайте
   - Генерируем 6-значный код, сохраняем его на бэке
   - Отправляем пользователю письмо с кодом (для авторизации и подтверждения почты, 2 в 1)

4. **Авторизация существующего аккаунта:**
   - Если аккаунт с такой почтой есть — отправляем 6-значный код на указанную почту
   - Код сохраняется на бэке

5. **Подтверждение почты:**
   - При первой успешной авторизации ставим галочку "почта подтверждена"
   - Если галочка уже есть — просто авторизуем по коду

6. **Ограничение частоты запросов:**
   - Повторно письмо с кодом можно отправить спустя 180 секунд
   - На бэке запоминаем, когда в последний раз код был обновлён
   - Если прошло меньше 180 секунд и получен новый запрос (пользователь обновил страницу) — отвечаем ошибкой
   - В ошибке указываем, через сколько времени можно повторить попытку

7. **Редактирование профиля:**
   - Всё редактирование данных профиля доступно только после авторизации

8. **Администраторы:**
   - ⚠️ **ВАЖНО:** Данная логика **не распространяется** на админские аккаунты
   - Пользователи с правами администратора авторизуются через `/wp-admin/`
   - Данный вид авторизации — **чисто клиентский**

---

## Бизнес-логика авторизации/регистрации

### Flow авторизации/регистрации

```
1. Пользователь вводит email
   ↓
2. Отправка запроса на бэк
   ↓
3. Проверка на бэке:
   ├── Если аккаунта нет:
   │   ├── Создаём аккаунт
   │   ├── Отправляем письмо об успешной регистрации
   │   ├── Генерируем 6-значный код
   │   ├── Сохраняем код на бэке
   │   └── Отправляем письмо с кодом
   │
   └── Если аккаунт есть:
       ├── Генерируем 6-значный код
       ├── Сохраняем код на бэке
       └── Отправляем письмо с кодом
   ↓
4. На фронте меняем форму: поле email → поле для ввода кода
   ↓
5. Пользователь вводит код
   ↓
6. Отправка кода на бэк для проверки
   ↓
7. Проверка кода на бэке:
   ├── Если код правильный:
   │   ├── Если первая авторизация → ставим галочку "почта подтверждена"
   │   ├── Авторизуем пользователя
   │   └── Возвращаем успешный ответ
   │
   └── Если код неправильный:
       └── Возвращаем ошибку
   ↓
8. На фронте:
   ├── Если успешно:
   │   ├── Сохраняем cookie авторизации
   │   ├── Обновляем состояние в window.app.auth
   │   ├── Перерисовываем хедер
   │   └── Продолжаем процесс (отправка заказа или переход в профиль)
   │
   └── Если ошибка:
       └── Показываем ошибку пользователю
```

### Ограничение частоты запросов кода

**Логика:**
- На бэке запоминаем время последнего обновления кода для каждого email
- При запросе нового кода проверяем:
  - Если прошло меньше 180 секунд с последнего обновления → возвращаем ошибку
  - В ошибке указываем, через сколько времени можно повторить попытку
- Если прошло 180 секунд или больше → генерируем новый код и обновляем время

**Пример ответа при слишком частом запросе:**
```json
{
  "success": false,
  "error": "too_frequent",
  "message": "Слишком частые запросы. Попробуйте через 120 секунд.",
  "retry_after": 120
}
```

### Генерация и хранение кода

**Требования:**
- Код должен быть 6-значным (цифры от 0 до 9)
- Код должен быть случайным
- Код должен храниться на бэке с привязкой к email
- Нужно хранить время генерации кода (для проверки времени жизни и частоты запросов)

**Время жизни кода:**
- ✅ Код действителен **24 часа**

### Подтверждение почты

**Логика:**
- При первой успешной авторизации по коду ставим флаг "почта подтверждена" в профиле пользователя
- Если флаг уже установлен — просто авторизуем пользователя
- Флаг подтверждения почты используется для:
  - Отправки важных уведомлений (только на подтверждённые почты)
  - Доверия к аккаунту (подтверждённый аккаунт более надёжный)

---

## Письма

### Письмо об успешной регистрации

**Отправляется:** Только при создании нового аккаунта (если аккаунта с таким email не было)

**Важно:** Это **отдельное письмо**, не содержит кода для подтверждения. Код присылается в отдельном письме.

**Содержание:**
- Благодарность за создание аккаунта
- Короткое описание механизма авторизации на сайте
- Ссылки на основные разделы сайта
- Короткий F.A.Q.

**Шаблон:** Отдельный шаблон для письма о регистрации

### Письмо с кодом

**Отправляется:** 
- При создании нового аккаунта (после письма об успешной регистрации)
- При запросе кода для существующего аккаунта
- При истечении кода (если пользователь пытается использовать истёкший код)

**Важно:** Это **отдельное письмо** с упрощённым шаблоном, содержит только код.

**Содержание:**
- 6-значный код
- **Дата создания кода по серверному времени**
- Комментарий, что новый код можно создать не ранее чем через 3 минуты
- Информация о том, что код действителен только в течение суток (24 часа)
- Ссылка на главную страницу
- **IP-адрес или устройство, с которого был запрошен код** (для безопасности)

**Шаблон:** Отдельный упрощённый шаблон для письма с кодом

**Не указываем:**
- Что это код для авторизации или для подтверждения почты
- Дополнительную информацию, не связанную с кодом

**🔮 В бэклог:** Добавить ссылку на авторизацию с кодом по GET параметрам:
- Параметры: `email` и `code`
- При переходе по ссылке открывается форма с кодом
- Код автоматически копируется из GET параметра
- Отправляется запрос на бэк (напрямую запрос на бэк с редиректом не user-friendly)

---

## Бизнес-логика (ответы на вопросы)

### 1. Ввод кода

#### 1.1. Неправильный код

**Ответы:**
- ✅ Если пользователь ввёл неправильный код — показывается ошибка "Код неверный"
- ✅ Предоставляется **10 попыток** ввода кода (счётчик на стороне бэка, а не фронта)
- ✅ После 10 неудачных попыток:
  - Код сбрасывается
  - Ввод кода для аккаунта блокируется на **3 часа**
  - Записываем дату блокировки
  - Если новый запрос на отправку кода или проверку кода приходит раньше по серверному времени — отправляем ошибку `too_many_attempts`
- ✅ После блокировки — **временная блокировка на 3 часа**
- 🔮 **В будущем:** Предусмотрим, чтобы сотрудники могли устанавливать блокировку на авторизацию или снимать её

#### 1.2. Время жизни кода

**Ответы:**
- ✅ Код действителен **24 часа**
- ✅ Если код истёк:
  - Проверяем дату генерации кода на бэке
  - Если время истекло и получен запрос на валидацию кода — отправляем письмо с новым кодом
  - На фронте показываем уведомление поверх формы: "Старый код истёк, мы вам на почту отправили новый код"
- ❌ Таймер обратного отсчёта для кода не показываем
- ✅ Можно запросить новый код ранее, спустя **3 минуты (180 секунд)** после выпуска предыдущего

#### 1.3. Изменение email

**Ответы:**
- ❌ **Нельзя изменить email** после того, как код был отправлен
- ⚠️ Другой email = другой аккаунт
- ❌ Кнопка "Изменить email" на этапе ввода кода не разрешена
- ⚠️ Если пользователь передумал и хочет использовать другой email:
  - Пока ничего не делаем автоматически
  - Пользователь может создать новый аккаунт на новую почту
  - Может обратиться к нам с просьбой перенести заказы на новый аккаунт
  - Старый аккаунт можем заблокировать

### 2. Повторная отправка кода

#### 2.1. Логика повторной отправки

**Ответы:**
- ✅ Показываем кнопку "Отправить код повторно" на этапе ввода кода
- ✅ Кнопка с таймером, когда она снова станет доступна
- ✅ Если прошло меньше 180 секунд:
  - Показываем таймер обратного отсчёта
  - Кнопка заблокирована
- ✅ Таймер обратного отсчёта до следующей возможности отправки **нужен**
- ✅ Если пользователь обновил страницу до истечения 180 секунд:
  - Может повторно ввести email
  - Если с момента генерации кода прошло меньше 3 минут — просим ввести уже ранее отправленный код
  - Если с момента генерации кода прошло больше 3 минут — отправляем новое письмо
  - **В письме надо явно указывать, когда по серверному времени оно было создано**

#### 2.2. Обработка ошибок

**Ответы:**
- ✅ Если письмо не пришло:
  - Пользователь может нажать кнопку "Отправить новый код"
  - После трёх отправок подряд без авторизации — предлагаем написать письмо на почту `zakaz@elka-retro.ru` с темой "проблема с авторизацией"
  - Мы отправим код вручную
  - **Актуальный код тогда в админке надо показывать администраторам**
- ✅ Связь с поддержкой:
  - Основной способ: email `zakaz@elka-retro.ru`
  - В будущем: будет добавлена ссылка на Telegram аккаунт для связи с поддержкой

### 3. Восстановление доступа

#### 3.1. Потеря доступа к email

**Ответы:**
- ✅ **Решение:** Восстановление доступа происходит через коммуникацию с `zakaz@elka-retro.ru`
- ✅ Если пользователь указал альтернативные способы и мы валидировали, что они ему принадлежат (например, другая почта или телефон):
  - По умолчанию — альтернативная почта
  - Телефон стоит денег (SMS отправлять)
- ✅ Пользователь всегда может создать новый аккаунт с новым email

#### 3.2. Смена email

**Ответы:**
- ❌ **Нельзя изменить email** в профиле после регистрации
- ⚠️ С точки зрения WordPress — логин нельзя менять, он один и навсегда
- 🔮 **В будущем:** Если пользователь меняет почту (UX):
  - По факту на бэке можем создавать новый аккаунт
  - Перепривязывать к нему все свойства и заказы, споры с прошлого аккаунта
  - Старый аккаунт блокируется

### 4. Безопасность

#### 4.1. Защита от злоупотреблений

**Ответы:**
- ✅ Нужна защита от массовой отправки кодов на один email:
  - Ограничиваем количество отправок на одну почту за сутки — **не больше 10 кодов**
- ❌ Защита от создания множества аккаунтов с одного IP не требуется
- ❌ Капча при запросе кода не нужна (не так страшен DDoS, если мы будем проверять на бэке, когда в последний раз был запрос и ничего не делать, если он был недавно)

#### 4.2. Валидация email

**Ответы:**
- ✅ Проверка существования email решается через код:
  - Если почты не существует — письмо с кодом не будет отправлено
  - Пользователь не подтвердит почту, заказ не создастся
  - Не нужно отправлять тестовое письмо перед созданием аккаунта

### 5. UX и отображение

#### 5.1. Индикация процесса

**Ответы:**
- ✅ Нужно показывать индикатор "Код отправлен" после запроса кода
- ✅ Нужно показывать, на какой email отправлен код
- ❌ Таймер обратного отсчёта для кода не показываем (код действителен 24 часа)

#### 5.2. Обработка ошибок

**Ответы:**
- ✅ Как показывать ошибки:
  - Неправильный код — "Код неверный"
  - Код истёк — уведомление поверх формы: "Старый код истёк, мы вам на почту отправили новый код"
  - Слишком частые запросы — ошибка с таймером до следующей возможности отправки
  - Слишком много попыток — ошибка `too_many_attempts` с указанием времени блокировки (3 часа)
- ✅ Нужны подсказки для пользователя (например, "Проверьте папку 'Спам'")

---

## Технические детали (для справки)

### REST API Endpoints

**Запрос кода:**
- `POST /wp-json/elkaretro/v1/auth/request-code`
- Параметры: `email` (string, required)
- Ответ: `{ success: true }` или ошибка с `retry_after`

**Проверка кода:**
- `POST /wp-json/elkaretro/v1/auth/verify-code`
- Параметры: `email` (string, required), `code` (string, required)
- Ответ: `{ success: true, user: {...} }` или ошибка

### Структура данных

**Пользователь:**
- `user_email` — email (используется как login)
- `user_login` — совпадает с email
- `email_verified` — флаг подтверждения почты (meta field)

**Код:**
- Хранится на бэке с привязкой к email
- Время генерации кода
- Время жизни кода: **24 часа**
- Счётчик неудачных попыток (максимум 10)
- Дата блокировки (если заблокирован на 3 часа)

---

## Способы связи с клиентом

### Для анонимных заказов

- ✅ Показывается дополнительное поле (textarea) для указания предпочитаемых способов связи
- ✅ Пользователь может указать ссылки на Telegram, WhatsApp, VK, Facebook и т.д.
- ✅ Может написать комментарий о том, когда ему будет удобно связаться через эти способы
- ✅ Поле необязательное, без валидации
- ✅ Информация сохраняется в заказе
- ✅ После закрытия заказа данные о способах связи очищаются (152-ФЗ)

### Для авторизованных пользователей

- ✅ Аналогичное поле (textarea) на шаге 3 оформления заказа
- ✅ Пользователя просят указать, как ему будет удобнее, чтобы с ним связались
- ✅ Может указать ссылки на Telegram, WhatsApp, VK, Facebook и т.д.
- ✅ Может написать комментарий о том, когда ему будет удобно связаться
- ✅ Поле необязательное, без валидации

---

## Связь с поддержкой

### Текущие способы связи

- ✅ **Email:** `zakaz@elka-retro.ru` — основной способ связи
- ✅ **Telegram:** В будущем будет добавлена ссылка на Telegram аккаунт для связи с поддержкой

### Восстановление доступа

- ✅ Восстановление доступа происходит через коммуникацию с `zakaz@elka-retro.ru`

---

## Планы на будущее (бэклог)

### 1. Привязка анонимных заказов к аккаунту

- 🔮 Теоретически возможно привязать анонимный заказ к аккаунту (если пользователь зарегистрируется позже)
- ⏳ Пока не реализуем, идея в бэклоге на будущее

### 2. Управление блокировками администраторами

- 🔮 Возможность для сотрудников устанавливать блокировку на авторизацию или снимать её
- ⏳ Планируется в будущем

### 3. Смена email

- 🔮 Если пользователь меняет почту (UX):
  - На бэке создаём новый аккаунт
  - Перепривязываем к нему все свойства и заказы, споры с прошлого аккаунта
  - Старый аккаунт блокируется
- ⏳ Планируется в будущем

### 4. Автоматическая привязка анонимных заказов

- ✅ Если пользователь создал анонимный заказ, а потом зарегистрировался с тем же email — **привязываем заказ к аккаунту**
- ✅ Нужна возможность "объединить" анонимные заказы с аккаунтом

---

## Следующие шаги

1. ✅ Получены ответы на все вопросы по бизнес-логике
2. ✅ Обновлена техническая документация с учётом ответов
3. ✅ Добавлена информация о способах связи с клиентом (для анонимных и авторизованных заказов)
4. ⏳ Обновить дата-модель и бэклог (после завершения документации)
5. ⏳ Начать реализацию

