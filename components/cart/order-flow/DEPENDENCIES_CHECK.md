# Проверка зависимостей Order Flow

## ✅ Проверено и работает

### Компоненты
- ✅ `order-flow` - зарегистрирован через `customElements.define('order-flow', OrderFlow)`
- ✅ `delivery-step` - зарегистрирован через `customElements.define('delivery-step', DeliveryStep)`
- ✅ `review-step` - зарегистрирован через `customElements.define('review-step', ReviewStep)`
- ✅ Все компоненты добавлены в `components.js` для статической загрузки

### Импорты
- ✅ `BaseElement` - базовый класс для всех компонентов
- ✅ `formatPrice` - утилита из `components/cart/helpers/price-formatter.js`
- ✅ Шаблоны и стили загружаются через `loadCSSOnce`

### Сервисы (window.app)
- ✅ `window.app.cart` - инициализируется в `app.js` через `getCartStore()`
- ✅ `window.app.auth` - инициализируется в `app.js` из `window.wpApiSettings.currentUser`
- ✅ `window.app.modal` - создаётся через `createModalManager()` в `app.js`
- ✅ `window.app.services.userUi` - инициализируется через `userUiService.init()` в `app.js`
- ✅ `window.wpApiSettings.nonce` - передаётся из PHP в `functions.php`

### UI компоненты
- ✅ `ui-button` - загружается статически в `components.js`
- ✅ `ui-icon` - загружается статически в `components.js`
- ✅ `ui-loader` - загружается статически в `components.js`
- ✅ `ui-form-controller` - загружается статически в `components.js`

### События
- ✅ `elkaretro:auth:login` - слушается в `review-step.js` для автоматической отправки заказа после авторизации
- ✅ `order-flow:step:next` - событие перехода на следующий шаг
- ✅ `order-flow:step:prev` - событие возврата на предыдущий шаг
- ✅ `order-flow:back-to-cart` - событие возврата к корзине

### LocalStorage
- ✅ `elkaretro_order_data` - используется для сохранения данных заказа между шагами

### REST API Endpoints
- ✅ `/wp-json/elkaretro/v1/orders` - создание заказа для авторизованных пользователей
- ✅ `/wp-json/elkaretro/v1/orders/create-anonymous` - создание анонимного заказа

## 📋 Зависимости

### Обязательные зависимости (должны быть доступны при загрузке)
1. `window.app` - глобальный объект приложения
2. `window.app.cart` - store корзины
3. `window.app.auth` - данные авторизации
4. `window.app.modal` - менеджер модальных окон
5. `window.app.services.userUi` - сервис для работы с UI пользователя
6. `window.wpApiSettings.nonce` - nonce для REST API запросов

### Опциональные зависимости
- `window.app.ui.showNotification` - для показа уведомлений (если доступен)

## 🔍 Проверка загрузки

### Порядок загрузки:
1. `app.js` - инициализирует все сервисы
2. `components.js` - загружает статические компоненты
3. `cart-page.js` - при клике на "Оформить заказ" динамически загружает `order-flow.js`
4. `order-flow.js` - статически импортирует `delivery-step.js` и `review-step.js`

### Динамическая загрузка:
- `order-flow` загружается динамически через `import()` в `cart-page.js`
- Это позволяет не загружать компоненты flow на всех страницах

## ⚠️ Потенциальные проблемы

1. **Nonce**: Исправлено использование `window.wpApiSettings?.nonce` вместо `window.app?.rest?.nonce`
2. **Сервисы**: Все сервисы должны быть инициализированы до использования компонентов
3. **Cart Store**: Должен быть инициализирован до использования `window.app.cart`

## ✅ Итоговая проверка

Все зависимости проверены и должны работать корректно. Компоненты загружаются в правильном порядке, все сервисы доступны через `window.app`.

