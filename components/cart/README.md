# Корзина и заказы

Модуль корзины и заказов обеспечивает полный цикл покупки товаров на сайте, включая управление корзиной, оформление заказа через пошаговый Wizard, проверку требований комиссионной торговли и интеграцию с сущностью Заказ через PODS.

## 📖 Архитектура

**Важно:** Перед началом работы с модулем корзины обязательно ознакомьтесь с документом [ARCHITECTURE.md](./ARCHITECTURE.md), который описывает принципы работы, публичный API и событийную модель.

## Архитектура (краткое описание)

### Компоненты

#### `cart-page/` - Страница корзины
Главная страница корзины, отображающая все добавленные товары с возможностью удаления.

**Компоненты:**
- `cart-page.js` - главный оркестратор страницы корзины
- `cart-page-template.js` - шаблон разметки
- `cart-page-styles.css` - стили страницы

**Функциональность:**
- Отображение списка товаров в корзине
- Удаление товаров из корзины
- Расчет итоговой стоимости
- Проверка требований комиссионной торговли
- Переход к оформлению заказа

#### `cart-item/` - Элемент корзины
Компонент для отображения отдельного товара в корзине.

**Компоненты:**
- `cart-item.js` - компонент элемента корзины
- `cart-item-template.js` - шаблон элемента
- `cart-item-styles.css` - стили элемента

**Функциональность:**
- Отображение информации о товаре (название, цена, фото)
- Удаление товара из корзины
- **Примечание:** Большинство товаров штучно уникальные, без количества

#### `order-wizard/` - Пошаговый Wizard оформления заказа
Многошаговый процесс оформления заказа с ветвлением для авторизованных/неавторизованных пользователей.

**Компоненты:**
- `order-wizard.js` - главный компонент Wizard
- `order-wizard-template.js` - шаблон Wizard
- `order-wizard-styles.css` - стили Wizard
- `steps/` - отдельные шаги Wizard:
  - `step-review.js` - шаг проверки заказа
  - `step-auth.js` - шаг авторизации/регистрации
  - `step-delivery.js` - шаг выбора доставки
  - `step-payment.js` - шаг выбора способа оплаты
  - `step-confirmation.js` - шаг подтверждения

**Функциональность:**
- Пошаговое оформление заказа
- Проверка авторизации при запуске Wizard
- Ветвление: ввод новых персональных данных (регистрация) или авторизация существующего пользователя
- **Гостевого оформления нет:** всегда требуется регистрация или авторизация
- Валидация данных на каждом шаге
- Сохранение прогресса в LocalStorage
- Создание заказа через REST API (для неавторизованных: сначала регистрация, затем создание заказа)

#### `cart-summary/` - Итоговая информация о корзине
Компонент для отображения итоговой стоимости, скидок, сборов.

**Компоненты:**
- `cart-summary.js` - компонент итогов
- `cart-summary-template.js` - шаблон
- `cart-summary-styles.css` - стили

**Функциональность:**
- Расчет итоговой стоимости
- Отображение скидок
- Расчет сбора на комплектацию
- **Примечание:** VIP статус не реализован в MVP

### Сервисы

#### `cart-store.js` - Управление состоянием корзины
Централизованное управление состоянием корзины.

**Функциональность:**
- Добавление/удаление товаров
- Синхронизация с LocalStorage (неавторизованные)
- Синхронизация с пользовательскими данными (авторизованные)
- Объединенная корзина для `toy_instance` и `ny_accessory`
- **Примечание:** Резервирование товаров не реализовано в MVP, будет добавлено в будущем только для авторизованных пользователей (6 часов)

#### `cart-service.js` - Сервис работы с корзиной
Бизнес-логика работы с корзиной.

**Методы:**
- `addItem(item)` - добавление товара (только уникальные товары, без количества)
- `removeItem(itemId)` - удаление товара
- `getCart()` - получение объединенной корзины (toy_instance + ny_accessory)
- `clearCart()` - очистка корзины
- `calculateTotal(cart)` - расчет итоговой стоимости
- `syncWithServer(userId)` - синхронизация с сервером
- **Примечание:** Резервирование не реализовано в MVP

#### `order-service.js` - Сервис работы с заказами
Бизнес-логика работы с заказами.

**Методы:**
- `createOrder(orderData)` - создание заказа
- `getOrder(orderId)` - получение заказа
- `getUserOrders(userId)` - получение заказов пользователя
- `updateOrderStatus(orderId, status)` - обновление статуса

#### `commission-rules.js` - Правила комиссионной торговли
Проверка и применение правил комиссионной торговли.

**Функциональность:**
- Расчет сбора на комплектацию (350 руб. при покупке < 3500 руб.)
- Расчет оптовых скидок (10% при покупке на 5000 руб., каждая < 2000 руб.)
- **Примечание:** Резервирование и VIP статус не реализованы в MVP, будут добавлены в будущем

### Адаптеры

#### `cart-api-adapter.js` - Адаптер REST API для корзины
Преобразование данных между форматом корзины и REST API.

#### `order-api-adapter.js` - Адаптер REST API для заказов
Преобразование данных между форматом заказа и REST API.

## Интеграция с WordPress

### REST API Endpoints

#### Корзина
- `GET /wp-json/elkaretro/v1/cart` - получение корзины пользователя (только для авторизованных)
- `PUT /wp-json/elkaretro/v1/cart/sync` - синхронизация корзины при авторизации
- **Примечание:** В MVP для неавторизованных пользователей корзина хранится только в LocalStorage, без backend операций

#### Заказы
- `POST /wp-json/elkaretro/v1/orders` - создание заказа (принимает данные пользователя и корзины для неавторизованных, или только данные заказа для авторизованных)
- `GET /wp-json/elkaretro/v1/orders/{orderId}` - получение заказа
- `GET /wp-json/elkaretro/v1/orders` - получение заказов пользователя
- `PUT /wp-json/elkaretro/v1/orders/{orderId}/status` - обновление статуса (только администратор)
- `PUT /wp-json/elkaretro/v1/orders/{orderId}` - обновление заказа (адрес доставки, только если заказ не отправлен)

### Сущность Заказ (PODS)

**Поля:**
- `order_number` - номер заказа (уникальный)
- `user` - связь с пользователем (relationship → user)
- `toy_instance_items` - экземпляры игрушек в заказе (relationship → toy_instance, multi select)
- `ny_accessory_items` - новогодние аксессуары в заказе (relationship → ny_accessory, multi select)
- **Примечание:** В PODS Relationship позволяет выбрать только один Related Type, поэтому созданы два отдельных поля. В коде они объединяются в массив `items`.
- `total_amount` - итоговая сумма
- `subtotal` - сумма без скидок и сборов
- `discount_amount` - сумма скидки
- `fee_amount` - сумма сбора на комплектацию
- `delivery_method` - способ доставки (перечисление типов, без интеграций в MVP)
- `delivery_address` - адрес доставки (1 адрес на аккаунт, можно редактировать)
- `payment_method` - способ оплаты (без интеграций в MVP, можно редактировать в будущем)
- `status` - статус заказа (custom post status)
- `created_at` - дата создания
- `notes` - примечания к заказу

**Кастомные статусы:**
- `awaiting_payment` - На оплате
- `collecting` - Собирается
- `shipped` - Отправлен
- `closed` - Закрыт
- `clarification` - Уточнение у клиента (промежуточный статус)

## Хранение данных

### Неавторизованные пользователи
- **LocalStorage**: `elkaretro_cart` (объединенная корзина)
- **Структура:**
  ```json
  {
    "items": [
      {
        "id": 123,
        "type": "toy_instance",
        "addedAt": "2024-01-15T10:00:00Z"
      },
      {
        "id": 456,
        "type": "ny_accessory",
        "addedAt": "2024-01-15T10:05:00Z"
      }
    ],
    "lastUpdated": "2024-01-15T10:05:00Z"
  }
  ```
- **Примечание:** В MVP резервирование не реализовано, товары не резервируются на backend

### Авторизованные пользователи
- **User Meta**: `elkaretro_cart` (объединенная корзина)
- **Синхронизация**: автоматическая при авторизации
- **Приоритет при конфликте:** User Meta имеет приоритет над LocalStorage
- **Примечание:** Резервирование будет реализовано в будущем (6 часов для авторизованных)

## События

### Глобальные события
- `elkaretro:cart:item-added` - товар добавлен в корзину
- `elkaretro:cart:item-removed` - товар удален из корзины
- `elkaretro:cart:updated` - корзина обновлена
- `elkaretro:cart:synced` - корзина синхронизирована с сервером
- `elkaretro:order:created` - заказ создан
- `elkaretro:order:status-changed` - статус заказа изменен

## Использование

### Добавление товара в корзину

```javascript
import { cartStore } from './cart-store.js';

// Добавление экземпляра игрушки (уникальный товар, без количества)
cartStore.addItem({
  id: 123,
  type: 'toy_instance',
  price: 3000
});

// Добавление аксессуара (уникальный товар, без количества)
cartStore.addItem({
  id: 456,
  type: 'ny_accessory',
  price: 1500
});

// Примечание: большинство товаров штучно уникальные, нельзя заказать несколько штук
```

**Для интеграции с каталогом:**
Метод `cartStore.addItem()` должен быть доступен глобально или через экспорт для использования в компонентах каталога (`toy-instance-card`, `ny-accessory-card`). Интерфейс кнопки "Добавить в корзину" будет реализован коллегой.

### Отображение страницы корзины

```html
<!-- В page-cart.php -->
<cart-page></cart-page>
```

**Создание шаблона и страницы:**
- Шаблон `page-cart.php` должен быть создан в корне темы
- Страница корзины автоматически создается при инициализации темы (в `functions.php`, хук `after_setup_theme`)
- Аналогично созданию страниц "Блог" и "Каталог"

### Отображение Wizard оформления

```html
<!-- В order-wizard.js -->
<order-wizard></order-wizard>
```

## Проверка требований комиссионной торговли

### Резервирование товаров
- **В MVP:** Резервирование не реализовано
- **В будущем:** Резервирование будет реализовано только для авторизованных пользователей (6 часов)
- При добавлении нового товара в корзину таймер будет обновляться
- Будет проработана логика коммуникаций в случае брошенной корзины

### Сбор на комплектацию
- Применяется при покупке на сумму менее 3500 руб.
- Размер сбора: 350 руб.
- Не применяется для VIP пользователей

### Скидки
- Оптовая скидка 10% при покупке на сумму 5000 руб. и более
- Условие: каждая игрушка должна стоить менее 2000 руб.
- Не применяется к товарам в распродаже
- **Примечание:** VIP статус не реализован в MVP, будет добавлен в будущем

## Интеграция с модулем пользователя

Модуль корзины интегрируется с модулем `user-profile`:
- Использует сервисы авторизации для определения статуса пользователя
- Синхронизирует корзину при авторизации (приоритет User Meta над LocalStorage)
- Сохраняет корзину в пользовательские данные
- Подтягивает данные профиля (адрес доставки) при авторизации
- Позволяет редактировать данные профиля на шаге оформления логистики

## Email уведомления

### Универсальный шаблон письма
- **Хранение:** в коде (`core/orders/order-email-templates.php`)
- **Формат:** HTML
- **Язык:** один язык (русский)
- **Редактирование:** через админку не требуется

### Содержание письма
- Все данные заказа (кроме платежных)
- Адрес доставки
- Перечень заказанных позиций
- Стоимость каждой позиции
- Общая стоимость заказа

### Типы писем
1. **Письмо при создании заказа:**
   - Отправляется администраторам и клиенту
   - Универсальный шаблон для обоих

2. **Письмо о закрытии заказа:**
   - Отправляется клиенту при закрытии заказа
   - Содержит просьбу оставить обратную связь

## Безопасность

- Все запросы используют WordPress nonce
- Валидация данных на клиенте и сервере
- Проверка прав доступа к заказам
- Защита от XSS через санитизацию данных

