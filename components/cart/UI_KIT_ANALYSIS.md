# Анализ UI Kit для корзины и заказов

Анализ существующих компонентов UI Kit и определение необходимых доработок для реализации функциональности корзины и заказов.

## Существующие компоненты UI Kit

### ✅ Доступные компоненты

#### 1. **ui-button** ✅
**Статус:** Доступен  
**Использование в корзине:**
- Кнопка "Добавить в корзину" в карточках товаров
- Кнопка "Удалить" в элементах корзины
- Кнопки навигации в Wizard ("Назад", "Далее", "Оформить заказ")
- Кнопка "Очистить корзину"

**Достаточно ли:** Да, полностью покрывает потребности

---

#### 2. **ui-form-controller** ✅
**Статус:** Доступен  
**Использование в корзине:**
- Форма выбора доставки в Wizard
- Форма выбора способа оплаты
- Форма ввода адреса доставки
- Форма регистрации/авторизации в Wizard

**Достаточно ли:** Да, полностью покрывает потребности

---

#### 3. **ui-form-field** ✅
**Статус:** Доступен  
**Использование в корзине:**
- Поля формы доставки
- Поля формы оплаты
- Поля формы регистрации

**Достаточно ли:** Да, полностью покрывает потребности

---

#### 4. **ui-input-text** ✅
**Статус:** Доступен  
**Использование в корзине:**
- Поле ввода адреса доставки
- Поле ввода имени/фамилии при регистрации
- Поле ввода телефона

**Достаточно ли:** Да, полностью покрывает потребности

---

#### 5. **ui-input-number** ✅
**Статус:** Доступен  
**Использование в корзине:**
- Поле ввода количества товаров (если нужно)
- Поле ввода суммы (для ручного ввода, если нужно)

**Достаточно ли:** Да, полностью покрывает потребности

---

#### 6. **ui-select-single** ✅
**Статус:** Доступен  
**Использование в корзине:**
- Выбор способа доставки
- Выбор способа оплаты
- Выбор региона/города доставки

**Достаточно ли:** Да, полностью покрывает потребности

---

#### 7. **ui-checkbox** ✅
**Статус:** Доступен  
**Использование в корзине:**
- Согласие с условиями покупки
- Сохранение адреса доставки для будущих заказов
- Подписка на рассылку

**Достаточно ли:** Да, полностью покрывает потребности

---

#### 8. **ui-modal** ✅
**Статус:** Доступен  
**Использование в корзине:**
- Модальное окно подтверждения удаления товара
- Модальное окно подтверждения очистки корзины
- Модальное окно с деталями заказа после оформления

**Достаточно ли:** Да, полностью покрывает потребности

---

#### 9. **ui-notification** ✅
**Статус:** Доступен  
**Использование в корзине:**
- Уведомление о добавлении товара в корзину
- Уведомление об удалении товара
- Уведомление об истечении резервирования
- Уведомление об успешном создании заказа
- Уведомления об ошибках

**Достаточно ли:** Да, полностью покрывает потребности

---

#### 10. **ui-loader** ✅
**Статус:** Доступен  
**Использование в корзине:**
- Загрузка данных корзины
- Загрузка при создании заказа
- Загрузка при синхронизации корзины

**Достаточно ли:** Да, полностью покрывает потребности

---

#### 11. **ui-icon** ✅
**Статус:** Доступен  
**Использование в корзине:**
- Иконка корзины в хедере
- Иконка удаления товара
- Иконки в Wizard (шаги, навигация)

**Достаточно ли:** Да, полностью покрывает потребности

---

#### 12. **ui-tooltip** ✅
**Статус:** Доступен  
**Использование в корзине:**
- Подсказки о правилах комиссионной торговли
- Подсказки о скидках и сборах
- Подсказки о VIP статусе

**Достаточно ли:** Да, полностью покрывает потребности

---

## Недостающие компоненты

### ❌ Компоненты, которые нужно создать

#### 1. **ui-step-indicator** (или использовать существующий подход)
**Назначение:** Индикатор шагов в Wizard оформления заказа

**Требования:**
- Отображение текущего шага
- Отображение пройденных шагов
- Отображение будущих шагов
- Возможность перехода к пройденным шагам (опционально)

**Альтернатива:** Можно реализовать как часть `order-wizard` компонента без отдельного UI Kit компонента

**Приоритет:** Низкий (можно обойтись без отдельного компонента)

---

#### 2. **ui-countdown-timer** (или использовать существующий подход)
**Назначение:** Таймер обратного отсчета для резервирования товаров

**Требования:**
- Отображение времени до истечения резервирования
- Формат: минуты:секунды или только минуты
- Визуальное предупреждение при приближении к истечению
- Автоматическое обновление каждую секунду

**Альтернатива:** Можно реализовать как часть `cart-item` или `cart-page` компонента

**Приоритет:** Средний (зависит от ответов на вопросы в QUESTIONS.md)

---

#### 3. **ui-price-display** (или использовать существующий подход)
**Назначение:** Единообразное отображение цен с учетом валюты и форматирования

**Требования:**
- Форматирование цены (разделители тысяч, валюта)
- Отображение старой и новой цены (для скидок)
- Отображение суммы скидки
- Отображение итоговой суммы

**Альтернатива:** Можно использовать утилиту форматирования без отдельного компонента

**Приоритет:** Низкий (можно обойтись утилитой форматирования)

---

## Рекомендации

### ✅ Достаточно существующих компонентов

Все основные потребности корзины и заказов покрываются существующими компонентами UI Kit:
- Формы: `ui-form-controller`, `ui-form-field`, `ui-input-text`, `ui-select-single`, `ui-checkbox`
- Навигация: `ui-button`
- Уведомления: `ui-notification`
- Модальные окна: `ui-modal`
- Загрузка: `ui-loader`
- Иконки: `ui-icon`
- Подсказки: `ui-tooltip`

### 🔧 Небольшие доработки (опционально)

1. **Утилита форматирования цен:**
   - Создать утилиту `formatPrice(amount, currency = 'RUB')` в `components/cart/helpers/price-formatter.js`
   - Использовать во всех компонентах корзины для единообразного отображения цен

2. **Утилита таймера:**
   - Создать утилиту `createCountdownTimer(targetDate, onTick, onExpire)` в `components/cart/helpers/timer-utils.js`
   - Использовать для отображения времени до истечения резервирования

3. **Компонент индикатора шагов (опционально):**
   - Если Wizard будет использоваться в других местах, вынести в UI Kit
   - Иначе реализовать как часть `order-wizard`

### 📋 Итоговый вывод

**Вывод:** Существующие компоненты UI Kit полностью покрывают потребности корзины и заказов. Дополнительные компоненты не требуются для MVP.

**Рекомендации:**
1. Использовать существующие компоненты UI Kit
2. Создать утилиты для форматирования цен и таймеров (не компоненты)
3. Реализовать специфичные для корзины компоненты (`cart-item`, `cart-page`, `order-wizard`) без создания новых UI Kit компонентов

## Примеры использования существующих компонентов

### Форма доставки в Wizard

```html
<ui-form-controller id="delivery-form">
  <ui-form-field>
    <ui-select-single
      id="delivery-method"
      label="Способ доставки"
      data-source='[{"value": "pickup", "label": "Самовывоз"}, {"value": "courier", "label": "Курьер"}]'
    ></ui-select-single>
  </ui-form-field>
  
  <ui-form-field>
    <ui-input-text
      id="delivery-address"
      label="Адрес доставки"
      placeholder="Введите адрес"
    ></ui-input-text>
  </ui-form-field>
</ui-form-controller>
```

### Уведомление о добавлении в корзину

```javascript
import { showNotification } from '../ui-kit/notification/notification.js';

showNotification({
  type: 'success',
  message: 'Товар добавлен в корзину',
  duration: 3000
});
```

### Модальное окно подтверждения удаления

```javascript
import { showModal } from '../ui-kit/modal/modal.js';

showModal({
  title: 'Удалить товар из корзины?',
  content: 'Вы уверены, что хотите удалить этот товар из корзины?',
  actions: [
    { label: 'Отмена', type: 'secondary', handler: () => modal.close() },
    { label: 'Удалить', type: 'primary', handler: () => { /* удаление */ } }
  ]
});
```

## Заключение

Существующие компоненты UI Kit полностью покрывают потребности реализации корзины и заказов. Дополнительные компоненты не требуются. Рекомендуется использовать существующие компоненты и создать только специфичные для корзины компоненты (`cart-item`, `cart-page`, `order-wizard`).

