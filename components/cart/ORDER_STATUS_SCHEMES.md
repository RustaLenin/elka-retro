# Схема реализации статусов заказов (Вариант 5: Упрощённая схема)

## Описание схемы

**Концепция:**
- `post_status` — три значения: `publish` (актуальный заказ, требует работы), `completed` (завершённый заказ), `rejected` (отклонённый/отменённый заказ)
- `order_status` (мета-поле) — детальная информация о том, какая именно работа требуется или как заказ закрыт

**Аналогия:** Как `resolution` в Jira:
- `Unresolved` = задача открыта (требует работы)
- Любая резолюция (`Fixed`, `Won't Fix`, `Duplicate`) = задача закрыта с определённой резолюцией

**Для заказов:**
- `post_status = publish` = заказ актуален, требует работы
- `post_status = completed` = заказ завершён (успешно выполнен)
- `post_status = rejected` = заказ отклонён/отменён (не выполнен)
- `order_status` (мета-поле) = детальная информация о состоянии/резолюции

---

## Структура статусов

### post_status (системное поле WordPress)

```php
'publish'    // Актуальный заказ, требует работы
'completed'  // Заказ завершён (успешно выполнен)
'rejected'   // Заказ отклонён/отменён (не выполнен)
```

### order_status (мета-поле, детальная информация)

**Для актуальных заказов (`post_status = publish`):**
```php
'pending_confirmation'  // Ожидает подтверждения администратора
'awaiting_payment'     // Ожидает оплаты (после сборки и подтверждения)
'awaiting_shippment'   // Заказ оплачен, но ещё не отправлен
'shipped'              // Отправлен (но ещё не получен/не закрыт)
'awaiting_pickup'      // Ожидает получения (для самовывоза с Удельной)
'clarification'        // Уточнение у клиента
```

**Для завершённых заказов (`post_status = completed`):**
```php
'completed'    // Заказ успешно выполнен и получен клиентом
'shipped'      // Заказ доставлен/получен (альтернатива completed)
```

**Для отклонённых заказов (`post_status = rejected`):**
```php
'rejected'     // Заказ отклонён магазином (клиент неадекватный и т.д.)
'cancelled'    // Заказ отменён клиентом (может сделать только до оплаты)
```

---

## Преимущества схемы

### 1. ✅ Решение проблем с WordPress Core

**Проблема:** WordPress может автоматически менять статус `publish`

**Решение:**
- Используем стандартные статусы `publish`, `completed` и `rejected`
- WordPress не будет автоматически менять эти статусы
- Нет конфликтов с WordPress Core

### 2. ✅ Решение проблем с плагинами

**Проблема:** Плагины могут конфликтовать с кастомными статусами

**Решение:**
- Используем только стандартные статусы
- Плагины корректно работают с `publish`, `completed` и `rejected`
- Нет конфликтов с SEO-плагинами, кэшированием и т.д.

### 3. ✅ Решение проблем с фильтрацией в админке

**Проблема:** Сложно фильтровать заказы по бизнес-статусам

**Решение:**
- По умолчанию в админке показываются только `publish` заказы (актуальные)
- Для фильтрации по детальным статусам используем `meta_query` с `order_status`
- Можно добавить кастомные фильтры в админке для `order_status`

### 4. ✅ Решение проблем с REST API

**Проблема:** REST API по умолчанию возвращает только `publish` посты

**Решение:**
- По умолчанию API возвращает только актуальные заказы (`publish`)
- Для истории заказов можно явно запросить `post_status = completed` или `post_status = rejected`
- Комбинировать не нужно — только `meta_query` для детальной фильтрации

### 5. ✅ Решение проблем с семантикой

**Проблема:** `publish` означает "опубликован", но заказ не подтверждён

**Решение:**
- `post_status` используется только для операционного состояния (актуален/завершён/отклонён)
- Семантика бизнес-логики переносится в `order_status`
- Чёткое разделение: операционное состояние vs бизнес-статус

### 6. ✅ Решение проблем с синхронизацией

**Проблема:** Нужно синхронизировать `post_status` и `order_status`

**Решение:**
- Синхронизация требуется только при завершении или отклонении заказа
- При завершении: `post_status = completed`, `order_status` устанавливается в `completed` (или остаётся `shipped`)
- При отклонении: `post_status = rejected`, `order_status` устанавливается в `rejected` или `cancelled`
- В остальных случаях `post_status` остаётся `publish`, меняется только `order_status`

---

## Логика переходов статусов

### Переходы order_status (при post_status = publish)

#### Стандартный flow (с доставкой, требуется оплата)

```
pending_confirmation → awaiting_payment → awaiting_shippment → shipped → [закрытие]
                     ↓
                clarification → [возврат к предыдущему статусу]
```

**Описание этапов:**

1. **`pending_confirmation`** — Ожидает подтверждения администратора
   - Администратор собирает заказ
   - Проверяет, что со всеми игрушками всё в порядке
   - Упаковывает заказ
   - После проверки и упаковки переходит в следующий статус

2. **`awaiting_payment`** — Ожидает оплаты
   - Заказ собран, проверен и упакован
   - Клиенту остаётся только оплатить
   - После оплаты переходит в следующий статус

3. **`awaiting_shippment`** — Заказ оплачен, но ещё не отправлен
   - Заказ оплачен клиентом
   - Ожидает отправки (курьером, почтой, в пункт выдачи)

4. **`shipped`** — Отправлен
   - Заказ отправлен клиенту
   - Ожидает получения (для доставки) или готов к получению (для самовывоза)

5. **`completed`** → `post_status = completed` — Завершён
   - Заказ получен клиентом
   - Заказ завершён

#### Flow для самовывоза с Удельной (без предоплаты)

```
pending_confirmation → awaiting_shippment → awaiting_pickup → [закрытие]
                     ↓
                clarification → [возврат к предыдущему статусу]
```

**Описание этапов:**

1. **`pending_confirmation`** — Ожидает подтверждения администратора
   - Администратор собирает заказ
   - Проверяет, что со всеми игрушками всё в порядке
   - Упаковывает заказ
   - **Оплата не требуется до получения** (особенность самовывоза с Удельной)

2. **`awaiting_shippment`** — Ожидает отправки/подготовки
   - Заказ собран, проверен и упакован
   - Готов к получению клиентом в магазине на Удельной
   - **Оплата происходит при получении** (наличными или переводом)

3. **`awaiting_pickup`** — Ожидает получения
   - Заказ готов к получению
   - Клиент может забрать заказ в магазине на Удельной
   - После получения переходит в закрытие

4. **`completed`** → `post_status = completed` — Завершён
   - Заказ получен клиентом
   - Заказ завершён

**Особенности самовывоза с Удельной:**
- ⚠️ Оплата часто происходит **при получении**, а не до отправки
- ⚠️ Статус `awaiting_payment` **пропускается** (или используется только если клиент решил оплатить заранее)
- ⚠️ После сборки заказ сразу переходит в `awaiting_shippment` (готов к получению)
- ⚠️ Новый статус `awaiting_pickup` для отслеживания ожидания получения

### Завершение или отклонение заказа

**Успешное завершение:**
- `post_status: publish → completed`
- `order_status: shipped → completed` (или остаётся `shipped`)

**Отклонение заказа магазином:**
- `post_status: publish → rejected`
- `order_status: [любой] → rejected`

**Отмена заказа клиентом:**
- `post_status: publish → rejected`
- `order_status: [любой] → cancelled`
- ⚠️ Клиент может отменить заказ только до оплаты (оплата — это точка принятия обязательств магазином на поставку)

---

## Реализация transition() метода

### Концепция

Создать общий метод `transition()` для обработки всех переходов статусов:

```php
/**
 * Обработка перехода статуса заказа
 * 
 * @param int    $order_id    ID заказа
 * @param string $new_status  Новый order_status
 * @return array|WP_Error
 */
public function transition( $order_id, $new_status ) {
    // 1. Валидация перехода
    $old_status = get_post_meta( $order_id, 'order_status', true );
    if ( ! $this->can_transition( $old_status, $new_status ) ) {
        return new WP_Error( 'invalid_transition', 'Invalid status transition' );
    }
    
    // 2. Обновление order_status
    update_post_meta( $order_id, 'order_status', $new_status );
    
    // 3. Проверка, нужно ли завершить или отклонить заказ
    $should_complete = $this->should_complete_order( $new_status );
    $should_reject = $this->should_reject_order( $new_status );
    
    if ( $should_complete ) {
        wp_update_post( array(
            'ID' => $order_id,
            'post_status' => 'completed'
        ) );
    } elseif ( $should_reject ) {
        wp_update_post( array(
            'ID' => $order_id,
            'post_status' => 'rejected'
        ) );
    }
    
    // 4. Вызов обработчиков перехода
    $this->handle_transition( $order_id, $old_status, $new_status );
    
    return $this->get_order( $order_id );
}

/**
 * Обработка конкретного перехода статуса
 * 
 * @param int    $order_id    ID заказа
 * @param string $old_status   Старый order_status
 * @param string $new_status   Новый order_status
 */
private function handle_transition( $order_id, $old_status, $new_status ) {
    // Pipeline обработчиков
    $handlers = array(
        'pending_confirmation' => array( $this, 'handle_pending_confirmation' ),
        'awaiting_payment'     => array( $this, 'handle_awaiting_payment' ),
        'awaiting_shippment'   => array( $this, 'handle_awaiting_shippment' ),
        'shipped'              => array( $this, 'handle_shipped' ),
        'awaiting_pickup'      => array( $this, 'handle_awaiting_pickup' ),
        'completed'            => array( $this, 'handle_completed' ),
        'rejected'              => array( $this, 'handle_rejected' ),
        'cancelled'            => array( $this, 'handle_cancelled' ),
    );
    
    // Вызов обработчика для нового статуса
    if ( isset( $handlers[ $new_status ] ) ) {
        call_user_func( $handlers[ $new_status ], $order_id, $old_status, $new_status );
    }
    
    // Вызов общего хука
    do_action( 'elkaretro_order_status_transition', $order_id, $old_status, $new_status );
}

/**
 * Обработчики для каждого статуса
 */
private function handle_pending_confirmation( $order_id, $old_status, $new_status ) {
    // Логика для статуса "ожидает подтверждения"
    // Например: отправка уведомления администратору
}

private function handle_awaiting_payment( $order_id, $old_status, $new_status ) {
    // Логика для статуса "ожидает оплаты"
    // Например: отправка письма с реквизитами
}

private function handle_awaiting_shippment( $order_id, $old_status, $new_status ) {
    // Логика для статуса "ожидает отправки"
    // Заказ оплачен (или готов к получению для самовывоза), готов к отправке
    // Например: уведомление о готовности к отправке
}

private function handle_shipped( $order_id, $old_status, $new_status ) {
    // Логика для статуса "отправлен"
    // Например: отправка трек-номера
    // Для доставки: заказ отправлен, ожидает получения
}

private function handle_awaiting_pickup( $order_id, $old_status, $new_status ) {
    // Логика для статуса "ожидает получения"
    // Для самовывоза с Удельной: заказ готов к получению
    // Например: уведомление клиенту о готовности заказа
}

private function handle_completed( $order_id, $old_status, $new_status ) {
    // Логика для статуса "завершён"
    // Например: отправка благодарственного письма
    // Изменение статусов товаров на "sold"
}

private function handle_rejected( $order_id, $old_status, $new_status ) {
    // Логика для статуса "отклонён"
    // Например: возврат товаров в доступные
    // Отправка уведомления об отмене
}

private function handle_cancelled( $order_id, $old_status, $new_status ) {
    // Логика для статуса "отменён"
    // Аналогично handle_rejected
}
```

### Преимущества подхода

- ✅ Явное разделение бизнес-логики и операционных действий
- ✅ Легко добавлять новые обработчики
- ✅ Централизованная логика переходов
- ✅ Можно использовать хуки WordPress (`transition_post_status`) для `post_status`
- ✅ Можно использовать кастомные хуки для `order_status`

---

## Использование хуков WordPress

### transition_post_status

Можно использовать для отслеживания закрытия заказа:

```php
add_action( 'transition_post_status', function( $new_status, $old_status, $post ) {
    if ( $post->post_type !== 'order' ) {
        return;
    }
    
    // Отслеживаем переход в completed или rejected
    if ( ( $new_status === 'completed' && $old_status !== 'completed' ) ||
         ( $new_status === 'rejected' && $old_status !== 'rejected' ) ) {
        // Заказ завершён или отклонён
        $order_status = get_post_meta( $post->ID, 'order_status', true );
        // Дополнительная логика при завершении/отклонении
    }
}, 10, 3 );
```

**Важно:** В большинстве случаев `post_status` не будет меняться, поэтому этот хук будет срабатывать редко (только при завершении или отклонении заказа).

---

## Работа с API

### Получение актуальных заказов

```php
// По умолчанию - только publish заказы
$orders = get_posts( array(
    'post_type'      => 'order',
    'post_status'    => 'publish',  // Только актуальные
    'posts_per_page' => -1,
) );

// С фильтрацией по order_status
$orders = get_posts( array(
    'post_type'      => 'order',
    'post_status'    => 'publish',
    'posts_per_page' => -1,
    'meta_query'     => array(
        array(
            'key'     => 'order_status',
            'value'   => 'awaiting_payment',
            'compare' => '=',
        ),
    ),
) );
```

### Получение истории заказов

```php
// Явно запрашиваем завершённые или отклонённые заказы
$orders = get_posts( array(
    'post_type'      => 'order',
    'post_status'    => array( 'completed', 'rejected' ),  // Завершённые или отклонённые заказы
    'posts_per_page' => -1,
    'meta_query'     => array(
        array(
            'key'     => 'order_status',
            'value'   => array( 'completed', 'rejected', 'cancelled' ),
            'compare' => 'IN',
        ),
    ),
) );
```

---

## Возможные проблемы и решения

### 1. ⚠️ Производительность

**Проблема:** Мета-запросы могут быть медленнее

**Решение:**
- Не Highload проект, производительность достаточна
- Можно добавить индекс на мета-поле `order_status`
- Можно использовать кэширование для часто запрашиваемых заказов

### 2. ⚠️ Дополнительная логика

**Проблема:** Нужно создавать обёртки для работы со статусами

**Решение:**
- Создать единый метод `transition()` для всех переходов
- Использовать его везде вместо прямого обновления мета-полей
- Это даже лучше — централизованная логика

### 3. ⚠️ Кастомизация админки

**Проблема:** Нужно кастомизировать админку для отображения `order_status`

**Решение:**
- Добавить колонку `order_status` в список заказов
- Добавить фильтры по `order_status` в админке
- Это правильнее — явное отображение бизнес-статусов

### 4. ⚠️ REST API

**Проблема:** Нужно кастомизировать REST API для возврата `order_status`

**Решение:**
- Добавить `order_status` в ответ REST API
- По умолчанию возвращать только `publish` заказы
- Для истории явно запрашивать `completed` или `rejected` заказы
- Это даже лучше — явное разделение актуальных, завершённых и отклонённых заказов

---

## Сравнение с другими вариантами

| Критерий | Вариант 1 (`publish`) | Вариант 2 (`order_status`) | Вариант 3 (`pending_confirmation`) | **Вариант 5 (Упрощённая)** |
|----------|---------------------|---------------------------|-----------------------------------|---------------------------|
| **Конфликты с плагинами** | ❌ Высокий риск | ✅ Нет | ✅ Нет | ✅ Нет |
| **Семантика** | ❌ Плохая | ✅ Хорошая | ✅ Отличная | ✅ Отличная |
| **Сложность реализации** | ✅ Простая | ❌ Сложная | ✅ Средняя | ✅ Средняя |
| **Производительность** | ✅ Хорошая | ⚠️ Может быть хуже | ✅ Хорошая | ⚠️ Может быть хуже (но не критично) |
| **Использование WordPress API** | ✅ Полное | ❌ Ограниченное | ✅ Полное | ⚠️ Частичное (но достаточно) |
| **Гибкость** | ❌ Плохая | ✅ Отличная | ⚠️ Средняя | ✅ Отличная |
| **Поддержка** | ⚠️ Средняя | ❌ Сложная | ✅ Простая | ✅ Простая |
| **Разделение логики** | ❌ Нет | ✅ Есть | ❌ Нет | ✅ Есть (явное) |

---

## Рекомендация

### ✅ Вариант 5 (Упрощённая схема) — **РЕКОМЕНДУЕТСЯ**

**Обоснование:**
1. ✅ Решает все проблемы с конфликтами (использует только стандартные статусы)
2. ✅ Чёткое разделение: операционное состояние (`post_status`) vs бизнес-статус (`order_status`)
3. ✅ Гибкость для расширения (легко добавить новые `order_status`)
4. ✅ Централизованная логика переходов через метод `transition()`
5. ✅ Явное разделение актуальных и закрытых заказов в API
6. ✅ Производительность достаточна для проекта

**Единственный минус:**
- ⚠️ Нужно создавать обёртки для работы со статусами (но это даже лучше — централизованная логика)

---

## План реализации

1. **Создать метод `transition()`** в `order-service.php`
2. **Обновить логику создания заказа:**
   - `post_status = 'publish'`
   - `order_status = 'pending_confirmation'`
3. **Обновить все места, где меняется статус заказа:**
   - Использовать `transition()` вместо прямого обновления
4. **Добавить обработчики для каждого статуса:**
   - `handle_pending_confirmation()`
   - `handle_awaiting_payment()`
   - `handle_awaiting_shippment()`
   - `handle_awaiting_pickup()`
   - `handle_shipped()`
   - `handle_completed()`
   - `handle_rejected()`
   - `handle_cancelled()`
5. **Обновить REST API:**
   - Добавить `order_status` в ответ
   - По умолчанию возвращать только `publish` заказы
6. **Кастомизировать админку:**
   - Добавить колонку `order_status`
   - Добавить фильтры по `order_status`
7. **Обновить документацию**

---

## Возможные проблемы и возражения

### 1. ⚠️ Что если заказ нужно вернуть из `completed` или `rejected` обратно в работу?

**Сценарий:** Клиент вернул товар или возникла проблема с заказом.

**Решение:** ❌ **Не возвращаем заказ в работу**

**Вместо этого:**
- **Спор** — для разрешения конфликтных ситуаций (см. раздел "Споры (Disputes)")
- **Новый заказ** — для компенсации клиенту (см. раздел "Новые заказы как результат спора")

**Обоснование:**
- ✅ Сохраняем исторические данные (заказ остаётся завершённым или отклонённым)
- ✅ Чёткое разделение: завершённый/отклонённый заказ vs новый заказ
- ✅ Легче отслеживать статистику и аналитику
- ✅ Нет путаницы с возвратом заказа в работу

**Реализация:**
- Создать сущность "Спор" (dispute), привязанную к завершённому или отклонённому заказу
- При необходимости создать новый заказ-компенсацию
- Оригинальный заказ остаётся завершённым или отклонённым

### 2. ⚠️ Миграция существующих заказов

**Проблема:** Нужно мигрировать все существующие заказы в новую схему.

**Решение:**
```php
// Миграция существующих заказов
foreach ( $existing_orders as $order ) {
    $current_status = $order->post_status;
    
    if ( $current_status === 'completed' ) {
        // Завершённые заказы
        wp_update_post( array(
            'ID' => $order->ID,
            'post_status' => 'completed'
        ) );
        update_post_meta( $order->ID, 'order_status', $current_status );
    } elseif ( $current_status === 'rejected' || $current_status === 'cancelled' ) {
        // Отклонённые заказы
        wp_update_post( array(
            'ID' => $order->ID,
            'post_status' => 'rejected'
        ) );
        update_post_meta( $order->ID, 'order_status', $current_status );
    } else {
        // Актуальные заказы
        wp_update_post( array(
            'ID' => $order->ID,
            'post_status' => 'publish'
        ) );
        update_post_meta( $order->ID, 'order_status', $current_status );
    }
}
```

### 3. ⚠️ Что если заказ отменяется после отправки?

**Сценарий:** Заказ уже отправлен (`order_status = shipped`), но нужно отменить.

**Решение:**
```php
// Отмена отправленного заказа
post_status: publish → completed (или rejected)
order_status: shipped → rejected (или cancelled)
```

**Логика:**
- В `handle_rejected()` или `handle_cancelled()` проверять предыдущий статус
- Если был `shipped`, нужно обработать возврат товаров
- Отправить уведомление клиенту об отмене

### 4. ⚠️ Кастомизация админки WordPress

**Проблема:** В админке по умолчанию показывается `post_status`, а не `order_status`.

**Решение:**
- Добавить колонку `order_status` в список заказов
- Добавить фильтры по `order_status` в админке
- Можно использовать хуки `manage_order_posts_columns` и `manage_order_posts_custom_column`

**Пример:**
```php
add_filter( 'manage_order_posts_columns', function( $columns ) {
    $columns['order_status'] = 'Статус заказа';
    return $columns;
} );

add_action( 'manage_order_posts_custom_column', function( $column, $post_id ) {
    if ( $column === 'order_status' ) {
        $order_status = get_post_meta( $post_id, 'order_status', true );
        echo esc_html( $order_status );
    }
}, 10, 2 );
```

### 5. ⚠️ REST API нужно обновить

**Проблема:** REST API по умолчанию возвращает `post_status`, а не `order_status`.

**Решение:**
- Добавить `order_status` в ответ REST API
- Обновить метод `get_order()` в `order-service.php`
- Обновить REST контроллер для включения `order_status` в ответ

### 6. ⚠️ Что если два заказа с разными `order_status` имеют одинаковый `post_status`?

**Сценарий:** Заказ в статусе `awaiting_payment` и заказ в статусе `awaiting_shippment` оба имеют `post_status = publish`.

**Решение:**
- Это нормально и ожидаемо
- Для фильтрации используем `meta_query` с `order_status`
- По умолчанию показываем все `publish` заказы, фильтруем по `order_status` при необходимости

### 7. ⚠️ Производительность при большом количестве заказов

**Проблема:** Мета-запросы могут быть медленнее при большом количестве заказов.

**Решение:**
- Пользователь прав: "не Highload, не умрём"
- Можно добавить индекс на мета-поле `order_status`:
  ```sql
  CREATE INDEX idx_order_status ON wp_postmeta(meta_key, meta_value) WHERE meta_key = 'order_status';
  ```
- Можно использовать кэширование для часто запрашиваемых заказов

### 8. ⚠️ Что если нужно отслеживать историю изменений `order_status`?

**Проблема:** Нет истории изменений статусов.

**Решение:**
- Можно добавить мета-поле `order_status_history` для хранения истории
- Или использовать отдельную таблицу для логов
- Или использовать плагин для аудита изменений

**Пример:**
```php
// При изменении order_status сохраняем историю
$history = get_post_meta( $order_id, 'order_status_history', true ) ?: array();
$history[] = array(
    'status' => $new_status,
    'timestamp' => current_time( 'mysql' ),
    'user_id' => get_current_user_id(),
);
update_post_meta( $order_id, 'order_status_history', $history );
```

---

## Вывод

**Предложенная схема логична и решает все проблемы:**

- ✅ Нет конфликтов с WordPress Core и плагинами
- ✅ Чёткое разделение операционного состояния и бизнес-статуса
- ✅ Гибкость для расширения
- ✅ Централизованная логика переходов
- ✅ Производительность достаточна

**Возможные проблемы решаемы:**

- ✅ Возврат заказа в работу — **не требуется** (используем споры и новые заказы)
- ✅ Миграция существующих заказов — создать скрипт миграции
- ✅ Отмена отправленного заказа — обработать в `handle_rejected()`
- ✅ Кастомизация админки — Kanban доска с фильтрами и группировкой
- ✅ REST API — добавить `order_status` в ответ
- ✅ История изменений — опционально, можно добавить позже
- ✅ Споры и компенсации — отдельные сущности (планируется в будущем)

**Рекомендация: Реализовать Вариант 5 (Упрощённая схема)**

**Схема действительно логична и хорошо продумана!** 🎯

---

## Kanban доска для управления заказами

### Текущая ситуация

**Проблема:**
- В админке используется базовая страница работы с заказами
- Карточка каждой записи слабо функциональна
- Нужно открывать каждый заказ отдельно для работы с ним
- Неудобно для управления потоком заказов

### Идеальная картинка

**Kanban доска для управления заказами:**

1. **Отображение заказов:**
   - `post_status = publish` → заказ показывается на доске
   - `post_status = completed` или `rejected` → заказ скрывается из доски (исторические данные)

2. **Структура доски:**
   - Колонки по `order_status` (например: "Ожидает подтверждения", "Ожидает оплаты", "Ожидает отправки", "Отправлен", "Ожидает получения")
   - Карточки заказов с краткой информацией
   - Drag & Drop для перемещения между колонками (изменение `order_status`)

3. **Детальная информация:**
   - Открытие информации по заказу в сайд-баре или модальном окне
   - Не нужно переходить на отдельную страницу
   - Быстрый доступ к редактированию и действиям

4. **Функциональность (аналогично Jira):**
   - **Quick filters** — быстрые фильтры по различным критериям:
     - По пользователю
     - По дате создания
     - По сумме заказа
     - По способу доставки
     - По способу оплаты
   - **Плавательные дорожки (Swimlanes)** — группировка заказов:
     - По приоритету (срочные, обычные)
     - По способу доставки
     - По администратору (если назначен ответственный)
   - **Поиск и фильтрация** — расширенный поиск по всем полям
   - **Массовые действия** — изменение статуса нескольких заказов одновременно

5. **Workflow:**
   - Заказы — это поток, который требует процессного WorkFlow
   - Каждый заказ проходит через этапы (колонки на доске)
   - Администраторы видят весь поток и могут управлять им визуально

### Преимущества Kanban доски

- ✅ Визуальное представление потока заказов
- ✅ Быстрый доступ к информации (сайд-бар/модальное окно)
- ✅ Удобное управление статусами (Drag & Drop)
- ✅ Группировка и фильтрация для работы с большим количеством заказов
- ✅ Процессный подход к управлению заказами

### Реализация

**Технические детали:**
- Использовать JavaScript библиотеку для Kanban (например, `react-beautiful-dnd`, `dnd-kit`, или кастомное решение)
- REST API для получения заказов с фильтрацией по `post_status = publish`
- REST API для обновления `order_status` при перемещении карточки
- Сайд-бар/модальное окно для детальной информации о заказе

**Структура данных:**
```php
// Запрос заказов для Kanban доски
$orders = get_posts( array(
    'post_type'      => 'order',
    'post_status'    => 'publish',  // Только актуальные заказы
    'posts_per_page' => -1,
    'meta_query'     => array(
        // Опционально: фильтрация по order_status
    ),
) );
```

---

## Споры (Disputes) как отдельная сущность

### Концепция

**Спор** — отдельная сущность, привязанная к завершённому или отклонённому заказу (`post_status = completed` или `rejected`).

**Логика:**
- Заказ уже завершён или отклонён (исторические данные сохраняются)
- Спор создаётся как новая сущность, привязанная к заказу
- По итогу спора магазин может вернуть средства или не вернуть
- Если пользователь не доказал, что это требуется — ответственность на стороне магазина

### Структура спора

**Поля спора:**
- `order_id` — привязка к завершённому или отклонённому заказу
- `user_id` — пользователь, который создал спор
- `reason` — причина спора (текстовое поле)
- `status` — статус спора:
  - `pending` — ожидает рассмотрения
  - `in_review` — на рассмотрении
  - `resolved` — разрешён (средства возвращены)
  - `rejected` — отклонён (средства не возвращены)
- `resolution` — резолюция спора (текстовое поле)
- `refund_amount` — сумма возврата (если применимо)
- `created_at` — дата создания спора
- `resolved_at` — дата разрешения спора

### Сценарии работы со спорами

#### Сценарий 1: Спор разрешён в пользу клиента

1. Клиент создаёт спор по завершённому или отклонённому заказу
2. Администратор рассматривает спор
3. Если магазин признаёт ответственность:
   - Спор получает статус `resolved`
   - Средства возвращаются клиенту (`refund_amount`)
   - Может быть создан новый заказ (компенсация)

#### Сценарий 2: Спор отклонён

1. Клиент создаёт спор по завершённому или отклонённому заказу
2. Администратор рассматривает спор
3. Если клиент не доказал ответственность магазина:
   - Спор получает статус `rejected`
   - Средства не возвращаются
   - Заказ остаётся закрытым

### Возврат заказа в работу

**Вопрос:** Стоит ли давать возможность возвращать заказ в работу?

**Ответ:** Нет, заказ остаётся закрытым. Вместо этого:

- **Спор** — для разрешения конфликтных ситуаций
- **Новый заказ** — для компенсации клиенту (если требуется)

**Преимущества:**
- ✅ Сохраняем исторические данные (заказ остаётся закрытым)
- ✅ Чёткое разделение: закрытый заказ vs новый заказ
- ✅ Легче отслеживать статистику и аналитику
- ✅ Нет путаницы с возвратом заказа в работу

---

## Новые заказы как результат спора

### Концепция

**Новый заказ как компенсация:**
- Создаётся по итогу спора, если магазин признаёт ответственность
- Может быть полностью за счёт магазина (не требует оплаты)
- Требует такой же логики обработки, как обычный заказ

### Структура заказа-компенсации

**Поля заказа:**
- `post_status = 'publish'` (актуальный заказ)
- `order_status = 'pending_confirmation'` (или другой статус)
- `dispute_id` — привязка к спору (если создан из спора)
- `original_order_id` — привязка к оригинальному заказу (если применимо)
- `is_compensation` — флаг, что это компенсационный заказ
- `payment_required` — `false` (не требует оплаты)
- `compensation_amount` — сумма компенсации (если применимо)

### Логика обработки

**Заказ-компенсация проходит те же этапы:**
1. `pending_confirmation` — ожидает подтверждения
2. `awaiting_payment` — **пропускается** (не требует оплаты)
3. `awaiting_shippment` — ожидает отправки (готов к отправке)
4. `shipped` — отправлен (или `awaiting_pickup` для самовывоза)
5. `completed` → `post_status = completed` — завершён

**Особенности:**
- Не требует оплаты (пропускается этап `awaiting_payment`)
- Проходит через те же этапы сборки и отправки
- Может быть привязан к спору для отслеживания

### Преимущества подхода

- ✅ Чёткое разделение: оригинальный заказ (закрыт) vs новый заказ (актуален)
- ✅ Сохраняем исторические данные
- ✅ Единая логика обработки для всех заказов
- ✅ Легко отслеживать компенсационные заказы
- ✅ Можно связать с оригинальным заказом через `original_order_id`

---

## Связь между сущностями

### Схема связей

```
Завершённый или отклонённый заказ (post_status = completed или rejected)
    │
    ├── Спор (dispute)
    │   ├── status: pending / in_review / resolved / rejected
    │   └── refund_amount (если resolved)
    │
    └── Новый заказ (компенсация)
        ├── post_status = publish
        ├── order_status = pending_confirmation
        ├── dispute_id (привязка к спору)
        ├── original_order_id (привязка к оригинальному заказу)
        ├── is_compensation = true
        └── payment_required = false
```

### Workflow спора и компенсации

```
1. Клиент создаёт спор по завершённому или отклонённому заказу
   └── dispute.status = pending

2. Администратор рассматривает спор
   └── dispute.status = in_review

3. Решение:
   ├── Если магазин признаёт ответственность:
   │   ├── dispute.status = resolved
   │   ├── dispute.refund_amount = [сумма]
   │   └── Создаётся новый заказ (компенсация)
   │       └── Проходит через обычный workflow
   │
   └── Если магазин не признаёт ответственность:
       └── dispute.status = rejected
```

---

## Планы на будущее

### MVP (текущий этап)

- ✅ Базовая схема статусов (`post_status` + `order_status`)
- ✅ Kanban доска для управления заказами
- ⚠️ Споры и компенсации — **не реализуем на старте**

### Будущие улучшения

- 🔮 Реализация споров как отдельной сущности
- 🔮 Автоматическое создание заказов-компенсаций
- 🔮 Интеграция споров с Kanban доской
- 🔮 Уведомления о спорах для администраторов
- 🔮 История споров в профиле пользователя

---

## Вывод

**Предложенная схема отлично подходит для:**

1. **Kanban доски:**
   - `post_status = publish` → показывается на доске
   - `post_status = completed` или `rejected` → скрывается из доски
   - Удобное управление потоком заказов

2. **Споров:**
   - Отдельная сущность, привязанная к закрытому заказу
   - Заказ остаётся закрытым (исторические данные)
   - Чёткое разделение ответственности

3. **Компенсаций:**
   - Новый заказ как результат спора
   - Единая логика обработки
   - Легко отслеживать и анализировать

**Схема масштабируема и готова к расширению!** 🚀

