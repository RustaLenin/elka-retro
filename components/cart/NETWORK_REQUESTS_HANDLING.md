# Обработка сетевых запросов и отображение результатов

> 📋 **Статус:** Актуально

## Принципы

### Когда использовать уведомления (notifications)

**Уведомления в правом верхнем углу** используются для:
- ✅ Быстрых действий, которые пользователь может легко повторить
- ✅ Действий, которые не требуют полного внимания пользователя
- ✅ Информационных сообщений, не критичных для процесса

**Примеры:**
- "Товар добавлен в корзину" / "Не удалось добавить товар"
- "Товар удалён из корзины"
- "Данные сохранены"

### Когда использовать полноэкранный overlay

**Полноэкранный overlay** используется для:
- ✅ Сетевых запросов, которые отправляют форму на BackEnd
- ✅ Действий, которые требуют ожидания ответа от сервера
- ✅ Критичных операций, результат которых важен для продолжения процесса

**Примеры:**
- Создание заказа (отправка формы на сервер)
- Авторизация/регистрация
- Оплата заказа
- Любые операции, которые показывают loader во время выполнения

## Логика работы overlay

### Единый контейнер для всех состояний

Один и тот же контейнер `.review-step_overlay` используется для отображения:
1. **Loading** — во время отправки запроса
2. **Success** — при успешном ответе
3. **Error** — при ошибке

### Состояния overlay

#### 1. Loading (Загрузка)

**Когда показывается:**
- Сразу после отправки сетевого запроса
- Пока ожидается ответ от сервера

**Что отображается:**
- Иконка loader (`<ui-loader>`)
- Текст: "Создание заказа..." (или соответствующий процесс)
- Подсказка: "Пожалуйста, не закрывайте страницу"

**Код:**
```javascript
isSubmitting: true
```

#### 2. Success (Успех)

**Когда показывается:**
- После получения успешного ответа от сервера

**Что отображается:**
- Иконка успеха (`check_circle`)
- Заголовок: "Заказ успешно создан!"
- Текст с номером заказа
- Описание дальнейших шагов
- Кнопка для продолжения (например, "Перейти в каталог")

**Код:**
```javascript
isSubmitting: true
success: true
orderId: 123
```

**Примечание:** После успешного создания заказа overlay показывается на уровне `cart-page`, а не `review-step`, так как корзина очищается и происходит переход в другое состояние.

#### 3. Error (Ошибка)

**Когда показывается:**
- После получения ошибки от сервера
- При сетевых ошибках
- При ошибках валидации на сервере

**Что отображается:**
- Иконка ошибки (`error` или `cancel`)
- Заголовок: "Ошибка при создании заказа" (или соответствующий)
- Текст ошибки (сообщение от сервера)
- Описание того, что можно сделать
- Кнопка "Попробовать снова" или "Вернуться назад"

**Код:**
```javascript
isSubmitting: true
error: true
errorMessage: "Товар закончился на складе"
```

## Реализация в компонентах

### State Schema

```javascript
static stateSchema = {
  isSubmitting: { type: 'boolean', default: false },
  success: { type: 'boolean', default: false },
  error: { type: 'boolean', default: false },
  errorMessage: { type: 'string', default: '' },
  orderId: { type: 'number', default: null },
};
```

### Шаблон overlay

```javascript
${isSubmitting ? `
  <div class="review-step_overlay">
    ${error ? `
      <!-- Error state -->
      <div class="review-step_error">
        <ui-icon name="error" size="large"></ui-icon>
        <h3>Ошибка при создании заказа</h3>
        <p>${errorMessage}</p>
        <ui-button label="Попробовать снова" event="review-step:retry"></ui-button>
      </div>
    ` : success ? `
      <!-- Success state -->
      <div class="review-step_success">
        <ui-icon name="check_circle" size="large"></ui-icon>
        <h3>Заказ успешно создан!</h3>
        <p>Ваш заказ №${orderId} успешно оформлен.</p>
      </div>
    ` : `
      <!-- Loading state -->
      <div class="review-step_loader">
        <ui-loader></ui-loader>
        <p>Создание заказа...</p>
        <p>Пожалуйста, не закрывайте страницу</p>
      </div>
    `}
  </div>
` : ''}
```

### Обработка запроса

```javascript
async submitOrder() {
  // 1. Устанавливаем состояние загрузки
  this.setState({ 
    isSubmitting: true, 
    error: false, 
    success: false,
    errorMessage: '' 
  });
  this.render();

  try {
    // 2. Отправляем запрос
    const response = await fetch('/wp-json/elkaretro/v1/orders', {
      method: 'POST',
      // ...
    });

    const result = await response.json();

    // 3. Проверяем результат
    if (!response.ok) {
      throw new Error(result.message || 'Ошибка при создании заказа');
    }

    // 4. Устанавливаем состояние успеха
    this.setState({ 
      success: true,
      orderId: result.order.id 
    });
    this.render();

    // 5. Обрабатываем успех (очистка корзины, переход и т.д.)
    // ...

  } catch (error) {
    // 6. Устанавливаем состояние ошибки
    this.setState({ 
      isSubmitting: true, // Оставляем overlay открытым
      error: true,
      errorMessage: error.message || 'Не удалось создать заказ'
    });
    this.render();
  }
}
```

## Важные моменты

### 1. Не смешивать уведомления и overlay

❌ **Неправильно:**
```javascript
catch (error) {
  this.showNotification(error.message, 'error'); // Маленькое уведомление
  this.setState({ isSubmitting: false });
}
```

✅ **Правильно:**
```javascript
catch (error) {
  this.setState({ 
    isSubmitting: true, // Оставляем overlay открытым
    error: true,
    errorMessage: error.message 
  });
  this.render();
}
```

### 2. Закрытие overlay при ошибке

Overlay должен оставаться открытым при ошибке, чтобы пользователь видел сообщение об ошибке и мог попробовать снова.

Кнопка "Попробовать снова" должна:
- Сбрасывать состояние ошибки
- Возвращать overlay в состояние loading
- Повторять запрос

### 3. Переходы между состояниями

```
[Idle] → [Loading] → [Success] → [Idle (другое состояние)]
[Idle] → [Loading] → [Error] → [Loading (retry)] → ...
```

### 4. Обработка успеха

После успешного создания заказа:
- Overlay показывается на уровне `cart-page` (не `review-step`)
- Корзина очищается
- Пользователь видит сообщение об успехе поверх страницы корзины

## Примеры использования

### Создание заказа (авторизованный пользователь)

```javascript
async submitOrder() {
  this.setState({ isSubmitting: true, error: false, success: false });
  this.render();

  try {
    const response = await fetch('/wp-json/elkaretro/v1/orders', { /* ... */ });
    const result = await response.json();

    if (!response.ok) {
      throw new Error(result.message || 'Ошибка при создании заказа');
    }

    // Успех обрабатывается через cart-page
    this.showOrderSuccess(result.order.id);
    
  } catch (error) {
    this.setState({ 
      isSubmitting: true,
      error: true,
      errorMessage: error.message 
    });
    this.render();
  }
}
```

### Создание анонимного заказа

```javascript
async submitAnonymousOrderRequest(email, preferredCommunication) {
  this.setState({ isSubmitting: true, error: false, success: false });
  this.render();

  try {
    const response = await fetch('/wp-json/elkaretro/v1/orders/create-anonymous', { /* ... */ });
    const result = await response.json();

    if (!response.ok) {
      throw new Error(result.message || 'Ошибка при создании заказа');
    }

    // Успех обрабатывается через cart-page
    this.showOrderSuccess(result.order.id);
    
  } catch (error) {
    this.setState({ 
      isSubmitting: true,
      error: true,
      errorMessage: error.message 
    });
    this.render();
  }
}
```

## Стилизация

### Overlay

```css
.review-step_overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.7);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 10000;
}
```

### Error state

```css
.review-step_error {
  background: var(--color-secondary, #202227);
  padding: 2rem;
  border-radius: 8px;
  text-align: center;
  border: 2px solid var(--color-error, #ef4444);
}

.review-step_error ui-icon {
  color: var(--color-error, #ef4444);
}
```

### Success state

```css
.review-step_success {
  background: var(--color-secondary, #202227);
  padding: 2rem;
  border-radius: 8px;
  text-align: center;
  border: 2px solid var(--color-success, #22c55e);
}

.review-step_success ui-icon {
  color: var(--color-success, #22c55e);
}
```

