# Беклог: Flow Оформления Заказа

> 📋 **Ответственность:** Специалист 2 (Flow Заказа)  
> 📖 **Документация:** `components/cart/ORDER_STEPS.md`

---

## Обзор

Реализация нового flow оформления заказа:
- 3 шага оформления (товары, доставка, проверка)
- Модальные окна для авторизации и анонимного заказа
- Два endpoint'а: для авторизованных и анонимных заказов
- Новая схема статусов (`post_status` + `order_status`)

---

## Backend задачи

### 1. Регистрация кастомных статусов WordPress

#### 1.1. Регистрация post_status

**Задача:** Зарегистрировать кастомные статусы `completed` и `rejected` для заказов

**Требования:**
- [ ] Использовать `register_post_status()` для регистрации статусов
- [ ] Статус `completed` — для завершённых заказов
- [ ] Статус `rejected` — для отклонённых/отменённых заказов
- [ ] Статус `publish` — используется по умолчанию (актуальные заказы)
- [ ] Добавить метки для админки WordPress

**Файлы для создания/изменения:**
- `core/orders/order-loader.php` — добавить регистрацию статусов в `init` hook

**Оценка:** 1-2 часа

---

### 2. Создание REST API endpoint'ов

#### 2.1. Endpoint: Создание заказа авторизованным пользователем (`/orders/create`)

**Задача:** Реализовать endpoint для создания заказа авторизованным пользователем

**Требования:**
- [ ] Требует авторизации (проверка `get_current_user_id()`)
- [ ] Принимает данные заказа:
  - `cart` — данные корзины
  - `delivery` — данные доставки
  - `comment` — комментарий к заказу (опционально)
  - `preferred_communication` — предпочитаемые способы связи (опционально)
- [ ] Валидирует корзину (не пустая)
- [ ] Валидирует и резервирует товары (`validate_and_book_items`)
- [ ] Валидирует и пересчитывает цены (`validate_and_recalculate_prices`)
- [ ] Генерирует номер заказа (`generate_order_number`)
- [ ] Создаёт заказ с `post_status = 'publish'` и `order_status = 'pending_confirmation'`
- [ ] Сохраняет данные в PODS поля
- [ ] Очищает корзину пользователя
- [ ] Отправляет email-уведомления
- [ ] Возвращает данные созданного заказа

**Файлы для изменения:**
- `core/orders/order-rest-controller.php` — обновить метод `create_order()`
- `core/orders/order-service.php` — обновить метод `create_order()`

**Оценка:** 4-6 часов

---

#### 2.2. Endpoint: Создание анонимного заказа (`/orders/create-anonymous`)

**Задача:** Реализовать endpoint для создания анонимного заказа без авторизации

**Требования:**
- [ ] Не требует авторизации (доступен для всех)
- [ ] Принимает данные заказа:
  - `cart` — данные корзины
  - `delivery` — данные доставки
  - `email` — email для связи (обязательно)
  - `comment` — комментарий к заказу (опционально)
  - `preferred_communication` — предпочитаемые способы связи (опционально)
- [ ] Валидирует email (формат)
- [ ] Валидирует корзину (не пустая)
- [ ] Валидирует и резервирует товары (`validate_and_book_items`)
- [ ] Валидирует и пересчитывает цены (`validate_and_recalculate_prices`)
- [ ] Генерирует номер заказа (`generate_order_number`)
- [ ] Создаёт заказ с `post_status = 'publish'` и `order_status = 'pending_confirmation'`
- [ ] Сохраняет email в мета-поле заказа (не создаёт пользователя)
- [ ] Сохраняет данные в PODS поля
- [ ] **НЕ** очищает корзину (так как пользователь не авторизован)
- [ ] Отправляет email-уведомления
- [ ] Возвращает данные созданного заказа

**Файлы для создания/изменения:**
- `core/orders/order-rest-controller.php` — добавить новый метод `create_anonymous_order()`
- `core/orders/order-service.php` — добавить новый метод `create_anonymous_order()`

**Оценка:** 4-6 часов

---

### 3. Обновление Order Service

#### 3.1. Обновление метода создания заказа

**Задача:** Обновить `create_order()` для работы с новой схемой статусов

**Требования:**
- [ ] Использовать `post_status = 'publish'` вместо `'awaiting_payment'`
- [ ] Устанавливать `order_status = 'pending_confirmation'` в мета-поле
- [ ] Сохранять историю изменений статусов (`order_status_history`)
- [ ] Обновить логику валидации и резервирования товаров
- [ ] Обновить логику очистки корзины (только для авторизованных)

**Файлы для изменения:**
- `core/orders/order-service.php` — метод `create_order()`

**Оценка:** 3-4 часа

---

#### 3.2. Новый метод для анонимных заказов

**Задача:** Создать метод `create_anonymous_order()` для анонимных заказов

**Требования:**
- [ ] Аналогичен `create_order()`, но:
  - Не требует `user_id`
  - Сохраняет email в мета-поле заказа
  - Не очищает корзину
  - Не привязывает заказ к пользователю
- [ ] Валидация и резервирование товаров
- [ ] Валидация и пересчёт цен
- [ ] Генерация номера заказа
- [ ] Сохранение в PODS
- [ ] Отправка email-уведомлений

**Файлы для создания/изменения:**
- `core/orders/order-service.php` — новый метод `create_anonymous_order()`

**Оценка:** 4-5 часов

---

#### 3.3. Метод для сохранения order_status

**Задача:** Создать метод для сохранения `order_status` в мета-поле

**Требования:**
- [ ] Метод `set_order_status($order_id, $status)`
- [ ] Сохранение в мета-поле `order_status`
- [ ] Сохранение истории изменений в `order_status_history`
- [ ] Валидация статуса (проверка допустимых переходов)

**Файлы для создания/изменения:**
- `core/orders/order-service.php` — новый метод `set_order_status()`

**Оценка:** 2-3 часа

---

#### 3.4. Метод для получения order_status

**Задача:** Создать метод для получения `order_status` из мета-поля

**Требования:**
- [ ] Метод `get_order_status($order_id)`
- [ ] Возвращает текущий `order_status`
- [ ] Если статус не установлен — возвращает `pending_confirmation` по умолчанию

**Файлы для создания/изменения:**
- `core/orders/order-service.php` — новый метод `get_order_status()`

**Оценка:** 1 час

---

### 4. Обновление PODS полей

#### 4.1. Добавление поля order_status

**Задача:** Добавить поле `order_status` в PODS конфигурацию заказов

**Требования:**
- [ ] Поле типа `text` или `select`
- [ ] Сохранение в мета-поле `order_status`
- [ ] Значения: `pending_confirmation`, `awaiting_payment`, `awaiting_shippment`, `shipped`, `awaiting_pickup`, `clarification`, `completed`, `rejected`, `cancelled`

**Файлы для изменения:**
- Конфигурация PODS (через админку или код)

**Оценка:** 1 час

---

#### 4.2. Добавление поля order_status_history

**Задача:** Добавить поле `order_status_history` для истории изменений статусов

**Требования:**
- [ ] Поле типа `text` или `json`
- [ ] Сохранение массива изменений с датами и статусами
- [ ] Формат: `[{status: 'pending_confirmation', date: '2024-01-01 12:00:00', changed_by: 'system'}]`

**Файлы для изменения:**
- Конфигурация PODS (через админку или код)

**Оценка:** 1 час

---

#### 4.3. Добавление полей для анонимных заказов

**Задача:** Добавить поля для хранения данных анонимных заказов

**Требования:**
- [ ] Поле `anonymous_email` — email анонимного заказа
- [ ] Поле `preferred_communication` — предпочитаемые способы связи
- [ ] Поле `is_anonymous` — флаг анонимного заказа

**Файлы для изменения:**
- Конфигурация PODS (через админку или код)

**Оценка:** 1 час

---

## Frontend задачи

### 5. Новый flow оформления заказа

#### 5.1. Обновление компонента cart-page

**Задача:** Обновить страницу корзины для нового flow

**Требования:**
- [ ] Убрать старый `order-wizard`
- [ ] Добавить новый компонент `order-flow` (или использовать существующий `cart-page` с шагами)
- [ ] Шаг 1: Перечень товаров (уже реализован в `cart-page`)
- [ ] Шаг 2: Выбор способа доставки (новый компонент)
- [ ] Шаг 3: Проверка данных (новый компонент)
- [ ] Навигация между шагами
- [ ] Сохранение данных в LocalStorage

**Файлы для изменения:**
- `components/cart/cart-page/cart-page.js`
- `components/cart/cart-page/cart-page-template.js`

**Оценка:** 4-6 часов

---

#### 5.2. Компонент шага 2: Выбор способа доставки

**Задача:** Создать компонент для выбора способа доставки

**Требования:**
- [ ] Компонент `delivery-step` (Web Component)
- [ ] Выбор категории доставки (Самовывоз / Курьерская доставка / Почта России)
- [ ] Выбор конкретного способа доставки в рамках категории
- [ ] Условное отображение полей в зависимости от способа доставки
- [ ] Динамическая валидация с задержкой (3 секунды)
- [ ] Расчёт стоимости доставки
- [ ] Обновление блока с итоговой стоимостью
- [ ] Сохранение данных в LocalStorage

**Файлы для создания:**
- `components/cart/delivery-step/delivery-step.js`
- `components/cart/delivery-step/delivery-step-template.js`
- `components/cart/delivery-step/delivery-step-styles.css`

**Оценка:** 8-10 часов

---

#### 5.3. Компонент шага 3: Проверка данных

**Задача:** Создать компонент для проверки данных заказа

**Требования:**
- [ ] Компонент `review-step` (Web Component)
- [ ] Отображение сводной информации:
  - Количество товаров (игрушки и аксессуары отдельно)
  - Способ доставки и адрес
  - Итоговая стоимость
- [ ] Поле для комментария к заказу (textarea, опционально)
- [ ] Поле для предпочитаемых способов связи (textarea, опционально)
- [ ] Кнопка "Оформить заказ"
- [ ] Обработка клика:
  - Если пользователь авторизован — сразу отправка заказа
  - Если не авторизован — показ модального окна

**Файлы для создания:**
- `components/cart/review-step/review-step.js`
- `components/cart/review-step/review-step-template.js`
- `components/cart/review-step/review-step-styles.css`

**Оценка:** 4-6 часов

---

### 6. Модальные окна

#### 6.1. Модальное окно для анонимного заказа

**Задача:** Создать модальное окно для анонимного заказа

**Требования:**
- [ ] Компонент `anonymous-order-modal` (Web Component)
- [ ] Поле для ввода email (обязательно)
- [ ] Поле для предпочитаемых способов связи (textarea, опционально)
- [ ] Валидация email на фронте
- [ ] Кнопка "Оформить заказ"
- [ ] Отправка запроса на `/orders/create-anonymous`
- [ ] Обработка ошибок
- [ ] Индикация загрузки
- [ ] Закрытие модального окна после успешного создания заказа

**Файлы для создания:**
- `components/cart/anonymous-order-modal/anonymous-order-modal.js`
- `components/cart/anonymous-order-modal/anonymous-order-modal-template.js`
- `components/cart/anonymous-order-modal/anonymous-order-modal-styles.css`

**Оценка:** 4-6 часов

---

#### 6.2. Интеграция модального окна авторизации

**Задача:** Интегрировать модальное окно авторизации в процесс заказа

**Требования:**
- [ ] Использовать компонент `auth-modal` (созданный специалистом 1)
- [ ] Открывать модальное окно при клике на "Зарегистрироваться/Авторизоваться"
- [ ] После успешной авторизации закрывать модальное окно и отправлять заказ
- [ ] Обработка ошибок авторизации

**Файлы для изменения:**
- `components/cart/review-step/review-step.js`

**Оценка:** 2-3 часа

---

### 7. Обновление логики создания заказа

#### 7.1. Обновление метода создания заказа на фронте

**Задача:** Обновить логику создания заказа для использования новых endpoint'ов

**Требования:**
- [ ] Определить, какой endpoint использовать:
  - Если пользователь авторизован — `/orders/create`
  - Если не авторизован — `/orders/create-anonymous` (через модальное окно)
- [ ] Подготовка данных заказа:
  - `cart` — данные корзины
  - `delivery` — данные доставки
  - `comment` — комментарий
  - `preferred_communication` — способы связи
- [ ] Отправка запроса на соответствующий endpoint
- [ ] Обработка ответа
- [ ] Очистка корзины после успешного создания заказа
- [ ] Редирект на страницу успеха или показ сообщения

**Файлы для изменения:**
- `components/cart/review-step/review-step.js`
- `components/cart/anonymous-order-modal/anonymous-order-modal.js`

**Оценка:** 3-4 часа

---

## Интеграция с авторизацией

### 8. Интеграция модального окна авторизации

**Задача:** Интегрировать модальное окно авторизации в flow заказа

**Требования:**
- [ ] Использовать компонент `auth-modal` (созданный специалистом 1)
- [ ] Открывать модальное окно при клике на "Зарегистрироваться/Авторизоваться"
- [ ] После успешной авторизации:
  - Закрывать модальное окно
  - Автоматически отправлять заказ на `/orders/create`
- [ ] Обработка ошибок авторизации

**Файлы для изменения:**
- `components/cart/review-step/review-step.js`

**Оценка:** 2-3 часа

---

## Удаление старого кода

### 9. Удаление старого order-wizard

**Задача:** Удалить старый компонент `order-wizard` и связанные шаги

**Требования:**
- [ ] Удалить компонент `order-wizard`
- [ ] Удалить старые шаги (`step-auth`, `step-personal`, `step-logistics`, `step-payment`, `step-confirmation`)
- [ ] Удалить старые стили
- [ ] Обновить документацию

**Файлы для удаления:**
- `components/cart/order-wizard/` — вся директория

**Оценка:** 1 час

---

### 10. Удаление старых endpoint'ов

**Задача:** Удалить старые методы создания заказов

**Требования:**
- [ ] Удалить старый метод `create_order()` из `order-service.php` (или обновить его)
- [ ] Удалить старый метод `register_user_for_order()` (если не используется)
- [ ] Обновить `order-rest-controller.php` для использования новых методов

**Файлы для изменения:**
- `core/orders/order-service.php`
- `core/orders/order-rest-controller.php`

**Оценка:** 2-3 часа

---

## Тестирование

### 11. Интеграционное тестирование

**Задача:** Протестировать полный flow оформления заказа

**Требования:**
- [ ] Тест оформления заказа авторизованным пользователем
- [ ] Тест оформления анонимного заказа
- [ ] Тест авторизации в процессе оформления заказа
- [ ] Тест валидации данных
- [ ] Тест обработки ошибок
- [ ] Тест сохранения в LocalStorage

**Оценка:** 3-4 часа

---

## Итого

**Общая оценка времени:**
- Backend: ~25-35 часов
- Frontend: ~25-35 часов
- Интеграция: ~2-3 часа
- Удаление старого кода: ~3-4 часа
- Тестирование: ~3-4 часа

**Итого: ~58-81 час (~7-10 рабочих дней)**

---

## Приоритеты

### Высокий приоритет (критично для работы)
1. Регистрация кастомных статусов WordPress
2. Endpoint `/orders/create` (авторизованный пользователь)
3. Endpoint `/orders/create-anonymous` (анонимный заказ)
4. Обновление Order Service для новой схемы статусов
5. Компонент шага 2: Выбор способа доставки
6. Компонент шага 3: Проверка данных
7. Модальное окно для анонимного заказа
8. Обновление логики создания заказа на фронте

### Средний приоритет (можно сделать параллельно)
9. Обновление PODS полей
10. Обновление компонента cart-page
11. Интеграция модального окна авторизации

### Низкий приоритет (можно сделать после интеграции)
12. Удаление старого кода
13. Интеграционное тестирование

---

## Зависимости от работы коллеги

### Критичные зависимости
- ✅ Модальное окно авторизации (`auth-modal`) должно быть готово для интеграции
- ✅ Endpoint'ы `/auth/request-code` и `/auth/verify-code` должны быть готовы

### Не критичные зависимости
- ⚠️ Можно использовать временную заглушку авторизации для разработки flow заказа

---

## Заметки

- Важно сохранять данные в LocalStorage на каждом шаге
- Валидация должна быть динамической с задержкой
- Обработка ошибок должна быть понятной для пользователя
- Email-уведомления должны отправляться корректно
- Важно обработать все edge cases (пустая корзина, неверные данные, ошибки сети)

