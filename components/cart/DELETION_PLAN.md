# План удаления старого кода

> 📋 **Статус:** Список файлов и кода, которые будут удалены после реализации новой логики

---

## Стратегия удаления

✅ **Подтверждение:** Да, мы **удаляем** старую логику, а не переписываем её.

**Причины:**
- Нет необходимости в обратной совместимости
- Старая логика не используется в продакшене (или используется минимально)
- Новая логика полностью заменяет старую
- Проще удалить и написать заново, чем пытаться переделать существующий код

---

## Что будет удалено

### 1. Backend: Авторизация/Регистрация (Специалист 1)

#### Файлы для удаления:
- ❌ **НЕ удаляем** `core/user-profile/user-profile-rest-controller.php` (файл остаётся, но методы будут изменены)
- ❌ **НЕ удаляем** `core/user-profile/user-profile-loader.php` (файл остаётся)

#### Методы для удаления/замены в `user-profile-rest-controller.php`:
- [ ] Метод `register()` — **удалить полностью** (строки ~308-399)
  - Старая логика регистрации с множеством полей
  - Заменяется на новые endpoint'ы `/auth/request-code` и `/auth/verify-code`

#### Методы для удаления/замены в `auth-service.js`:
- [ ] Метод `register()` — **удалить или переписать** (строки ~233-307)
- [ ] Метод `login()` — **переписать** для использования новой системы кодов
- [ ] Удалить логику работы с паролями

---

### 2. Backend: Flow Заказа (Специалист 2)

#### Файлы для удаления:
- ❌ **НЕ удаляем** `core/orders/order-service.php` (файл остаётся, но методы будут изменены)
- ❌ **НЕ удаляем** `core/orders/order-rest-controller.php` (файл остаётся, но методы будут изменены)

#### Методы для удаления/замены в `order-service.php`:
- [ ] Метод `register_user_for_order()` — **удалить полностью** (строки ~132-220+)
  - Старая логика регистрации пользователя при создании заказа
  - Заменяется на новую систему авторизации через код
- [ ] Метод `create_order()` — **переписать полностью**
  - Убрать логику регистрации пользователя
  - Добавить поддержку анонимных заказов
  - Обновить схему статусов (`post_status` + `order_status`)

#### Методы для удаления/замены в `order-rest-controller.php`:
- [ ] Метод `create_order()` — **переписать полностью**
  - Убрать обработку `personal` данных
  - Добавить проверку авторизации
  - Добавить новый метод `create_anonymous_order()`

---

### 3. Frontend: Старые формы авторизации/регистрации (Специалист 1)

#### Файлы для удаления:
- [ ] `app/forms/register.js` — **удалить полностью**
  - Старая форма регистрации с множеством полей
  - Заменяется на новое модальное окно `auth-modal`
- [ ] `app/forms/sign-in.js` — **удалить полностью**
  - Старая форма авторизации с паролем
  - Заменяется на новое модальное окно `auth-modal`

#### Компоненты для удаления/замены:
- [ ] Старые модальные окна авторизации/регистрации (если есть)
- [ ] Старые формы в компонентах `user-profile`

---

### 4. Frontend: Старый Order Wizard (Специалист 2)

#### Файлы для удаления:
- [ ] `components/cart/order-wizard/` — **удалить всю директорию**
  - `order-wizard.js`
  - `order-wizard-template.js`
  - `order-wizard-styles.css`
  - `steps/step-auth.js` и связанные файлы
  - `steps/step-personal.js` и связанные файлы
  - `steps/step-logistics.js` и связанные файлы
  - `steps/step-payment.js` и связанные файлы
  - `steps/step-confirmation.js` и связанные файлы

**Причина:** Весь старый wizard заменяется новым flow (шаги 1-3 + модальные окна)

---

### 5. Frontend: Старые формы заказа (Специалист 2)

#### Файлы для удаления:
- [ ] `app/forms/order-personal.js` — **удалить полностью**
  - Старая форма личных данных для заказа
  - Заменяется на модальное окно авторизации или анонимного заказа
- [ ] `app/forms/order-personal-authorized.js` — **удалить полностью**
  - Старая форма для авторизованных пользователей
  - Заменяется на новый flow без этого шага
- [ ] `app/forms/order-logistics.js` — **проверить, возможно переписать**
  - Может использоваться в новом flow, но нужно проверить
- [ ] `app/forms/order-payment.js` — **удалить полностью**
  - Выбор способа оплаты убран из flow заказа
  - Оплата происходит после оформления заказа

---

### 6. Документация (уже удалена)

✅ Уже удалены:
- `ORDER_FLOW.md`
- `ORDER_FLOW_DISCUSSION.md`
- `ORDER_FLOW_IMPLEMENTATION.md`
- `ORDER_CREATION_COMPARISON.md`
- `ORDER_PROCESSING_LOGIC.md`
- `ORDER_PROCESSING_USER_GUIDE.md`
- `QUESTIONS.md`
- `QUESTIONS_ANSWERS.md`
- `BACKLOG.md`
- `order-wizard/FORMS_REFACTORING_PLAN.md`
- `order-wizard/FORMS_REFACTORING_TODO.md`
- `order-wizard/OVERLAY_ANALYSIS.md`

---

## Порядок удаления

### Этап 1: После реализации новой логики (но до удаления старой)

1. ✅ Развернуть новую логику параллельно со старой
2. ✅ Протестировать новую логику
3. ✅ Убедиться, что всё работает

### Этап 2: Удаление старого кода

**Специалист 1 (Авторизация/Регистрация):**
1. Удалить старые методы `register()` и `login()` из backend
2. Удалить старые формы `register.js` и `sign-in.js`
3. Удалить старые компоненты авторизации/регистрации

**Специалист 2 (Flow Заказа):**
1. Удалить метод `register_user_for_order()` из `order-service.php`
2. Удалить старые формы `order-personal.js`, `order-personal-authorized.js`, `order-payment.js`
3. Удалить всю директорию `order-wizard/`
4. Обновить `order-service.php` и `order-rest-controller.php`

### Этап 3: Очистка зависимостей

1. Удалить импорты старых компонентов
2. Удалить регистрацию старых компонентов в `components.js`
3. Удалить старые route'ы из REST API (если не используются)

---

## Риски при удалении

### ⚠️ Важно проверить перед удалением:

1. **Нет ли зависимостей от старого кода:**
   - Проверить, не используется ли старый код в других местах
   - Проверить, нет ли ссылок на старые endpoint'ы

2. **Админка WordPress:**
   - Убедиться, что админка не использует старые методы
   - Проверить, что старые заказы не зависят от удаляемого кода

3. **Интеграции:**
   - Проверить, нет ли внешних интеграций, использующих старые endpoint'ы
   - Проверить, нет ли скриптов, использующих старые формы

---

## Чеклист перед удалением

### Перед удалением старого кода убедиться:

- [ ] Новая логика полностью реализована
- [ ] Новая логика протестирована и работает
- [ ] Новая логика развёрнута на проде
- [ ] Старая логика не используется в продакшене
- [ ] Нет зависимостей от старого кода
- [ ] Старые endpoint'ы не используются
- [ ] Старые компоненты не используются
- [ ] Документация обновлена

---

## Примечания

- **Не удаляем файлы сразу** — сначала реализуем новую логику, потом удаляем старую
- **Удаляем постепенно** — сначала удаляем код, который точно не используется
- **Сохраняем бэкапы** — на случай, если что-то пойдёт не так (git history)
- **Тестируем после удаления** — убеждаемся, что ничего не сломалось

