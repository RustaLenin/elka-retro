# UI Form Kit

UI Form Kit предоставляет набор универсальных Web Components для построения форм. Подход основан на декларативной конфигурации: внешний код формирует JSON-описание формы, а `ui-form-controller` создаёт DOM-структуру, управляет состояниями полей и обрабатывает жизненный цикл отправки данных. Для адаптации доменных источников данных используются конвертеры (см. `helpers/field-config-adapter.js`). Конфигурации не привязаны к конкретной предметной области и могут переиспользоваться в любых проектах.

## 📚 Документация

- **[Архитектура системы форм](./controller/ARCHITECTURE.md)** - Полное руководство по архитектуре, best practices и anti-patterns

## Состояния полей (`ui-form-field`)

Каждое поле теперь самостоятельно управляет визуальными статусами:

- `field.showError(message?, duration?)` — включает состояние ошибки, добавляет атрибут `is_error="true"` и показывает сообщение в встроенном контейнере (по умолчанию не исчезает, если не указан `duration`).
- `field.showSuccess(message?, duration=2000)` — включает атрибут `is_success="true"`, показывает подсказку (например «Готово») и автоматически скрывает её через 2 секунды.
- `field.clearStatus()` — сбрасывает оба состояния и скрывает сообщение.

### Настройка сообщений и автопроверки

- Атрибуты `error-message` и `success-message` задают дефолтные тексты для `showError`/`showSuccess`.
- Атрибут `auto-validate` включает локальную проверку при потере фокуса. Поле:
  1. Проверяет `required`
  2. Прогоняет правила из `config.validation` (`pattern`, `minLength`, `maxLength`, `min`, `max`)
  3. Вызывает `showError()` или `showSuccess()` автоматически.

### CSS-хуки

- `ui-form-field[is_error="true"]` — можно стилизовать рамку, фон, иконки.
- `ui-form-field[is_success="true"]` — для подсветки успешного ввода.
- Сообщение выводится элементом `.ui-form-field__status-message` (абсолютное позиционирование, не влияет на layout).

### Глобальная валидация

`ui-form-controller` больше не рисует общий блок при локальных ошибках: он делегирует поля через `showError/showSuccess`. Благодаря этому автопроверенные поля не валидируются повторно — если поле уже в `is_success`, контроллер видит статус и пропускает лишние проверки.

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Инпуты НЕ перерисовываются при вводе

**ПРАВИЛО**: Инпуты (`ui-input-text`, `ui-input-number`, `ui-input-range`) **НИКОГДА** не должны перерисовываться (вызывать `render()` или менять `innerHTML`) во время ввода пользователем. Это приводит к потере фокуса и невозможности нормального ввода текста.

### Что делать:

1. **Отключить автоматический рендер**: Установить `static autoRender = false` в классе инпута
2. **Обновлять только значение**: В `onStateChanged` при изменении `value` обновлять только `.value` элемента `<input>` напрямую
3. **Обновлять статус без перерисовки**: При изменении `status`/`messages` обновлять только классы и атрибуты через `_updateStatus()`
4. **Перерисовывать только структурные изменения**: Вызывать `render()` только при изменении `disabled`, `readonly`, `clearable`, `prefix`, `suffix` и других свойств, влияющих на структуру DOM

### Пример правильной реализации:

```js
export class UITextInput extends BaseElement {
  static autoRender = false; // Отключаем автоматический рендер

  onStateChanged(_key) {
    // Обновляем только значение инпута напрямую, БЕЗ перерисовки
    if (_key === 'value' && this._inputEl && this._inputEl.value !== this.state.value) {
      this._inputEl.value = this.state.value ?? '';
    }

    // Обновляем статус (классы/атрибуты) без полной перерисовки
    if (_key === 'status' || _key === 'messages') {
      this._updateStatus();
    }

    // Перерисовываем только при изменении структуры
    const structuralKeys = ['disabled', 'readonly', 'clearable', 'prefix', 'suffix'];
    if (structuralKeys.includes(_key)) {
      this.render();
    }
  }

  _updateStatus() {
    // Обновляем только классы и атрибуты, не весь DOM
    const status = this.state.status || 'default';
    this.classList.remove('ui-input-text--error', 'ui-input-text--success');
    if (status !== 'default') {
      this.classList.add(`ui-input-text--${status}`);
    }
    if (this._inputEl) {
      if (status === 'error') {
        this._inputEl.setAttribute('aria-invalid', 'true');
      } else {
        this._inputEl.removeAttribute('aria-invalid');
      }
    }
  }
}
```

**Почему это критично**: При каждом `render()` создаётся новый `<input>` элемент, что приводит к потере фокуса и невозможности ввести более одного символа подряд.

## Стилизация компонентов

### Темная тема

Все инпуты адаптированы под темную тему и используют CSS переменные из глобального `style.css`:
- **Фон инпутов**: `var(--color-card, #17181d)` (темный фон карточек)
- **Границы**: `var(--color-border, #2a2d31)` (темные границы)
- **Текст**: `var(--color-foreground, #f5f7fa)` (светлый текст)
- **Приглушенный текст**: `var(--color-muted-foreground, #8a8f99)` (для placeholder, labels, иконок)
- **Фокус**: `var(--color-primary, #9b8cff)` (фиолетовый акцент)

### Ограничение ширины

Все инпуты имеют ограничение максимальной ширины **600px** для улучшения читаемости и UX:
```css
/* ⚠️ ВАЖНО: Web Component - это элемент, а не класс! Селектор БЕЗ точки */
ui-input-text,
ui-input-number,
ui-input-range {
  max-width: 600px;
  width: 100%;
}
```

Это предотвращает растягивание инпутов на всю ширину контейнера на больших экранах, что делает формы более читаемыми и удобными.

**Примечание**: Все компоненты форм (инпуты, селекты, чекбоксы, segmented-toggle, filter-chip) имеют ограничение максимальной ширины **600px** для единообразия.

## Рендеринг контролов в полях

**КРИТИЧЕСКИ ВАЖНО**: Контролы (инпуты, селекты, чекбоксы) **вставляются на уровне шаблона формы**, а не управляются полями напрямую.

### Как это работает:

1. `form-controller` через `renderField()` в `form-controller-template-utils.js` генерирует HTML, который включает контрол **внутрь** `<ui-form-field>` как дочерний элемент
2. `ui-form-field` использует `<slot></slot>` для проецирования дочерних элементов (контролов) в свой шаблон
3. При вызове `render()` в `ui-form-field`:
   - **Сначала сохраняются** все дочерние элементы (контролы) перед перезаписью `innerHTML`
   - Затем перезаписывается `innerHTML` новым шаблоном (который содержит `<slot>`)
   - **Затем сохраённые контролы вставляются обратно** как прямые дочерние элементы, чтобы `<slot>` мог их проецировать

**Почему это важно**: Если не сохранять контролы при перерисовке поля, они будут удалены, и `<slot>` не сможет их проецировать.

### Анти-паттерн:

❌ **НЕ создавайте контролы динамически в `ui-form-field`** - они должны быть вставлены через шаблон формы:

```javascript
// ❌ НЕПРАВИЛЬНО
connectedCallback() {
  const control = document.createElement('ui-input-text');
  this.appendChild(control);
}
```

✅ **ПРАВИЛЬНО**: Контролы вставляются через `renderField()` в шаблоне формы.

## Field Config Adapter

Универсальный адаптер для конвертации доменных моделей данных в конфигурации полей UI Form Kit.

### Основные возможности

- **Маппинг типов полей** — автоматическое преобразование доменных типов (taxonomy, number, currency) в типы UI Kit компонентов
- **Источники данных** — поддержка различных источников (window.taxonomy_terms, REST API, статические опции)
- **Декораторы** — расширяемая система для добавления иконок, цветов и метаданных через отдельные адаптеры
- **Валидация** — автоматическая генерация правил валидации на основе конфигурации поля
- **Расширяемость** — регистрация кастомных мапперов, декораторов и адаптеров

### Использование

```javascript
import { FieldConfigAdapter, defaultAdapter } from './helpers/field-config-adapter.js';

// Использование дефолтного адаптера
const fieldConfig = defaultAdapter.adaptField('email', {
  type: 'email',
  display_name: 'Email адрес',
  required: true,
  max_length: 255
});

// Создание кастомного адаптера
const adapter = new FieldConfigAdapter({
  typeMappers: {
    // Добавить кастомный маппер типа
    customType: (fieldConfig) => 'text'
  },
  decoratorAdapter: (sourceConfig, context) => {
    // Добавить декораторы (иконки, цвета)
    return {
      icon: { key: 'mail', icon_position: 'left' }
    };
  },
  dataSourceAdapter: (fieldConfig, context) => {
    // Кастомная обработка источников данных
    return { type: 'static', options: [] };
  }
});

// Конвертация множества полей
const fields = adapter.adaptFields(sourceFields, {
  dataContext: { taxonomyTerms: window.taxonomy_terms },
  skipIfFiltered: true, // Пропустить поля с show_in_filters: false
  sortByLabel: true
});
```

### Поддерживаемые типы полей

- **Текстовые**: `text`, `string`, `email`, `url`, `tel`, `password`
- **Числовые**: `number`, `integer`, `float`, `decimal`, `currency`
- **Диапазоны**: `range` (или `number`/`currency` с `is_range: true`)
- **Таксономии**: `taxonomy` → `select-single` / `select-multi`
- **Выборы**: `select`, `enum` → `select-single` / `select-multi`
- **Чекбоксы**: `boolean`, `checkbox` → `checkbox`
- **Переключатели**: `toggle` → `segmented-toggle`

### Утилита для taxonomy_terms

```javascript
import { taxonomyOptionAdapter } from './helpers/field-config-adapter.js';

// Преобразование терминов таксономии в опции
const options = taxonomyOptionAdapter.adaptOptions(window.taxonomy_terms['occurrence']);
// Результат: [{ value: 'slug', label: 'Name', description: '...' }]
```

## Data Sanitizer

Утилиты для очистки и нормализации данных форм перед валидацией и отправкой.

### Основные возможности

- **Типобезопасная нормализация** — автоматическое приведение типов для разных полей
- **Защита от XSS** — опциональное экранирование HTML для текстовых полей
- **Очистка строк** — удаление пробелов, обрезка по maxLength
- **Нормализация чисел** — парсинг строк в числа, применение min/max/precision
- **Обработка специальных типов** — диапазоны, множественный выбор, чекбоксы
- **Предустановленные режимы** — strict, soft, filters

### Использование

```javascript
import { sanitizeForm, sanitizeField, createSanitizeHandler, sanitizePresets } from './helpers/data-sanitizer.js';

// Санитизация одного поля
const cleanValue = sanitizeField('email', '  user@example.com  ', {
  type: 'email',
  maxLength: 255
}, { trimStrings: true });

// Санитизация всей формы
const cleanValues = sanitizeForm(formValues, fieldConfigs, sanitizePresets.strict);

// Создание хендлера для pipeline
const formConfig = {
  pipeline: {
    sanitize: createSanitizeHandler(fieldConfigs, sanitizePresets.strict),
    validate: validateHandler,
    submit: submitHandler
  }
};
```

### Поддерживаемые типы полей

- **text/email/url/tel/password** — trim, maxLength, HTML-экранирование (опционально)
- **number** — парсинг, нормализация, применение min/max/precision
- **range** — нормализация min/max, проверка min <= max
- **select-single** — trim, преобразование пустых в null
- **select-multi** — нормализация массива, удаление дубликатов, maxSelections
- **checkbox** — преобразование в boolean
- **segmented-toggle** — валидация значения по опциям

### Предустановки

- **`sanitizePresets.strict`** — строгая санитизация (trim, escapeHtml, convertEmptyToNull)
- **`sanitizePresets.soft`** — мягкая санитизация (только trim)
- **`sanitizePresets.filters`** — для фильтров (trim, normalizeNumbers, без escapeHtml)

## Form Storage

Адаптер для сохранения и восстановления состояния формы в LocalStorage.

### Основные возможности

- **Сохранение/восстановление** — автоматическое сохранение значений полей и восстановление при загрузке
- **Версионирование** — поддержка миграций данных при изменении схемы
- **Безопасность** — исключение чувствительных полей (пароли, токены) для GDPR/privacy
- **Автоочистка** — удаление устаревших данных по maxAge
- **Обработка ошибок** — graceful degradation при недоступности LocalStorage

### Использование

```javascript
import { createFormStorage, storagePresets } from './helpers/form-storage.js';

// Создание адаптера хранилища
const storage = createFormStorage('login-form:v1', storagePresets.standard);

// Сохранение состояния формы
storage.save(formValues, { lastModified: Date.now() });

// Восстановление состояния
const savedValues = storage.load();
if (savedValues) {
  // Восстановить значения в форму
  formController.setState({ values: savedValues });
}

// Очистка сохраненных данных
storage.clear();

// Проверка наличия данных
if (storage.hasData()) {
  // Данные есть
}
```

### Предустановки

- **`storagePresets.standard`** — стандартное хранилище без ограничений
- **`storagePresets.filters`** — для фильтров (автоочистка через 7 дней)
- **`storagePresets.secure`** — безопасное хранилище (исключает пароли, токены)
- **`storagePresets.temporary`** — временное хранилище (автоочистка через 1 час)

### Интеграция с form-controller

```javascript
const formConfig = {
  id: 'login-form',
  storageKey: 'login-form:v1',
  // ... остальная конфигурация
};

// При инициализации формы
const formController = document.querySelector('ui-form-controller');
const storage = createFormStorage(formConfig.storageKey, {
  excludeFields: ['password'], // Не сохранять пароль
  maxAge: 24 * 60 * 60 * 1000 // Хранить 24 часа
});

// Восстановление при загрузке
const savedValues = storage.load();
if (savedValues) {
  formController.setState({ values: savedValues });
}

// Сохранение при изменении
formController.addEventListener('ui-form:change', () => {
  storage.save(formController.getValues());
});
```

### Миграции данных

```javascript
const storage = createFormStorage('form:v1', {
  version: '2.0.0',
  migrate: (oldVersion, oldData) => {
    // Преобразование данных из старой версии в новую
    if (oldVersion === '1.0.0') {
      return {
        values: {
          // Маппинг старых полей в новые
          newField: oldData.values.oldField,
          ...oldData.values
        },
        metadata: oldData.metadata
      };
    }
    return oldData;
  }
});
```

## Form Pipeline Utilities

Утилиты для оркестрации пайплайна обработки формы: sanitize → validate → submit → handle response.

### Основные возможности

- **Готовые обработчики** — создание обработчиков для sanitize, validate, submit, onSuccess, onError
- **Интеграция с helpers** — автоматическая интеграция с validation-registry и data-sanitizer
- **REST API** — готовый обработчик отправки через fetch
- **Retry политика** — поддержка повторных попыток при ошибках
- **Предустановки** — готовые конфигурации для разных сценариев

### Использование

```javascript
import { 
  createPipeline, 
  createPipelineSanitizeHandler, 
  createValidateHandler,
  createRestSubmitHandler,
  createRetryHandler,
  pipelinePresets 
} from './helpers/form-pipeline.js';

// Создание полного пайплайна
const pipeline = createPipeline({
  fields: fieldConfigs,
  endpoint: '/api/submit',
  storageAdapter: storage,
  sanitizeOptions: sanitizePresets.strict,
  retryOptions: {
    maxRetries: 3,
    delay: 1000
  }
});

// Использование предустановок
const restPipeline = pipelinePresets.rest('/api/form', fieldConfigs);
const filtersPipeline = pipelinePresets.filters(submitHandler, fieldConfigs, storage);
const resilientPipeline = pipelinePresets.resilient('/api/form', fieldConfigs, {
  retryOptions: { maxRetries: 5 }
});

// Кастомные обработчики
const pipeline = {
  sanitize: createPipelineSanitizeHandler(fields, sanitizePresets.strict),
  validate: createValidateHandler(fields),
  submit: createRetryHandler(
    createRestSubmitHandler('/api/submit'),
    { maxRetries: 3, delay: 1000 }
  ),
  onSuccess: (context) => {
    console.log('Success!', context.result);
  },
  onError: (context, error) => {
    console.error('Error:', error);
  }
};
```

### Обработчики

- **`createPipelineSanitizeHandler(fields, options)`** — обработчик санитизации для pipeline на основе data-sanitizer
- **`createValidateHandler(fields)`** — обработчик валидации на основе validation-registry
- **`createRestSubmitHandler(endpoint, options)`** — обработчик отправки через REST API
- **`createRetryHandler(handler, options)`** — обертка с retry политикой
- **`createSaveOnSuccessHandler(storageAdapter)`** — сохранение данных при успехе
- **`createErrorHandler(options)`** — обработчик ошибок с логированием

### Предустановки

- **`pipelinePresets.rest`** — стандартный пайплайн для REST API
- **`pipelinePresets.filters`** — пайплайн для фильтров (без валидации, с сохранением)
- **`pipelinePresets.resilient`** — пайплайн с retry для нестабильных соединений

### Импорт всех утилит

Для удобства все утилиты можно импортировать из одного места:

```javascript
import {
  validationRegistry,
  FieldConfigAdapter,
  sanitizeForm,
  createFormStorage,
  createPipeline,
  getActiveFilters
} from './helpers/index.js';
```

## Supported Field Types
- **text** — однострочная текстовая ячейка. Поддерживаемые ключи: `id`, `label`, `placeholder`, `defaultValue`, `maxLength`, `mask`, `disabled`, `readonly`, `required`, `autocomplete`, `hints`, `validation`, `tooltip`, `icon`. Компонент: `ui-input-text`.
- **number** — контроль для числового ввода. Ключи: `id`, `label`, `placeholder`, `defaultValue`, `min`, `max`, `step`, `precision`, `format`, `currency`, `disabled`, `readonly`, `required`, `hints`, `validation`, `tooltip`, `icon`. Компонент: `ui-input-number`.
- **range** — диапазон значений (два связанных поля: min и max). Ключи: `id`, `label`, `minLabel`, `maxLabel`, `minPlaceholder`, `maxPlaceholder`, `defaultValue` (объект `{ min, max }`), `min`, `max`, `step`, `precision`, `format`, `currency`, `disabled`, `readonly`, `required`, `hints`, `validation`, `tooltip`, `icon`. Компонент: `ui-input-range`. Валидация: автоматически проверяет `min <= max`.
- **segmented-toggle** — переключатель режимов (компактный выбор из нескольких опций). Ключи: `id`, `label`, `defaultValue`, `options` (массив `[{ value, label }]`), `disabled`, `required`, `hints`, `validation`, `tooltip`, `icon`. Компонент: `ui-segmented-toggle`. События: `ui-segmented-toggle:change` с `{ value, previousValue }`.
- **select-single** — селект с единственным выбором. Ключи: `id`, `label`, `placeholder`, `defaultValue`, `searchable`, `allowClear`, `disabled`, `required`, `dataSource`, `decorators`, `hints`, `validation`, `tooltip`, `icon`. Компонент: `ui-select-single`.
- **select-multi** — селект с множественным выбором. Ключи: `id`, `label`, `placeholder`, `defaultValue`, `searchable`, `maxSelections`, `allowSelectAll`, `disabled`, `required`, `dataSource`, `decorators`, `hints`, `validation`, `tooltip`, `icon`. Компонент: `ui-select-multi`.
- **checkbox** — одиночный чекбокс. Ключи: `id`, `label`, `description`, `defaultValue`, `indeterminate`, `disabled`, `required`, `hints`, `validation`, `tooltip`, `icon`. Компонент: `ui-form-checkbox`.

### Common Field Attributes
- `id` — строковый ключ, по которому поле будет идентифицировано при сборе данных.
- `label` — основная подпись.
- `description` — дополнительный поясняющий текст (необязательный).
- `defaultValue` — начальное значение до загрузки сохранённого состояния.
- `disabled` / `readonly` — управление доступностью взаимодействия.
- `required` — признак обязательности при валидации.
- `hints` — набор подсказок: `{"field": "...", "success": "...", "error": "..."}`.
- `validation` — последовательность проверок (`rule`, `value`, `message`, `severity`, `async`).
- `tooltip` — описание всплывающей подсказки (тип, контент, иконка).
- `icon` — конфигурация иконки, передаваемая в `ui-icon`.
  ```json
  {
    "key": "filter",
    "icon_position": "left",
    "on_click": "clear-data",
    "color": "#FFAA00"
  }
  ```

### Option Attributes (select controls)
- `value` — машинное значение опции.
- `label` — отображаемый текст.
- `description` — дополнительное пояснение (optional).
- `disabled` — запрет выбора конкретной опции.
- `icon` — конфигурация иконки для опции (передаётся целиком в `ui-icon`).
- `color` — цветовая метка (HEX или CSS-переменная).
- `meta` — произвольный объект, доступный шаблону опции.

### Select-specific attributes
- `searchable` — явно включает поиск (автоматически включается при большом количестве опций).
- `decorators` — флаги для отображения иконок/цветов/мета-данных (`{ "optionIcon": true, "optionColor": true }`).
- `allowClear` (single) — разрешает сброс значения.
- `allowSelectAll` (multi) — добавляет действие «выбрать все/снять все».
- `maxSelections` (multi) — лимит количества выбранных опций.
- `hierarchical` / `groups` — будущая поддержка древовидных и группированных списков (адаптер отвечает за структуру).
- `filterFn` — путь к кастомной функции фильтрации (принимает `(options, query)`).
- `loading` — отображает состояние загрузки (поддержка в шаблоне, данные управляются извне).
- `emptyMessage`, `searchPlaceholder` — локализация пустых/поисковых состояний.

## Form Attributes
- `id` — идентификатор формы, используется в событиях и при сохранении состояния.
- `title` — заголовок интерфейса.
- `description` — вступительный текст/инструкция.
- `submitLabel` — подпись основной кнопки.
- `cancelLabel` — подпись вторичной кнопки (если требуется).
- `storageKey` — ключ для LocalStorage; пустое значение отключает сохранение.
- `icon` — конфигурация иконки формы.
  ```json
  {
    "key": "settings",
    "icon_position": "left",
    "on_click": "hide-show-data",
    "color": "#0055FF"
  }
  ```
- `pipeline` — контракт обработчиков: `sanitize`, `validate`, `submit`, `onSuccess`, `onError` (строки или ссылки на функции).
- `state` — предустановленные значения полей.
- `metadata` — произвольные данные, которые контроллер пробрасывает в события.
- `debug` — переключатель расширенного режима (дополнительные кнопки, панели, логирование).
- `layout` — указания по верстке (`columns`, `groups`, `steps`, `sections`).
- `fields` — массив деклараций полей (см. ниже).
- `actions` — описание кнопок и управляющих элементов (см. раздел ниже).

## Layout regions
```text
form.ui-form-controller
 ├── header   → заголовок, описание, иконка (может быть вынесен в header модального окна)
 ├── status   → глобальные сообщения (валидация, успех, ошибка, прогресс)
 ├── body     → контейнер для полей и секций
 │   └── layout scaffolding (columns, groups, steps)
 ├── actions  → зона кнопок (submit + дополнительные действия)
 └── debug    → отладочная панель (опционально, включается `debug: true`)
```
- Структура одинакова для страниц и модальных форм; при использовании модального окна header/footer можно привязывать к соответствующим слотам модала.
- Лоадеры после сабмита, предупреждения и итоговые сообщения располагаются в секции `status`.
- Отладочная панель отображает текущее состояние, очищенные данные и журнал пайплайна.

### Layout Scaffolding

Форма поддерживает гибкую структуру раскладки через конфигурацию `layout`.

#### Columns (Колонки)

Размещение полей в несколько колонок:

```json
{
  "layout": {
    "columns": 2,
    "type": "columns"
  }
}
```

Поддерживается 1, 2 или 3 колонки. На мобильных устройствах автоматически переключается на 1 колонку.

#### Groups (Группировка полей)

Группировка полей с заголовками и описаниями:

```json
{
  "layout": {
    "columns": 2,
    "groups": [
      {
        "id": "personal",
        "label": "Личные данные",
        "description": "Укажите вашу контактную информацию",
        "fields": ["firstName", "lastName", "email"]
      },
      {
        "id": "address",
        "label": "Адрес",
        "fields": ["street", "city", "zipCode"]
      }
    ]
  }
}
```

Группы могут размещаться в несколько колонок. Поля размещаются внутри групп через обычный slot. Связь полей с группами осуществляется через атрибут `data-group-id` на полях или автоматически по порядку размещения.

#### Steps (Пошаговая форма)

Реализация wizard-форм с несколькими шагами:

```json
{
  "layout": {
    "type": "steps",
    "currentStep": 1,
    "steps": [
      {
        "id": "step1",
        "label": "Личные данные",
        "description": "Шаг 1 из 3",
        "groups": [
          { "id": "personal", "label": "Контактная информация", "fields": ["name", "email"] }
        ]
      },
      {
        "id": "step2",
        "label": "Адрес",
        "description": "Шаг 2 из 3",
        "groups": [
          { "id": "address", "label": "Место доставки", "fields": ["street", "city"] }
        ]
      },
      {
        "id": "step3",
        "label": "Подтверждение",
        "description": "Шаг 3 из 3"
      }
    ]
  }
}
```

Каждый шаг может содержать группы полей. Шаги имеют индикаторы прогресса, активный шаг помечается визуально.

**Особенности:**
- Только активный шаг отображается
- Completed шаги помечаются визуально
- Индикаторы шагов в шапке формы
- Поддержка валидации на уровне шагов (TODO: в будущем)

**Программное управление шагами:**
```javascript
const formController = document.querySelector('ui-form-controller');
formController.configure({ 
  layout: { 
    ...formController.state.layout, 
    currentStep: 2 
  } 
});
```

## Actions
```json
{
  "submit": {
    "label": "Отправить",
    "type": "primary",
    "icon": {
      "key": "arrow-right",
      "icon_position": "right",
      "on_click": "noop",
      "color": "#0055FF"
    },
    "loadingLabel": "Отправляем...",
    "successLabel": "Готово",
    "disabled": false
  },
  "extra": [
    {
      "id": "clear",
      "label": "Очистить",
      "type": "secondary",
      "icon": {
        "key": "trash",
        "icon_position": "left",
        "on_click": "clear-data",
        "color": "#888888"
      },
      "handler": "formController.clear",
      "visibleWhen": "debug"
    },
    {
      "id": "copy-json",
      "label": "Скопировать JSON",
      "type": "ghost",
      "icon": {
        "key": "copy",
        "icon_position": "left",
        "on_click": "copy-data",
        "color": "#888888"
      },
      "handler": "formController.copyToClipboard",
      "visibleWhen": "debug"
    }
  ]
}
```
- `submit` — основная кнопка формы, основана на компоненте `ui-button`.
- `extra` — массив дополнительных действий (очистка, копирование, кастомные сценарии).
- `visibleWhen` — условие показа (`"always"`, `"debug"`, `"never"` или функция). По умолчанию кнопка видна всегда.
- `handler` — ссылка на функцию или явный колбэк; получает форму, текущее состояние и результат санитизации.
- Все свойства `ui-button` поддерживаются и могут передаваться в конфигурации.

## Debug mode
```json
{
  "debug": true,
  "actions": {
    "extra": [
      { "id": "clear", "handler": "formController.clear", "visibleWhen": "debug" },
      { "id": "copy-json", "handler": "formController.copyToClipboard", "visibleWhen": "debug" }
    ]
  }
}
```
- В режиме `debug` форма может отображать техническую панель: текущее состояние, очищенные данные, вывод валидации, ответы submit-хендлера.
- Активируется расширенное логирование пайплайна: `sanitize`, `validate`, `submit`, `onSuccess`, `onError`.
- Кнопки с `visibleWhen: "debug"` становятся видимыми автоматически.
- Рекомендуется отключать режим на продакшене или управлять им через фичфлаги.

## Validation Registry

UI Form Kit включает реестр валидаторов (`validation-registry.js`), который предоставляет стандартизированный способ валидации полей формы.

### Встроенные валидаторы

- **`required`** — проверка обязательного поля
- **`minLength`** / **`maxLength`** — проверка длины строки
- **`pattern`** — проверка по регулярному выражению
- **`min`** / **`max`** — проверка числовых значений
- **`range`** — проверка диапазона (для `ui-input-range`)
- **`maxSelections`** — максимальное количество выбранных опций (для `ui-select-multi`)
- **`email`** — простая проверка email адреса

### Использование

```javascript
import { validationRegistry } from './helpers/validation-registry.js';

// Валидация одного поля
const result = await validationRegistry.validate(
  fieldValue,
  fieldConfig.validation,
  { fieldId: 'email', fieldConfig, formValues }
);

// Регистрация кастомного валидатора
validationRegistry.register({
  rule: 'customRule',
  validator: (value, ruleValue, context) => {
    // ваша логика валидации
    return true; // или ValidationError
  },
  defaultMessage: 'Сообщение по умолчанию',
  async: false // или true для async валидатора
});
```

### Формат правил валидации

```json
{
  "validation": [
    {
      "rule": "required",
      "value": true,
      "message": "Поле обязательно для заполнения",
      "severity": "error"
    },
    {
      "rule": "pattern",
      "value": "^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$",
      "message": "Введите корректный email",
      "severity": "error"
    }
  ]
}
```

### Формат результата валидации

```javascript
{
  valid: boolean,
  errors: [
    {
      rule: 'required',
      message: 'Поле обязательно для заполнения',
      severity: 'error',
      fieldId: 'email'
    }
  ],
  fieldMessages: {
    'email': {
      status: 'error',
      messages: {
        error: ['Поле обязательно для заполнения'],
        warning: [],
        info: []
      }
    }
  },
  formMessages: {
    message: 'Пожалуйста, исправьте ошибки в полях формы',
    details: ['Поле обязательно для заполнения']
  }
}
```

Валидация совместима с форматом, используемым `ui-form-controller` (`_applyValidationResult`).

## Submission pipeline
- `ui-form-controller` предоставляет публичный метод `submit()`, который запускает последовательность `sanitize → validate → submit → onSuccess/onError`.
- Каждая стадия подключается через конфиг `pipeline` и может быть синхронной или асинхронной (Promise).
- Форма ожидает, что `submit` вернёт результат (например, ответ сервиса) или выбросит исключение — это попадёт в `onSuccess`/`onError`.
- Дополнительные методы `clear()` и `copyToClipboard()` должны использоваться действиями (`actions.extra`) и доступны в режиме отладки.
- Отправка данных (HTTP, RPC, локальное хранилище) реализуется внутри переданного хендлера `pipeline.submit`, что позволяет выносить конкретные протоколы из компонента.
- Зона body может содержать как поля, так и вспомогательные блоки (инструкции, баннеры); главное — следовать декларативной схеме.
- Поля инициируют события `…:input`, `…:change`, `…:validation`, которые обрабатывает `ui-form-controller` для синхронизации состояния.
- Перед запуском пользовательской `validate` фазы выполняется встроенная проверка `required` (включая чекбоксы с `required=true`): при нарушении автоматически формируются ошибки на полях и общее сообщение.

## Controller API
```ts
// Методы экземпляра <ui-form-controller>
submit(): Promise<void>
clear(): void
copyToClipboard(): void
configure(config: FormConfig): void
setPipeline(pipeline: PipelineHandlers): void
setActions(actions: FormActions): void
getValues(): Record<string, unknown>
getField(fieldId: string): HTMLElement | null
clearField(fieldId: string): boolean
enableAutosubmit(config?: AutosubmitConfig): void
disableAutosubmit(): void
isAutosubmitEnabled(): boolean
```
- `configure` позволяет применить конфиг динамически (иконка, layout, autosubmit и т.п.).
- `setPipeline` и `setActions` используются для поздней инициализации обработчиков.
- `submit`, `clear`, `copyToClipboard` синхронизируются с action-кнопками (submit + extra).
- `getValues` возвращает текущий снимок `state.values` (без мутаций исходного объекта).
- `getField` возвращает элемент `ui-form-field` по его `id` или `null` если поле не найдено.
- `clearField` сбрасывает значение конкретного поля в начальное состояние и возвращает `true` при успехе.
- `enableAutosubmit` включает автопередачу с опциональной конфигурацией (если не передана, используется текущая или дефолтная).
- `disableAutosubmit` отключает автопередачу и очищает активный таймер.
- `isAutosubmitEnabled` возвращает `true` если автопередача активна.

### Field & Control Events
- `ui-input-text:*` и `ui-input-number:*` bubbling-события проксируются `ui-form-field` в события вида `ui-form-field:input`, `ui-form-field:change`, `ui-form-field:validation`, сохраняя полезный detail (`value`, `rawValue`, `status`, `messages`, `origin`).
- `ui-form-field` отслеживает состояние `touched`, `dirty`, `status`, `messages` и синхронизирует атрибуты (`disabled`, `required`, `aria-invalid`) с вложенным контролом.
- `ui-form-controller` подписывается на события `ui-form-field:*` для обновления стейта формы, валидации и запуска actions.
- Контролы могут эмитить дополнительные события (`…:clear`, `…:increment`, `…:decrement`) — поле пробрасывает их наружу с контекстом поля через detail.
- `ui-select:*` и `ui-checkbox:*` используют общую схему: `open/close/search/select/deselect/change`, `input/change/validation`, что позволяет `form-field` агрегировать состояние и перезапускать autosubmit.

### Form Events (`ui-form:*`)
События всплывают от `ui-form-controller`, detail включает `formId`, `values`, `controller` и контекст.

| Событие | Когда срабатывает | detail |
| --- | --- | --- |
| `ui-form:init` | После инициализации form controller и привязки полей | `{ values }` |
| `ui-form:input` | Любое `ui-form-field:input` | `{ fieldId, name, value, origin }` |
| `ui-form:change` | После `ui-form-field:change` / stepper | `{ fieldId, value }` |
| `ui-form:validation` | Поле прислало статус/сообщения | `{ fieldId, status, messages }` |
| `ui-form:success` | Пайплайн успешно завершён (`submit`) | `{ values, result }` |
| `ui-form:error` | Пайплайн упал с ошибкой | `{ error }` |
| `ui-form:invalid` | Валидация не прошла (sync/async) | `{ values, validation }` |
| `ui-form:clear` | Метод/кнопка очистки сбросили значения | `{ values }` |
| `ui-form:copy` | Кнопка копирования успешно скопировала JSON | `{ values }` |
| `ui-form:autosubmit-scheduled` | Таймер автопередачи запланирован после изменения поля | `{ delay, eventType, fieldId, scheduledAt }` |
| `ui-form:field-cleared` | Поле очищено через `clearField()` | `{ fieldId, value }` |

## Autosubmit
```json
{
  "autosubmit": {
    "enabled": true,
    "debounceMs": 5000,
    "events": ["change", "blur"],
    "excludeFields": ["password"]
  }
}
```
- При включении `autosubmit.enabled` контроллер запускает таймер после изменения данных.
- Любое новое изменение сбрасывает таймер; по истечении `debounceMs` автоматически вызывается `submit()`.
- Можно указать события, которые подписывают таймер, и список полей, игнорируемых автосабмитом.
- При планировании таймера генерируется событие `ui-form:autosubmit-scheduled` с деталями (`delay`, `eventType`, `fieldId`, `scheduledAt`).
- Публичные методы `enableAutosubmit(config?)`, `disableAutosubmit()`, `isAutosubmitEnabled()` позволяют управлять автопередачей программно.
- Форма корректно реагирует на проходящие события `…:input`, `…:change`: таймер перезапускается только по перечисленным событиям.

## Field Config Example
```json
{
  "id": "product-categories",
  "type": "select-multi",
  "label": "Категории",
  "description": "Выберите один или несколько вариантов",
  "placeholder": "Категории не выбраны",
  "required": false,
  "icon": {
    "key": "category",
    "icon_position": "left",
    "on_click": "clear-data",
    "color": "#FFAA00"
  },
  "searchable": true,
  "maxSelections": 5,
  "dataSource": {
    "type": "global",
    "path": "appData.categories",
    "adapter": "defaultOptionAdapter"
  },
  "decorators": {
    "optionIcon": true,
    "optionColor": true
  },
  "hints": {
    "field": "Можно отметить несколько значений",
    "options": "Цвет отражает актуальность"
  },
  "validation": [
    { "rule": "maxSelections", "value": 5, "message": "Не выбирайте более пяти пунктов" }
  ]
}
```

## Form Config Example (Login)
```json
{
  "id": "login-form",
  "title": "Вход",
  "description": "Введите учетные данные для авторизации",
  "submitLabel": "Войти",
  "cancelLabel": "Забыли пароль?",
  "storageKey": "login-form:v1",
  "debug": false,
  "icon": {
    "key": "user",
    "icon_position": "left",
    "on_click": "hide-show-data",
    "color": "#0055FF"
  },
  "layout": {
    "columns": 1,
    "groups": [
      { "id": "credentials", "label": "Данные для входа", "fields": ["email", "password", "remember-me"] }
    ]
  },
  "fields": [
    {
      "id": "email",
      "type": "text",
      "label": "Email",
      "placeholder": "you@example.com",
      "autocomplete": "email",
      "required": true,
      "validation": [
        { "rule": "pattern", "value": "^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$", "message": "Введите корректный email" }
      ],
      "icon": {
        "key": "mail",
        "icon_position": "left",
        "on_click": "clear-data",
        "color": "#888888"
      }
    },
    {
      "id": "password",
      "type": "text",
      "label": "Пароль",
      "placeholder": "Введите пароль",
      "required": true,
      "mask": "password",
      "validation": [
        { "rule": "minLength", "value": 18, "message": "Минимум 18 символов" }
      ],
      "icon": {
        "key": "lock",
        "icon_position": "left",
        "on_click": "hide-show-data",
        "color": "#888888"
      },
      "hints": {
        "field": "Пароль чувствителен к регистру"
      }
    },
    {
      "id": "remember-me",
      "type": "checkbox",
      "label": "Запомнить меня",
      "defaultValue": true,
      "icon": {
        "key": "check",
        "icon_position": "left",
        "on_click": "clear-data",
        "color": "#00AA55"
      }
    }
  ],
  "actions": {
    "submit": {
      "label": "Войти",
      "type": "primary",
      "icon": {
        "key": "arrow-right",
        "icon_position": "right",
        "on_click": "noop",
        "color": "#0055FF"
      },
      "loadingLabel": "Отправляем...",
      "successLabel": "Готово"
    },
    "extra": [
      {
        "id": "clear",
        "label": "Очистить",
        "type": "secondary",
        "icon": {
          "key": "trash",
          "icon_position": "left",
          "on_click": "clear-data",
          "color": "#888888"
        },
        "handler": "formController.clear",
        "visibleWhen": "debug"
      },
      {
        "id": "copy-json",
        "label": "Копировать JSON",
        "type": "ghost",
        "icon": {
          "key": "copy",
          "icon_position": "left",
          "on_click": "copy-data",
          "color": "#888888"
        },
        "handler": "formController.copyToClipboard",
        "visibleWhen": "debug"
      }
    ]
  },
  "pipeline": {
    "sanitize": "formPipeline.sanitizeLogin",
    "validate": "formPipeline.validateLogin",
    "submit": "authController.submitLogin",
    "onSuccess": "authController.onLoginSuccess",
    "onError": "authController.onLoginError"
  }
}
```

Любая система может подготовить аналогичную конфигурацию, используя собственные источники данных и адаптеры. UI Form Kit интерпретирует только универсальную схему и не делает предположений о природе данных.

## Filter Chips

Компоненты для отображения активных фильтров в виде чипсов (tags).

### ui-filter-chip

Отдельный чипс для одного фильтра.

**Атрибуты:**
- `label` — название фильтра
- `value` — значение фильтра
- `filter-id` — идентификатор фильтра (для связи с полем формы)
- `removable` — разрешает удаление (по умолчанию `true`)

**События:**
- `ui-filter-chip:remove` — при клике на крестик (detail: `{ filterId, label, value }`)
- `ui-filter-chip:click` — при клике на чипс (detail: `{ filterId }`)

### ui-filters-summary

Контейнер для чипсов фильтров.

**Атрибуты:**
- `empty-label` — текст при отсутствии фильтров (по умолчанию "Нет активных фильтров")

**Методы:**
- `setFilters(filters[])` — обновляет список фильтров. Формат: `[{ id, label, value, removable? }]`

**События:**
- `ui-filters-summary:remove` — проксирует `ui-filter-chip:remove`
- `ui-filters-summary:click` — проксирует `ui-filter-chip:click`

### getActiveFilters()

Утилита для получения активных фильтров из формы.

```javascript
import { getActiveFilters } from './helpers/filter-utils.js';

const formController = document.querySelector('ui-form-controller');
const activeFilters = getActiveFilters(formController);
// Возвращает: [{ id, label, value, fieldConfig }]
```

**Особенности:**
- Исключает пустые значения (null, undefined, '', [], пустые объекты)
- Для `range` проверяет, что хотя бы одно значение (min или max) заполнено
- Возвращает полную информацию о поле (`fieldConfig`)

### Пример использования

```javascript
import { getActiveFilters } from './helpers/filter-utils.js';

const filtersSummary = document.querySelector('ui-filters-summary');
const formController = document.querySelector('ui-form-controller');

formController.addEventListener('ui-form:change', () => {
  const activeFilters = getActiveFilters(formController);
  filtersSummary.setFilters(activeFilters.map(f => ({
    id: f.id,
    label: f.fieldConfig.label || f.id,
    value: Array.isArray(f.value) 
      ? f.value.join(', ') 
      : (typeof f.value === 'object' && f.value !== null && !Array.isArray(f.value))
        ? `${f.value.min || ''}-${f.value.max || ''}`
        : String(f.value),
    removable: true
  })));
});

filtersSummary.addEventListener('ui-filters-summary:remove', (e) => {
  const { filterId } = e.detail;
  const field = formController.getField(filterId);
  if (field) {
    const fieldConfig = field.state.meta?.fieldConfig;
    const defaultValue = fieldConfig?.type === 'select-multi' ? [] : null;
    field.setState({ value: defaultValue });
  }
});
```