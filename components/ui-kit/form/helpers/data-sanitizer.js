// TODO: Implement functions to sanitize and normalize form data before validation/submission.
// Expected responsibilities:
// - Map raw field values to data model structure defined in `core/data-model.json`.
// - Coerce types (string → number, trim whitespace, handle locale-specific formats).
// - Apply default values and remove transient UI-only props.


