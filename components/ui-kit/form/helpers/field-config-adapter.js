// TODO: Convert project-specific datasets (e.g., catalog data-model.json) into generic form field configs.
// Responsibilities:
// - Map domain attributes to universal config schema consumable by ui-kit components.
// - Inject presentation metadata (icons, colors) via adapter without polluting source datasets.
// - Support extensibility for other projects by keeping the adapter loosely coupled.


