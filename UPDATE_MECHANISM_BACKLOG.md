# Backlog: Механизм принудительного обновления сайта

## 🎯 Цель

Реализовать механизм уведомления пользователя о новой версии сайта с принудительным обновлением через перезагрузку страницы.

## 📋 Задачи

### 1. ~~Обновить генерацию версии~~ — НЕ ТРЕБУЕТСЯ

**Файл:** `core/asset-versioning.php`

**Решение:** Оставить генерацию версии на основе всех файлов темы (как сейчас).

**Обоснование:** Обычно обновления затрагивают либо все файлы, либо точечно JS/CSS, поэтому отслеживание всех файлов является правильным подходом.

---

### 2. Создать inline script для проверки версии (в PHP)

**Файл:** `header.php` или `functions.php` (через `wp_head`)

**Задача:** Добавить inline script ДО загрузки `app.js` и `components.js`, который:
- Проверяет наличие параметра `forceUpdate=true` в URL
- Если параметр есть → обновляет версию в LocalStorage и убирает параметр из URL
- Если параметра нет → сравнивает версию из `window.APP_VERSION` с версией в LocalStorage
  - Если совпадают → ничего не делает
  - Если не совпадают → показывает уведомление и готовится к перезагрузке

**Логика работы:**
```javascript
// Псевдокод
const urlParams = new URLSearchParams(window.location.search);
const forceUpdate = urlParams.get('forceUpdate') === 'true';

if (forceUpdate) {
  // Обновляем версию в LocalStorage
  localStorage.setItem('app_version', window.APP_VERSION);
  
  // Убираем параметр из URL (через history.replaceState)
  urlParams.delete('forceUpdate');
  const newUrl = window.location.pathname + (urlParams.toString() ? '?' + urlParams.toString() : '');
  window.history.replaceState({}, '', newUrl);
} else {
  // Стандартная проверка
  const currentVersion = window.APP_VERSION;
  const savedVersion = localStorage.getItem('app_version');
  
  if (!savedVersion || savedVersion !== currentVersion) {
    // Показываем уведомление
    showUpdateNotification();
  }
}
```

**Ожидаемый результат:** Скрипт работает ДО загрузки app.js, предотвращая кеширование логики проверки.

---

### 3. Создать блокирующее уведомление об обновлении (inline)

**Файл:** Встроить в inline script в `header.php` (ДО app.js)

**Задача:** Реализовать функцию `showUpdateNotification()`, которая:
- Показывает **блокирующее** overlay/modal, полностью перекрывающее страницу
- Блокирует взаимодействие с сайтом до обновления
- Отображает текст: "Доступна новая версия сайта. Обновление через N секунд..."
- Показывает кнопку "Обновить сейчас"
- Имеет таймер обратного отсчёта (5 секунд)
- По истечении таймера или при клике на кнопку → перезагружает страницу с параметрами `_refresh` и `forceUpdate=true`

**Метод перезагрузки:**
```javascript
function forceReload() {
  const url = new URL(window.location.href);
  url.searchParams.set('_refresh', Date.now());
  url.searchParams.set('forceUpdate', 'true');
  window.location.href = url.toString();
}
```

**Стили:** Создать inline CSS блок в `<head>` (ДО загрузки components.js), так как UI-Kit ещё не загружен:
- Блокирующий overlay (полупрозрачный фон на весь экран, z-index максимальный)
- Модальное окно в центре экрана
- Адаптивным (мобильная версия)
- С простой анимацией появления

**Проверка первого посещения:**
- Если `localStorage.app_version` отсутствует → это первый визит или первый после очистки
- В этом случае сохраняем текущую версию и НЕ показываем уведомление
- Если версия в LocalStorage есть и не совпадает с текущей → показываем уведомление

**Ожидаемый результат:** Пользователь видит блокирующее уведомление и не может использовать сайт до обновления.

---

### 4. Синхронизация версий между вкладками

**Файл:** В том же inline script

**Задача:** Добавить слушатель события `storage` для синхронизации версий между вкладками.

**Логика:**
- Когда в одной вкладке обновляется версия в LocalStorage, другие вкладки должны это узнать
- При изменении `app_version` в LocalStorage → проверять версию снова
- Если версия не совпадает → показывать уведомление

**Ожидаемый результат:** Если пользователь обновил одну вкладку, другие вкладки тоже узнают об обновлении.

---

### 5. Перевести все компоненты на статические импорты

**Файл:** `components/components.js`, `app/app.js`, и все компоненты с динамическими импортами

**Задача:** Убрать все динамические импорты из registry и внутри компонентов, заменив их на статические импорты.

**Список файлов с динамическими импортами:**

**В `components/components.js` (registry):**
- Все компоненты из registry (около 25 компонентов)

**В `app/app.js`:**
- `./forms/index.js`
- `../components/catalog/catalog-store.js`
- `../components/accessory-catalog/accessory-catalog-store.js`
- `../components/cart/cart-store.js`
- `../components/user-profile/services/auth-service.js`
- `../components/user-profile/services/user-service.js`
- `./analytics/yandex-metrika.js`
- `../components/ui-kit/modal/modal.js` (если ещё есть)
- `../components/ui-kit/notification/notification.js` (если ещё есть)

**В компонентах:**
- `components/toy-instance/toy-instance-card/toy-instance-card.js` - `toy-instance-modal`
- `components/cart/cart-item/cart-item.js` - `toy-instance-modal`
- `components/navigation/site-header/site-header.js` - `user-ui-service`, `user-menu`
- `components/cart/cart-page/cart-page.js` - `order-wizard`, `price-formatter`
- `components/cart/order-wizard/order-wizard.js` - шаги wizard
- `components/cart/order-wizard/steps/step-auth.js` - `user-ui-service`
- `components/cart/order-wizard/steps/step-personal.js` - `forms/index.js`
- `components/user-profile/profile-page/profile-page.js` - табы профиля
- `components/user-profile/profile-page/tabs/order-history/order-history.js` - `order-card`
- `components/user-profile/user-menu/user-menu.js` - `user-ui-service`

**Примечание:** Версионирование (`?v=`) в статических импортах **оставляем** для дополнительной надёжности.

**Ожидаемый результат:** Все компоненты загружаются статически, что гарантирует их обновление при `location.reload()`.

---

### 6. ~~Убрать версионирование из статических импортов~~ — НЕ ТРЕБУЕТСЯ

**Решение:** Оставить версионирование (`?v=`) в статических импортах для дополнительной надёжности. Механизмы дополняют друг друга, а не противоречат.

---

### 7. Тестирование

**Задачи:**
1. **Локальное тестирование:**
   - Изменить любой JS файл
   - Проверить, что версия обновилась
   - Проверить, что уведомление появляется
   - Проверить, что при клике/таймере страница перезагружается
   - Проверить, что после перезагрузки уведомление не появляется

2. **Тестирование множественных вкладок:**
   - Открыть сайт в двух вкладках
   - Обновить одну вкладку
   - Проверить, что вторая вкладка тоже получила уведомление

3. **Тестирование race condition:**
   - Показать уведомление
   - Нажать F5 (обычная перезагрузка) до истечения таймера
   - Проверить, что при следующей загрузке уведомление появляется снова

4. **Тестирование производительности:**
   - Проверить время загрузки страницы со всеми статическими импортами
   - Убедиться, что это приемлемо

---

## 🔄 Порядок выполнения

1. ✅ **Задача 2** — Создать inline script для проверки версии (с inline CSS для уведомления)
2. ✅ **Задача 3** — Создать блокирующее уведомление (inline)
3. ✅ **Задача 4** — Синхронизация между вкладками
4. ✅ **Задача 5** — Перевести все компоненты на статические импорты (оставить версионирование)
5. ✅ **Задача 7** — Тестирование

---

## 📝 Дополнительные заметки

### Параметры URL для перезагрузки:
- `_refresh` — timestamp для сброса кеша браузера
- `forceUpdate=true` — флаг, что это принудительное обновление (обновляем LocalStorage)

### Хранение версии:
- **LocalStorage key:** `app_version`
- **Значение:** Версия из `window.APP_VERSION` (timestamp)

### Очистка URL:
После обработки `forceUpdate=true` параметр убирается из URL через `history.replaceState()`, чтобы не мешать пользователю.

### Проверка первого посещения:
- Если `localStorage.app_version` отсутствует → первый визит
- Сохраняем текущую версию и НЕ показываем уведомление
- Если версия есть и не совпадает → показываем уведомление

---

## ⚠️ Важные моменты

1. **Inline script должен быть ДО app.js** — иначе сам app.js может быть из кеша
2. **Inline CSS должен быть ДО components.js** — UI-Kit ещё не загружен
3. **Уведомление блокирующее** — пользователь не может использовать сайт до обновления
4. **Уведомление показывать сразу** — не дожидаться загрузки страницы
5. **Таймер 5 секунд** — автоматическое обновление
6. **Все компоненты статически** — для гарантированного обновления (версионирование оставляем)
7. **Версия от всех файлов** — отслеживание всех изменений в теме
8. **Проверка первого посещения** — если нет версии в LocalStorage, сохраняем и не показываем уведомление

---

## 🎨 Пример UI уведомления

**Тип:** Блокирующий overlay/modal

```
┌─────────────────────────────────────────────────────┐
│                                                     │
│         ┌─────────────────────────────────┐         │
│         │ 🎉 Доступна новая версия сайта  │         │
│         │                                 │         │
│         │ Обновление автоматически через  │         │
│         │ 3 секунды...                    │         │
│         │                                 │         │
│         │      [Обновить сейчас]          │         │
│         └─────────────────────────────────┘         │
│                                                     │
└─────────────────────────────────────────────────────┘
```

**Позиция:** Блокирующий overlay на весь экран (z-index: 999999)
**Стиль:** Inline CSS в `<head>` (UI-Kit ещё не загружен)

---

## ✅ Критерии готовности

- [ ] Inline script проверяет версию ДО загрузки app.js
- [ ] Уведомление показывается сразу при несовпадении версий
- [ ] При наличии `forceUpdate=true` версия обновляется в LocalStorage
- [ ] Параметр `forceUpdate=true` убирается из URL после обработки
- [ ] Inline CSS для уведомления добавлен в `<head>` ДО components.js
- [ ] Уведомление блокирующее (overlay на весь экран)
- [ ] Таймер работает 5 секунд с обратным отсчётом
- [ ] Перезагрузка использует метод с `_refresh` и `forceUpdate=true`
- [ ] Проверка первого посещения работает (не показываем уведомление при отсутствии версии)
- [ ] Все компоненты загружаются статически (версионирование сохранено)
- [ ] Синхронизация между вкладками работает
- [ ] Все динамические импорты найдены и переведены на статические
- [ ] Протестировано локально и на проде

---

## 🔮 Будущие улучшения

### Версионирование URL при навигации

**Проблема:** При навигации на другие страницы (не через перезагрузку) браузер может использовать кэшированные JS/CSS файлы, даже если версия на сервере уже обновлена.

**Идея:** Добавлять версию во все URL/ссылки при перехвате событий изменения истории (`popstate`, `pushstate`, клики по ссылкам) и отдавать их модифицированными.

**Подход:**
- Перехватывать события навигации (клики по ссылкам, `history.pushState`, `history.replaceState`)
- Модифицировать все внутренние ссылки, добавляя параметр версии `?v=...`
- Это гарантирует, что при переходе на новую страницу все ресурсы будут загружены с правильной версией

**Статус:** Отложено на будущее, требует дополнительного анализа и обсуждения.

