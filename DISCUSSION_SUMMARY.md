# Обсуждение механизма принудительного обновления

## 🎯 Суть предложения

1. Отказаться от динамических импортов → все скрипты статически
2. Проверять версию при загрузке через LocalStorage
3. Показывать уведомление при несовпадении версий
4. Обновлять через `location.reload()` при клике/таймере

---

## ✅ Подход РАБОТАЕТ, но есть нюансы

### Критические проблемы:

1. **`location.reload(true)` устарел**
   - Параметр `true` deprecated, может не работать
   - Использовать просто `location.reload()` — должно работать для статических импортов

2. **Race condition при обычной перезагрузке (F5)**
   - Если пользователь нажмёт F5 после показа уведомления, но до `location.reload()`
   - Может сохраниться новая версия в LocalStorage без сброса кеша
   - **Решение:** Использовать `sessionStorage` флаг `forced_reload` и отслеживать обычные перезагрузки через `beforeunload`

3. **ES Modules кешируются браузером**
   - Могут кешироваться даже при `location.reload()`
   - Но если версия в URL меняется (`?v=timestamp`), браузер должен запросить заново

4. **Версия меняется без изменения JS/CSS**
   - Сейчас версия = timestamp от последнего изменения **любого файла** в теме
   - Если обновили только PHP-файлы, версия изменится, но JS/CSS не изменились
   - **Решение:** Генерировать версию только на основе JS/CSS файлов

5. **Множественные вкладки**
   - Если обновил одну вкладку, другие не знают
   - **Решение:** Слушать `storage` событие в LocalStorage для синхронизации

6. **Производительность**
   - Статическая загрузка всех скриптов увеличит размер начального бандла
   - Можно сделать гибрид: критичные компоненты — статически, редкие — динамически

---

## 💡 Рекомендации

### Что точно нужно сделать:

1. ✅ Отслеживать обычные перезагрузки через `beforeunload` + `sessionStorage`
2. ✅ Сохранять версию в LocalStorage **только после принудительного обновления**
3. ✅ Генерировать версию только на основе JS/CSS файлов (не PHP)
4. ✅ Синхронизировать версии между вкладками через `storage` событие
5. ✅ Использовать `location.reload()` без параметра `true`

### Логика работы:

```
При загрузке страницы:
├─ Сравниваем window.APP_VERSION с localStorage.app_version
├─ Если не совпадают:
│  ├─ Проверяем sessionStorage.forced_reload
│  ├─ Если был forced_reload, но версия всё ещё не совпадает:
│  │  └─ Пользователь обновил обычным способом → показываем уведомление снова
│  └─ Если forced_reload не было:
│     └─ Показываем уведомление
└─ Если совпадают:
   └─ Всё ок, продолжаем работу

При клике "Обновить" / таймере:
├─ sessionStorage.setItem('forced_reload', 'true')
├─ localStorage.setItem('app_version', window.APP_VERSION)
└─ location.reload()
```

---

## ❓ Вопросы для обсуждения

1. **UX уведомления:**
   - Показывать сразу при загрузке или после полной загрузки страницы?
   - Автоматическое обновление после таймера? Какой таймер (5 сек, 10 сек)?
   - Можно ли закрыть уведомление без обновления?

2. **Производительность:**
   - Загружать ВСЕ компоненты статически или только критичные?
   - Текущие динамические импорты: `cart-page`, `profile-page`, `order-wizard` — они редкие, можно оставить динамическими?

3. **Версионирование:**
   - Генерировать версию только на основе JS/CSS или всех файлов?
   - Может быть, использовать hash от содержимого файлов вместо timestamp?

4. **Откат:**
   - Что делать, если новая версия сломалась и нужно откатиться?
   - Нужен ли механизм принудительного сброса версии?

---

## 🎯 Вывод

**Механизм сработает**, но нужно учесть все описанные проблемы. Основная сложность — правильно обработать race condition при обычной перезагрузке страницы.

**Предлагаю:**
1. Обсудить все вопросы выше
2. Согласовать логику работы
3. Реализовать с учётом всех нюансов

**Время реализации:** ~1-2 дня с тестированием

