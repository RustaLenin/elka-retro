# TODO: Рефакторинг форм

Этот файл содержит задачи по миграции старых форм на новый подход с централизованными конфигурациями в `app/forms/`.

**См. документацию:** [`app/forms/README.md`](./README.md)

## Подход

Конфигурации форм теперь хранятся в `app/forms/` как часть логики приложения, а не компонентов.

### Принципы:
- Формы инициализируются через атрибут `config-path` в шаблоне
- Компонент не должен программно управлять формой после создания в DOM
- Конфигурация отделена от компонента

### Как мигрировать:
1. Создать файл `app/forms/[название-формы].js` с конфигурацией
2. Зарегистрировать в `app/forms/index.js`
3. Обновить шаблон: `<ui-form-controller config-path="window.app.forms.[название]"></ui-form-controller>`
4. Убрать всю логику `_initForm`, `_initFormAsync`, `setState`, `configure` из компонента

## Задачи по миграции

### 🔴 Высокий приоритет (модальные окна авторизации)

#### 1. `register-modal`
**Файл:** `components/user-profile/modals/register-modal/register-modal.js`

**Текущее состояние:**
- Использует `_initFormAsync()` и `_initForm()` для программной инициализации
- Конфигурация формы (поля, pipeline) определена внутри метода `_initForm()`

**Действия:**
- [ ] Создать `app/forms/register.js` с конфигурацией формы регистрации
- [ ] Перенести конфигурацию полей из `_initForm()` в `app/forms/register.js`:
  - Email (обязательное, валидация email)
  - Login (обязательное, минимум 3 символа)
  - Phone (обязательное, валидация телефона)
  - Password (обязательное, минимум 16 символов, сложность)
  - Confirm Password (обязательное, совпадение с паролем)
- [ ] Перенести pipeline (sanitize, validate, submit, onSuccess, onError) в конфигурацию
- [ ] Зарегистрировать в `app/forms/index.js` как `window.app.forms.register`
- [ ] Обновить шаблон `register-modal-template.js`: добавить `config-path="window.app.forms.register"`
- [ ] Удалить методы `_initFormAsync()` и `_initForm()` из компонента
- [ ] Убрать флаг `_formInitialized` и связанную логику
- [ ] Упростить `_updateModalVisibility()` - убрать проверки инициализации формы
- [ ] Обновить обработчики ссылок (оставить только привязку обработчиков в `connectedCallback`)

#### 2. `password-reset-modal`
**Файл:** `components/user-profile/modals/password-reset-modal/password-reset-modal.js`

**Текущее состояние:**
- Использует `_initFormAsync()` и `_initForm()` для программной инициализации
- Конфигурация формы определена внутри метода `_initForm()`
- Имеет особую логику для состояния `submitted` (показывает сообщение об успехе)

**Действия:**
- [ ] Создать `app/forms/password-reset.js` с конфигурацией формы восстановления пароля
- [ ] Перенести конфигурацию полей (email) в `app/forms/password-reset.js`
- [ ] Перенести pipeline в конфигурацию
- [ ] Зарегистрировать в `app/forms/index.js` как `window.app.forms.passwordReset`
- [ ] Обновить шаблон `password-reset-modal-template.js`: добавить `config-path="window.app.forms.passwordReset"`
- [ ] Удалить методы `_initFormAsync()` и `_initForm()` из компонента
- [ ] Упростить `_updateModalVisibility()` - убрать проверки инициализации формы
- [ ] Обработать состояние `submitted` через события формы (возможно, нужно модифицировать `onSuccess` в конфигурации)

### 🟡 Средний приоритет (страница профиля)

#### 3. `profile-settings`
**Файл:** `components/user-profile/profile-page/tabs/profile-settings/profile-settings.js`

**Текущее состояние:**
- Использует метод `_initForms()` для инициализации нескольких форм:
  - Форма редактирования профиля
  - Форма смены пароля

**Действия:**
- [ ] Создать `app/forms/profile-edit.js` с конфигурацией формы редактирования профиля:
  - Поля: Имя, Фамилия, Email, Phone, Display Name, Адрес, Ссылка на мессенджер
  - Pipeline для обновления профиля через `user-service.js`
- [ ] Создать `app/forms/profile-change-password.js` с конфигурацией формы смены пароля:
  - Поля: Текущий пароль, Новый пароль, Подтверждение пароля
  - Pipeline для смены пароля
- [ ] Зарегистрировать обе формы в `app/forms/index.js`
- [ ] Обновить шаблон `profile-settings-template.js`: использовать `config-path` для обеих форм
- [ ] Удалить метод `_initForms()` из компонента
- [ ] Упростить логику компонента

#### 4. `contact-form`
**Файл:** `components/user-profile/profile-page/tabs/contact-form/contact-form.js`

**Текущее состояние:**
- Использует метод `_initForm()` для программной инициализации
- Конфигурация формы определена внутри метода `_initForm()`

**Действия:**
- [ ] Создать `app/forms/contact.js` с конфигурацией формы обратной связи:
  - Поля: Тема (subject), Сообщение (message)
  - Pipeline для отправки сообщения на `zakaz@elka-retro.ru`
- [ ] Зарегистрировать в `app/forms/index.js` как `window.app.forms.contact`
- [ ] Обновить шаблон `contact-form-template.js`: добавить `config-path="window.app.forms.contact"`
- [ ] Удалить метод `_initForm()` из компонента
- [ ] Упростить логику компонента

### 🟢 Низкий приоритет (другие формы)

#### 5. `step-personal` (форма личных данных в оформлении заказа)
**Файл:** `components/cart/order-wizard/steps/step-personal.js`

**Текущее состояние:**
- Использует метод `initForm()` для программной инициализации
- Конфигурация формы определена внутри метода `initForm()`
- Это часть мастера оформления заказа, может иметь специфическую логику

**Действия:**
- [ ] Создать `app/forms/order-personal.js` с конфигурацией формы личных данных
- [ ] Перенести конфигурацию полей (email, username, phone, name, surname)
- [ ] Перенести pipeline в конфигурацию
- [ ] Зарегистрировать в `app/forms/index.js` как `window.app.forms.orderPersonal`
- [ ] Обновить шаблон: использовать `config-path`
- [ ] Удалить метод `initForm()` из компонента
- [ ] Упростить логику компонента

#### 6. `catalog-sidebar` (динамическая форма фильтров)
**Файл:** `components/catalog/sidebar/catalog-sidebar.js`

**Текущее состояние:**
- Создаёт форму программно через `document.createElement('ui-form-controller')`
- Конфигурация формируется динамически на основе фильтров каталога
- Использует атрибуты JSON для передачи конфигурации

**Действия:**
- [ ] Рассмотреть возможность использования `config-path` с динамическим путём
- [ ] Или оставить текущий подход, если динамическая генерация конфигурации необходима
- [ ] **Примечание:** Этот случай может требовать особого подхода, так как конфигурация генерируется динамически

## Общие улучшения

- [ ] Добавить типизацию конфигураций форм (JSDoc или TypeScript)
- [ ] Создать утилиты для переиспользования общих паттернов pipeline (например, стандартные обработчики ошибок)
- [ ] Добавить примеры использования в документации

## Примечания

- После миграции каждой формы проверьте, что всё работает корректно
- Убедитесь, что события форм правильно обрабатываются компонентами
- Проверьте обработку ошибок и успешных отправок
- Убедитесь, что состояние модальных окон обновляется корректно

## Ссылки

- **Документация:** [`app/forms/README.md`](./README.md)
- **Пример мигрированной формы:** [`app/forms/sign-in.js`](./sign-in.js)
- **Пример использования:** `components/user-profile/modals/auth-modal/auth-modal-template.js`

