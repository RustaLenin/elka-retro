# План рефакторинга форм под новую архитектуру

**Документация по архитектуре:** [`components/ui-kit/form/controller/ARCHITECTURE.md`](../../components/ui-kit/form/controller/ARCHITECTURE.md)

## Изменения в архитектуре

### Ключевые изменения:

1. **Синхронизация через ссылки** (State Sharing by Reference)
   - Единый источник истины: `form.state.values`
   - Автоматическая синхронизация через getter/setter
   - Не нужно слушать события для синхронизации состояния

2. **Поля рендерятся автоматически** из конфигурации
   - Не нужно создавать поля вручную в HTML
   - Форма рендерит поля напрямую из `fields` конфигурации

3. **Режим `mode="modal"`** для модальных окон
   - Не рендерит header (использует title модального окна)
   - Рендерит actions как `[data-form-actions]` для перемещения в footer

4. **Pipeline handlers** принимают `payload` (контекст)
   - `payload = { controller, values }` (или `{ controller, values, result }` для onSuccess)
   - Для обратной совместимости можно использовать `context.values`

5. **Конфигурация через `config-path`**
   - Путь может быть `app.forms.signIn` (без `window`) или `window.app.forms.signIn`

## Задачи рефакторинга

### ✅ 1. Обновить конфигурацию формы авторизации (`app/forms/sign-in.js`)

**Текущее состояние:**
- Использует старую сигнатуру pipeline (`context` вместо `payload`)
- Pipeline handlers получают `context` и используют `context.values`

**Действия:**
- [x] Проверить, что конфигурация использует `config-path="app.forms.signIn"` (или `window.app.forms.signIn`)
- [ ] Обновить pipeline handlers для использования `payload` вместо `context`:
  - `sanitize: (payload) => payload.values`
  - `validate: (payload) => { ... }` где `payload.values`
  - `submit: (payload) => payload.values`
  - `onSuccess: (payload) => { ... }` где `payload.result`
  - `onError: (payload, error) => { ... }`

**Примечание:** Старая сигнатура (`context`) должна работать, но лучше использовать новую.

### 2. Обновить шаблон `auth-modal-template.js`

**Текущее состояние:**
- Использует `config-path="window.app.forms.signIn"` - можно упростить
- Отсутствует `mode="modal"` - нужно добавить

**Действия:**
- [ ] Изменить `config-path` на `app.forms.signIn` (без `window`)
- [ ] Добавить `mode="modal"` для правильной интеграции с модальным окном

### 3. Мигрировать `register-modal`

**Текущее состояние:**
- Использует программную инициализацию через `_initForm()` и `_initFormAsync()`
- Конфигурация формы определена внутри компонента

**Действия:**
- [ ] Создать `app/forms/register.js` с конфигурацией формы регистрации
- [ ] Перенести конфигурацию полей:
  - Email (обязательное, валидация email)
  - Login (обязательное, минимум 3 символа)
  - Phone (обязательное, валидация телефона)
  - Password (обязательное, минимум 16 символов, сложность)
  - Confirm Password (обязательное, совпадение с паролем)
- [ ] Перенести pipeline в конфигурацию (обновить сигнатуру на `payload`)
- [ ] Зарегистрировать в `app/forms/index.js` как `window.app.forms.register`
- [ ] Обновить шаблон `register-modal-template.js`:
  - Добавить `config-path="app.forms.register"`
  - Добавить `mode="modal"`
- [ ] Удалить методы `_initFormAsync()` и `_initForm()` из компонента
- [ ] Упростить `_updateModalVisibility()` - убрать проверки инициализации формы
- [ ] Убрать флаг `_formInitialized` и связанную логику

### 4. Мигрировать `password-reset-modal`

**Текущее состояние:**
- Использует программную инициализацию через `_initForm()` и `_initFormAsync()`
- Конфигурация формы определена внутри компонента
- Имеет особую логику для состояния `submitted` (показывает сообщение об успехе)

**Действия:**
- [ ] Создать `app/forms/password-reset.js` с конфигурацией формы восстановления пароля
- [ ] Перенести конфигурацию полей (email)
- [ ] Перенести pipeline в конфигурацию (обновить сигнатуру на `payload`)
- [ ] Обработать состояние `submitted` через `onSuccess` в pipeline (можно диспатчить событие)
- [ ] Зарегистрировать в `app/forms/index.js` как `window.app.forms.passwordReset`
- [ ] Обновить шаблон `password-reset-modal-template.js`:
  - Добавить `config-path="app.forms.passwordReset"`
  - Добавить `mode="modal"`
- [ ] Удалить методы `_initFormAsync()` и `_initForm()` из компонента
- [ ] Упростить `_updateModalVisibility()` - убрать проверки инициализации формы
- [ ] Убрать флаг `_formInitialized` и связанную логику

### 5. Мигрировать `profile-settings`

**Текущее состояние:**
- Использует метод `_initForms()` для инициализации нескольких форм:
  - Форма редактирования профиля
  - Форма смены пароля

**Действия:**
- [ ] Создать `app/forms/profile-edit.js` с конфигурацией формы редактирования профиля:
  - Поля: Имя, Фамилия, Email, Phone, Display Name, Адрес, Ссылка на мессенджер
  - Pipeline для обновления профиля через `user-service.js`
- [ ] Создать `app/forms/profile-change-password.js` с конфигурацией формы смены пароля:
  - Поля: Текущий пароль, Новый пароль, Подтверждение пароля
  - Pipeline для смены пароля
- [ ] Зарегистрировать обе формы в `app/forms/index.js`
- [ ] Обновить шаблон `profile-settings-template.js`: использовать `config-path` для обеих форм
- [ ] Удалить метод `_initForms()` из компонента
- [ ] Упростить логику компонента

### 6. Мигрировать `contact-form`

**Текущее состояние:**
- Использует метод `_initForm()` для программной инициализации
- Конфигурация формы определена внутри метода `_initForm()`

**Действия:**
- [ ] Создать `app/forms/contact.js` с конфигурацией формы обратной связи:
  - Поля: Тема (subject), Сообщение (message)
  - Pipeline для отправки сообщения на `zakaz@elka-retro.ru`
- [ ] Зарегистрировать в `app/forms/index.js` как `window.app.forms.contact`
- [ ] Обновить шаблон `contact-form-template.js`: добавить `config-path="app.forms.contact"`
- [ ] Удалить метод `_initForm()` из компонента
- [ ] Упростить логику компонента

## Изменения в конфигурации форм

### Формат pipeline handlers

**Старая сигнатура (работает, но устарела):**
```javascript
pipeline: {
  sanitize: (context) => {
    const values = context.values || {};
    return { ... };
  },
  validate: async (context) => {
    const values = context.values || {};
    return { ... };
  },
  submit: async (context) => {
    const values = context.values || {};
    return result;
  },
  onSuccess: (context) => {
    // context.result, context.values
  },
  onError: (context, error) => {
    // context.values, error
  }
}
```

**Новая сигнатура (рекомендуется):**
```javascript
pipeline: {
  sanitize: (payload) => {
    const { values } = payload; // или payload.values
    return { ... };
  },
  validate: async (payload) => {
    const { values } = payload;
    return { valid: true, errors: [] };
  },
  submit: async (payload) => {
    const { values } = payload;
    return result;
  },
  onSuccess: (payload) => {
    const { result, values } = payload;
    // Обработка успеха
  },
  onError: (payload, error) => {
    const { values } = payload;
    // Обработка ошибки
  }
}
```

**Встроенные обработчики:**
```javascript
pipeline: {
  sanitize: true,  // Использовать встроенный обработчик
  validate: true,  // Использовать встроенный обработчик (на основе validation правил из fields)
  submit: async (payload) => {
    const { values } = payload;
    // Кастомная отправка
  }
}
```

### Формат полей

Поля должны соответствовать формату UI Form Kit. Основные свойства:
- `id` - уникальный идентификатор поля
- `type` - тип поля (`text`, `number`, `checkbox`, `select-single`, `select-multi`, `range`)
- `label` - подпись поля
- `required` - обязательное поле
- `validation` - массив правил валидации или объект `{ rules: [...] }`

**Старый формат (работает):**
```javascript
{
  id: 'email',
  type: 'text',
  label: 'Email',
  required: true,
  validation: [
    { rule: 'required', value: true, message: 'Email обязателен', severity: 'error' },
    { rule: 'email', value: true, message: 'Некорректный email', severity: 'error' }
  ]
}
```

**Новый формат (рекомендуется):**
```javascript
{
  id: 'email',
  type: 'text',
  label: 'Email',
  required: true,
  validation: {
    rules: ['required', 'email']
  }
}
```

## Приоритеты

### 🔴 Высокий приоритет
1. Обновить `sign-in.js` - адаптировать под новую архитектуру
2. Обновить шаблон `auth-modal-template.js` - добавить `mode="modal"` и упростить `config-path`

### 🟡 Средний приоритет
3. Мигрировать `register-modal` - перенести конфигурацию в `app/forms/register.js`
4. Мигрировать `password-reset-modal` - перенести конфигурацию в `app/forms/password-reset.js`

### 🟢 Низкий приоритет
5. Мигрировать `profile-settings` - перенести конфигурации в `app/forms/profile-edit.js` и `app/forms/profile-change-password.js`
6. Мигрировать `contact-form` - перенести конфигурацию в `app/forms/contact.js`

## Проверочный список после рефакторинга

После миграции каждой формы проверить:

- [ ] Форма инициализируется автоматически через `config-path`
- [ ] Поля рендерятся автоматически из конфигурации
- [ ] Для модальных окон добавлен `mode="modal"`
- [ ] Кнопки формы находятся в footer модального окна (для модальных форм)
- [ ] Pipeline handlers используют правильную сигнатуру
- [ ] События формы корректно обрабатываются компонентами
- [ ] Обработка ошибок работает корректно
- [ ] Состояние модальных окон обновляется корректно
- [ ] Удалена вся логика программной инициализации (`_initForm`, `setState`, `configure`)
- [ ] Компонент не управляет формой после её создания в DOM

## Дополнительные улучшения

После базовой миграции можно рассмотреть:

- [ ] Использование встроенных обработчиков `sanitize: true`, `validate: true` где возможно
- [ ] Упрощение валидации через `validation: { rules: [...] }` вместо массива объектов
- [ ] Использование утилит из `form-pipeline.js` для создания pipeline handlers
- [ ] Типизация конфигураций форм (JSDoc или TypeScript)

